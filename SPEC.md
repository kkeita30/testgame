# Survivor Bites 仕様書

現在バージョン: **v1.7.1**

ブラウザで動作するシンプルな2Dヴァンサバ(Vampire Survivors)ライクゲーム。
Canvas + バニラJS、ビルド不要、外部ライブラリ不使用。タッチ操作対応。

対象ファイル: `index.html` / `style.css` / `main.js`

---

## 1. 基本コンセプト

- プレイヤーは画面中央のキャラクターを操作し、四方から迫る敵から生き残る
- 攻撃は自動(最寄りの敵に向けて自動発射)。プレイヤーの操作は「移動」のみ
- 敵を倒すとXPジェムをドロップし、一定量集めるとレベルアップして強化を選択
- HPが0になるとゲームオーバー。生存時間・撃破数・到達レベルを競う

---

## 2. 操作方法

### 移動(ドラッグ操作、視覚的なスティックUIなし)
- 画面(canvas)上の任意の場所をタッチ/クリックし、そのまま指・カーソルをドラッグすると、押した位置からの方向・距離に応じてプレイヤーが移動する
- 仮想スティックのグラフィックは表示しない(タッチ位置とUI表示のズレを避けるため)
- ドラッグ距離が `DRAG_RADIUS = 42px` に達すると最大速度になる(アナログ入力)
- 指が画面外に出て `touchend`/`touchcancel` が発生しない場合でも、新しい `touchstart` が来ると常にそちらを掴み直す(古いタッチにロックされて操作不能になることを防止)
- デスクトップ用にマウスドラッグと矢印キー/WASDにも対応

### その他の操作
- 画面上部中央の一時停止ボタン(`II` / `>`)でポーズ/再開
- レベルアップ画面・一時停止解除後は、実際に画面をタップ/ドラッグ開始するまでゲームが動き出さない(「タップして再開」表示。詳細は §6)

---

## 3. プレイヤー

### 初期ステータス(`Player` クラス、`BASELINE_STATS` 相当)

| ステータス | 初期値 |
|---|---|
| 移動速度 (`baseSpeed`) | 190 |
| 最大HP (`maxHp`) | 100 |
| ダメージ (`damage`) | 10 |
| 攻撃間隔 (`atkCooldown`) | 0.7秒 |
| 弾速 (`projSpeed`) | 380 |
| 同時発射数 (`projCount`) | 1 |
| 貫通数 (`pierce`) | 0 |
| XP回収範囲 (`pickupRadius`) | 70 |
| HP自然回復 (`regen`) | 0/秒 |

- 被弾すると0.6秒の無敵時間が発生(点滅表示)
- レベルアップに必要なXP: 初期10、レベルアップごとに `xpNext = round(xpNext * 1.35 + 5)`

### 攻撃ロジック
- 攻撃間隔ごとに、最も近い敵から `projCount` 体を選び、それぞれに向けて弾を発射
- 貫通(`pierce`)が残っている限り、1発の弾が複数の敵を貫通してダメージを与える

---

## 4. レベルアップ・強化選択

レベルアップ時、ランダムな3択 + 常設の「スキップ」の計4択から1つを選ぶ。

### 強化プール(ランダム3択、`UPGRADE_POOL`)

| 強化 | 効果 |
|---|---|
| ダメージ強化 | 攻撃ダメージ +50% |
| 攻撃速度アップ | 攻撃間隔 -15%(下限0.15秒) |
| 移動速度アップ | 移動速度 +12% |
| 最大HPアップ | 最大HP +25、HP+25回復 |
| マルチショット | 同時発射数 +1 |
| 回収範囲アップ | XP回収範囲 +30 |
| 貫通強化 | 貫通数 +1 |
| リジェネ | HP自然回復 +1/秒 |

### スキップ(常設4つ目の選択肢)
- ステータス強化は一切行わない
- 次のレベルアップに必要なXPの **30%** を獲得した状態にする(XPゼロ戻しではない)
- 選択直後にHUDのXPバーへ即座に反映される

### レベルアップ後の一時停止解除待ち
- 強化(またはスキップ)を選んだ直後は、指が画面から離れているため無操作状態になる
- そのため選択後は「タップして再開」を表示し、実際に画面をタップ/ドラッグ開始するまでゲームを停止したままにする
- 一時停止ボタンでの再開時も同じ仕組みを適用(ボタンを押した直後はまだ操作していないため)

---

## 5. 敵

### 敵タイプ(`ENEMY_TYPES`)

| タイプ | HP(基礎) | 速度 | 半径 | 接触ダメージ(基礎) | XP | 解禁条件 |
|---|---|---|---|---|---|---|
| grunt(通常) | 18 | 78 | 13 | 8 | 3 | 常時出現 |
| fast(高速) | 10 | 140 | 10 | 6 | 4 | 敵Lv.2以上(出現率50%) |
| tank(タンク) | 70 | 48 | 20 | 14 | 10 | 敵Lv.5以上(出現率22%) |

- 最大HPが15を超える敵は頭上にHPバーを表示
- 敵は常にプレイヤーへ直進し、接触するとダメージ(0.5秒クールダウン)を与える
- 撃破するとXPジェムをドロップし、パーティクルが散る

### 敵レベル(`enemyLevel`)
- 初期値は1。60秒ごとに「撃破率に応じた昇降判定」が行われる、階段状の単一の難易度指標
- HUDのタイマー横に「敵Lv.N」として常時表示
- 敵HP・敵ダメージのベース倍率、出現間隔、同時出現数、敵種解禁のすべてがこの指標で駆動される

#### 撃破率による昇降判定(60秒ごと)
ゲーム開始からの累計出現数(`totalSpawned`)と累計撃破数(`kills`)から撃破率を算出し、次の60秒間の敵レベルを決める(直近60秒だけでなく、ラン全体の累計で判定する点に注意):

```
撃破率 = kills / totalSpawned  (ゲーム開始からの累計)
```

| 撃破率 | 次の60秒間の敵レベル |
|---|---|
| 50%以上 | +1(通常通り上昇) |
| 25%以上50%未満 | 変化なし(据え置き) |
| 25%未満 | -1(下限は1) |

プレイヤーが敵を捌ききれていない(累計撃破率が低い)場合は難易度上昇を止める、あるいは引き下げることで、難易度が一方的に積み上がって詰むことを防ぐ「撃破率に応じた難易度緩和」の仕組み。累計ベースのため、直近1分だけが悪くても過去の好調が相殺してくれる(逆に直近だけ好調でも過去の不調をすぐには帳消しにできない)。

---

## 6. 難易度スケーリングの仕組み

難易度は「敵レベルによる緩やかな階段状の底上げ」と「プレイヤーのビルド(強化の取り方)に応じた追加スケーリング」の2層構成。**強化を取っていない軸には追加スケーリングがかからない**設計により、「攻撃力を伸ばしていないのに敵だけ硬くなる」といった噛み合わない難易度上昇を避けている。

### 6-1. 敵レベルによる階段状のベース値(`L = enemyLevel`)

| 項目 | 計算式 |
|---|---|
| 敵HP倍率 | `1 + (L-1) * 0.18` |
| 敵ダメージ倍率 | `1 + (L-1) * 0.14` |
| 出現間隔 | `max(0.22, 1.1 - (L-1) * 0.11)` 秒 |
| 同時出現数 | `1 + floor((L-1) / 5)` |
| fast出現開始 | `L >= 2` |
| tank出現開始 | `L >= 5` |

### 6-2. プレイヤーのビルドに応じた追加スケーリング

プレイヤーの現在ステータスを初期値(`BASELINE_STATS`)と比較し、強化度合いに応じた倍率を算出:

```js
offensePowerMult  = (damage / 10) * (0.7 / atkCooldown)      // ダメージ×攻撃速度
crowdPowerMult    = 1 + max(0, projCount - 1) * 0.18 + pierce * 0.15  // マルチショット×貫通
survivalPowerMult = maxHp / 100                                // 最大HP
```

これらが初期値(=1)を超えた分(`〜Extra = max(0, 〜Mult - 1)`)だけ、以下に上乗せされる:

| プレイヤーが伸ばした軸 | 追加スケーリングの対象 | 係数 |
|---|---|---|
| ダメージ × 攻撃速度 | 敵HP | `1 + offenseExtra * 0.25` |
| マルチショット × 貫通 | 出現間隔の短縮・同時出現数 | 出現間隔: `/(1 + crowdExtra * 0.5)`、同時出現数: `+ round(crowdExtra * 2)` |
| 最大HP | 敵の接触ダメージ | `1 + survivalExtra * 0.7` |

オフェンス系の係数(0.25)は意図的に低めに設定しており、ダメージ/攻撃速度を強化した際に「強くなった実感」が敵の硬さ上昇でほぼ相殺されないようにしている(例: ダメージのみ2倍にした場合、敵HPは約1.25倍に留まる)。

---

## 7. 画面・UI構成

| 画面/要素 | 内容 |
|---|---|
| スタート画面 | タイトル、操作説明、「ゲーム開始」ボタン、右下にバージョン表記(`v1.6.0`) |
| HUD | HPバー、XPバー、タイマー、プレイヤーLv.、敵Lv.、撃破数 |
| レベルアップ画面 | 強化3択 + スキップの計4択カード |
| 「タップして再開」表示 | レベルアップ後・一時停止解除後、実際の入力があるまで表示され続ける半透明オーバーレイ(操作はブロックしない) |
| ゲームオーバー画面 | 生存時間・撃破数・到達レベルを表示、「もう一度」ボタン |
| 一時停止ボタン | 画面上部中央、`II`(稼働中)/`>`(停止中)を切り替え |

---

## 8. カメラ・描画

- プレイヤーは常に画面中央に固定され、ワールド座標をカメラオフセットで描画(無限マップ的な見た目)
- 背景はシンプルなグリッド線(64px間隔)
- 被弾時に画面シェイク(0.15秒)。一時停止中はシェイクの再抽選を止め、揺れが固定されるバグを回避済み
- 敵・プレイヤー・弾・XPジェムはすべて単色の円/矩形によるシンプルな図形描画

---

## 9. 既知の設計上の注意点

- `main.js` はIIFEで完結しており、外部モジュールやビルドステップは無し
- ゲームロジックの主要な状態は `Game` クラスのインスタンス(`game`変数)に集約
- HUD更新は通常 `update()` 内でのみ行われるため、`awaitingResume` 中の変更(スキップのXP反映など)は明示的に `updateHud()` を呼ぶ必要がある
- バージョン番号は `main.js` 冒頭の `GAME_VERSION` 定数を書き換えるだけで更新可能
