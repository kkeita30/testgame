# Survivor Bites 仕様書

現在バージョン: **v1.36.100**

ブラウザで動作するシンプルな2Dヴァンサバ(Vampire Survivors)ライクゲーム。
Canvas + バニラJS、ビルド不要、外部ライブラリ不使用。タッチ操作対応。

対象ファイル: `index.html` / `style.css` / `main.js`

---

## 1. 基本コンセプト

- プレイヤーは画面中央のキャラクターを操作し、四方から迫る敵から生き残る
- 攻撃は自動(最寄りの敵に向けて自動発射)。プレイヤーの操作は「移動」のみ
- 敵を倒すとXPジェムをドロップし、一定量集めるとレベルアップして強化を選択
- HPが0になるとゲームオーバー。生存時間・撃破数・到達レベルを競う

---

## 2. 操作方法

### 移動(ドラッグ操作、視覚的なスティックUIなし)
- 画面(canvas)上の任意の場所をタッチ/クリックし、そのまま指・カーソルをドラッグすると、押した位置からの方向・距離に応じてプレイヤーが移動する
- 仮想スティックのグラフィックは表示しない(タッチ位置とUI表示のズレを避けるため)
- ドラッグ距離が `DRAG_RADIUS = 42px` に達すると最大速度になる(アナログ入力)
- 指が画面外に出て `touchend`/`touchcancel` が発生しない場合でも、新しい `touchstart` が来ると常にそちらを掴み直す(古いタッチにロックされて操作不能になることを防止)
- デスクトップ用にマウスドラッグと矢印キー/WASDにも対応

### その他の操作
- 画面上部中央の一時停止ボタン(`II`)を押すと一時停止画面が開く(詳細は §7-5)。一時停止画面内の「再開」ボタンでゲームに戻る
- レベルアップ画面を閉じた直後・一時停止から再開した直後は、短時間の無敵時間が付与される(詳細は §4-6)

---

## 3. プレイヤー

### 初期ステータス(`Player` クラス、`BASELINE_STATS` 相当)

| ステータス | 初期値 |
|---|---|
| 移動速度 (`baseSpeed`) | 210(v1.35.0で190から引き上げ。詳細は§4-1) |
| 最大HP (`maxHp`) | 100 |
| ダメージ (`damage`) | 10 |
| 攻撃間隔 (`atkCooldown`) | 0.7秒 |
| 弾速 (`projSpeed`) | 380 |
| 同時発射数 (`projCount`) | 1 |
| 貫通数 (`pierce`) | 0 |
| XP回収範囲 (`pickupRadius`) | 100(v1.35.0で70から引き上げ。詳細は§4-1) |
| HP自然回復 (`regen`) | 0/秒 |
| 射程倍率 (`rangeMult`) | 1 |

- 被弾すると0.6秒の無敵時間が発生(点滅表示)
- レベルアップに必要なXP(`xpNext`): `min(1069, round((10 + 8 * level^1.5) * 0.7))`
  - レベル数から直接計算する多項式カーブ(以前の `xpNext = round(xpNext * 1.35 + 5)` という複利式は、レベル15前後から要求量が指数的に膨れ上がりほとんど上がらなくなる問題があったため変更)
  - 上限は長時間サバイブした際の安全弁(初期は1000→3000→**1527**(v1.36.27)と変遷。3000は要求量がレベル52前後までずっと伸び続けてしまい、長時間プレイでのレベリングが実質失速しているように感じられる期間が長すぎたため、無上限カーブ上でのレベル33相当の値まで引き下げた)
  - **全体を70%に緩和(`XP_REQUIREMENT_MULT = 0.7`、v1.36.70)**: 上限(1527→1069)を含むカーブ全体に一律70%を掛けた。アップグレードの種類(弾丸効果・ドローン・カテゴリリロールなど)が大幅に増えたことを受け、同じプレイ時間でより多くのレベルアップ(=より多くの選択機会)を得られるようにする調整。カーブの形自体(多項式の指数や上限の考え方)は変えず、係数を一律で掛けているだけなので、各レベル間の相対的な伸び方は従来通り
  - 目安(70%適用後): Lv1→2は13、Lv15→16は332、Lv33以降は上限の1069で頭打ち

### 攻撃ロジック
- 攻撃間隔ごとに、**射程内**にいる敵の中から最も近い `projCount` 体を選び、それぞれに向けて弾を発射。射程外の敵は選択対象にすら入らない(射程内に敵が1体もいなければ、その攻撃間隔は単に不発になる。攻撃間隔タイマー自体は経過し続けるので、敵が射程内に入った瞬間は待たされずに即座に発射される)
- 貫通(`pierce`)が残っている限り、1発の弾が複数の敵を貫通してダメージを与える

### 武器の射程(`weaponRange`、v1.29.0で導入、v1.31.0で基準を短辺→長辺に変更)

`weaponRange(p) = max(W, H) * 0.5 * WEAPON_RANGE_OVERSHOOT * p.rangeMult`(`W`/`H`は現在のビューポートのcanvasサイズ、`WEAPON_RANGE_OVERSHOOT = 1.1`、`p.rangeMult`は初期値1の倍率ステータス)。

- プレイヤーは常に画面中央に固定されているため(§8)、`min(W, H)`基準だと画面のどの方向からもはみ出さない「完全に画面内」な射程になる一方、アスペクト比が正方形から離れるほど(横長・縦長の画面ほど)射程が窮屈になりすぎる問題があった。そこで基準を`max(W, H)`(長辺)に変更し、さらに`WEAPON_RANGE_OVERSHOOT`倍(現在1.1倍、長辺の半分より10%広い)だけ意図的にオーバーさせている
- この結果、短辺方向については画面端からわずかにはみ出た位置の敵まで射程に入る(=ごくわずかに画面外の敵を倒すことがある)が、以前(射程の概念が無かった頃)のように大きく画面外の敵まで倒してしまう状態とは程度が大きく異なる。長辺方向には画面内で完結する
- 敵の出現距離(`spawnDist = max(W, H) * 0.65 + 60`)は`weaponRange`より常に大きい(差は少なくとも`max(W,H) * 0.1 + 60`)ため、敵は必ず一度射程外から射程内へ歩いて入ってくる形は変わらず維持される
- `rangeMult`はキャラクター・武器・将来のアップグレードから調整できる汎用の倍率として用意しているが、現時点でこれを変更する強化・パッシブ・特殊能力は無い(常に1)

---

## 4. レベルアップ・強化選択

レベルアップ時、以下3つのプールを合わせた候補から3択 + 常設の「スキップ」の計4択を提示する。3択のうち**1枠目は「弾丸効果系の優先枠」**として扱われる(詳細は§4-4)。

**アップグレードカードの説明文は具体的な数値を出さない方針(v1.36.42)**: 以前は「攻撃ダメージ +50%」のように、ゲーム内のカード説明文にも上昇率・秒数・範囲などの具体的な数値をそのまま表示していたが、v1.36.42でこれをやめ、「攻撃ダメージが大きく上昇する」のような抽象的な表現に統一した(狂乱のように隠しステータス的な仕様が特に多いものはこの方針の代表例)。本ドキュメント(SPEC.md)自体はこれまで通り正確な数値・数式を記録し続ける - これは設計・実装の技術資料であり、あくまでゲーム内表示側だけの方針転換である。以下の各節で示す数値は実際の内部仕様であり、プレイヤーがゲーム画面上で目にする文言とは一致しない。

### 4-1. 通常強化プール(`UPGRADE_POOL`)

| 強化 | 効果 |
|---|---|
| ダメージ強化 | 攻撃ダメージ +50% |
| 射程アップ | 射程 +10%(上限`STAT_LIMITS.maxRangeMult`=2.0倍)。視界(カメラのズーム)も連動して広がる(§8) |
| 移動速度アップ | 移動速度 +10%(上限`STAT_LIMITS.maxSpeedMult`=1.5倍) |
| 回収範囲アップ | XP回収範囲 +10%(上限`STAT_LIMITS.maxPickupRadius`=200px) |
| 攻撃速度アップ | 攻撃間隔 -20%(下限`STAT_LIMITS.minAtkCooldown`=0.15秒) |
| 最大HPアップ | 最大HP +15%(現在値基準、増加分だけHPも回復) |
| ハート回復量増加 | ハートの回復量 +10%(上限`STAT_LIMITS.maxHeartHealFrac`=50%、v1.36.69で25%から引き上げ。詳細は§6-7) |

**最大HPアップは固定値ではなく割合(v1.26.0)**: 以前は「最大HP +25(固定)」だったため、キャラクターやビルドによって基準の最大HPが変わっても増加量が変わらず、HPが多いビルドほど相対的な伸びが弱くなっていた(例: 当時のタンクの基準HP180(現ヒーラー、v1.36.65で130に変更済み)に対して+25は+14%相当にしかならない)。現在値に対する割合(+15%)に変更したことで、基準HPの大小によらず一定の伸び率になる。上げ幅は当初25%を検討したが、複利的に伸びやすい割合アップグレードとしては大きすぎると判断し15%に調整した。

**射程アップ(v1.36.23で追加、v1.36.24で+20%→+10%に調整)**: `Player.rangeMult`(初期値1、`weaponRange(p)`に掛かる)を伸ばす、攻撃速度アップと同じ「上限に達したら候補から消える」パターンの強化。上限は`STAT_LIMITS.maxRangeMult = 2.0`で、+10%ずつでは約8回で頭打ちになる。`rangeMult`はスタンダード・チャージビームの`weaponRange(p)`にそのまま乗算されるため両者の射程を実際に伸ばすが、パルスウェーブは固定の近接射程(`WIDE_ATTACK_RANGE`、§7-4-1)を、ラピッドファイア(§7-4-3)は自機の移動方向へ発射する仕組み上そもそも`weaponRange(p)`を参照しないため、この2種は射程そのものは変化しない - ただしこの強化は同時にカメラのズーム(視界の広さ)も`rangeMult`と連動して変えるため(§8)、この2種の使用中でも「見える範囲が広がる」という副次的な効用はある。上限を設けた理由は、`rangeMult`をそのまま視界のズーム倍率としても使っているため、ダメージ・最大HPのように無制限に伸ばせてしまうと視界がどこまでも際限なくズームアウトしてしまうため。初回実装時は+20%刻みだったが、他の上限付き強化(攻撃速度アップ+20%など)と揃えつつ、より緩やかな刻みにするため+10%に調整した。

**リジェネ・生命転化はアップグレードから廃止(v1.36.11)**: 本作は連続して被弾する場面が少なく、一度食らってもHPが0にさえならなければ、あとは回避に専念しているだけでリジェネが+1でも回復が追いついてしまう。これは明確なデメリットのない「リジェネ」だけでなく、デメリット付きの「生命転化」(HP自然回復+2/最大HP-15%)にも同様に当てはまる問題で、最大HPが下がるという代償は「回復が間に合いやすすぎる」という根本の強さには効いていない。両方とも選択肢としては廃止したが、`Player.regen`フィールドと`update()`内の回復ティック処理自体は意図的に残してある。今後キャラクターの特殊能力・パッシブ能力(§7-2・§7-3)がリジェネを直接付与する形で使う可能性があるため。

**移動速度アップ・回収範囲アップ(v1.35.0で廃止、v1.36.24で上限付きの形で再追加)**: 当初この2つには他の強化と異なる構造的な問題があった。

- どちらも`offensePowerMult`/`crowdPowerMult`/`survivalPowerMult`(§6-2)のいずれにも算入されておらず、難易度スケーリングと一切連動しない「野放し」のステータスだった
- 移動速度は、ある程度は快適な操作に必要だが、一定を超えると逆に細かい操作(被弾回避など)が難しくなり、伸ばすほど得とは言い切れない性質を持つ。しかし上限が無いため、際限なく積み増すことができてしまっていた
- 回収範囲は伸ばしてもデメリットは無いが、実質1回のアップグレードでほぼ困らなくなる程度の効果しかなく、2回目以降は「損はしないが実質空気」な選択肢になっていた

v1.35.0では、この2つを弾丸効果のように上限付きの選択式に作り直すよりも、そもそも「選んでもらう」形自体をやめ、廃止分の価値を基礎値に織り込む方針を取った。`baseSpeed`を190→**210**、`pickupRadius`を70→**100**に引き上げ(いずれも旧アップグレード1回分に相当する増加量)。

その後、射程アップ(v1.36.23)を上限付きの形で追加した流れを受け、同じ構造的な問題(移動速度は「上限が無いこと」、回収範囲は「2回目以降が実質空気なこと」)を上限で解決できると判断し、v1.36.24で選択式として再追加した。移動速度アップは`Player.speedMult`を+10%ずつ、`STAT_LIMITS.maxSpeedMult = 1.5`を上限に伸ばす(スピードキャラのパッシブが`speedMult *= 1.35`を素の時点で適用しているため、上限はそれより高い値に設定してある - そうでないとスピードキャラでは強化が最初から候補にすら出現しなくなってしまう)。回収範囲アップは`Player.pickupRadius`を+10%ずつ、`STAT_LIMITS.maxPickupRadius = 200px`(基礎値100の2倍)を上限に伸ばす。いずれも攻撃速度アップ・射程アップと同じ「上限到達で候補から消える」`available`ゲートを使っている。

デメリット付きの「俊足の代償」(移動速度+30%/最大HP-15%、TRADEOFF_POOL)も、明確な代償こそあるものの結局は移動速度を伸ばす強化である以上「伸ばしすぎると操作しづらくなる」という同じ問題を抱えていたため、あわせて廃止した(v1.35.0の直後に追加削除)。

### 4-2. デメリット付きアップグレード(`TRADEOFF_POOL`)

強いメリットと引き換えにデメリットも発生する強化。各ステータスには下限/上限(`STAT_LIMITS`)があり、繰り返し取得してもそれ以上は悪化しない(下限/上限に達した後は実質デメリットなしでメリットだけ得られる)。

| 強化 | 効果 | 関連する下限/上限 |
|---|---|---|
| 捨て身の一撃 | ダメージ +80% / 攻撃間隔 +15% | 攻撃間隔の上限 1.4秒 |
| 速射特化 | 攻撃間隔 -31% / ダメージ -20% | 攻撃間隔の下限0.15秒(メリット側)・ダメージの下限3(デメリット側) |
| 鉄壁の構え | 最大HP +25%(現在値基準、増加分だけHPも回復) / ダメージ -15% | ダメージの下限 3 |

**攻撃間隔が下限の時は攻撃速度アップ・速射特化を出現から除外(v1.36.2で修正)**: 捨て身の一撃・生命転化・鉄壁の構えは「デメリット側」に下限/上限があるため、そこに達した後もメリット側は伸び続け、実質メリットのみになる(上記の説明どおり、意図した挙動)。一方、攻撃速度アップ(`UPGRADE_POOL`)と速射特化は逆に「メリット側」(攻撃間隔の短縮)に下限があるため、`atkCooldown`が既に`STAT_LIMITS.minAtkCooldown`(0.15秒)に達していると、攻撃速度アップは完全に無意味な選択肢になり、速射特化に至ってはメリットゼロのままデメリット(ダメージ-20%)だけが適用される「罠」になってしまっていた。両エントリに`available: p => p.atkCooldown > STAT_LIMITS.minAtkCooldown`という出現条件を追加し、`onLevelUp()`の抽選プールが`BULLET_EFFECTS`の`notMaxedEffects`と同様にこの条件でフィルタするよう変更した。ほとんどの`UPGRADE_POOL`/`TRADEOFF_POOL`エントリは上限がなく永久に有用なため、`available`はデフォルトでは指定不要(未指定なら常に出現)というオプトイン方式にしてある。

### 4-3. 弾丸効果アップグレード(`BULLET_EFFECTS`)

「弾丸効果」は初回取得で有効化(Lv.1)、以降の取得でレベルが上がり効果が強化される。各効果には上限レベル(現在すべて**Lv.5**)があり、上限に達した効果はそれ以降レベルアップ画面の候補から出現しなくなる。貫通は既存の`pierce`ステータスをそのままレベルとして扱う(旧仕様では無制限だったが、上限5が設定された)。本書では引き続き数値の刻みを「Lv.」表記で説明するが、v1.36.89以降、実際のゲーム画面(アップグレードカードのタイトル・一時停止画面のステータス表示)では自機レベルと区別するため「ランク」という語で表示される(例: 「狂乱 ランク3→4」)。

| 効果 | 内容 | レベルごとの変化 |
|---|---|---|
| 爆発 | 着弾時、`EXPLOSION_TRIGGER_CHANCE=50%`の確率で発動し(v1.36.43で追加、下記)、着弾地点の周囲に範囲ダメージ(本体ダメージの80%固定。範囲は狭めだが火力が高いコンセプト) | 範囲半径のみ増加: `50 + 20*(Lv-1)` px(Lv.1=50 → Lv.5=130)。ダメージ比率・発動確率はレベルで変化しない |
| 連鎖 | 着弾時、`CHAIN_TRIGGER_CHANCE=50%`の確率で発動し、近くの敵にダメージ(本体ダメージの`CHAIN_DAMAGE_PCT=50%`/回)と、その一撃が本来持っていた状態異常(低速・猛毒・狂乱・爆弾化・衰弱・脆弱、取得していれば爆発の誘爆も)をすべて連鎖させる(連鎖半径150px固定)。発生地点と対象を結ぶ水色の線エフェクトが一瞬表示される。ダメージソースというより状態異常の拡散役としての位置づけ | 連鎖回数(ホップ数) = Lv(Lv.1=1回 → Lv.5=5回)。発動確率・1回あたりのダメージ割合はレベルで変化しない |
| 雷撃(v1.36.95で追加) | 連鎖が単一経路(1体ずつ直線的にホップ)から分岐型(各ホップ地点から`LIGHTNING_BRANCH_FACTOR=2`体まで同時に分岐)に変化する(`連鎖`Lv.5到達後にのみ出現、下記)。他の連鎖の仕様(発動確率・ダメージ割合・状態異常伝播・爆発シナジー)は変更なし | 単一ランクのみ |
| 低速 | 着弾した敵の移動速度を一時的に50%に低下 | 減速時間: `1.0 + 0.5*(Lv-1)` 秒(Lv.1=1.0秒 → Lv.5=3.0秒) |
| 貫通 | 弾が敵を貫通してダメージを与える(既存実装)。パルスウェーブ・チャージビーム・ランドマイン・マルチミサイル・ソード＆シールド(§7-1)使用中は候補から出現しない(v1.36.16/v1.36.18/v1.36.99、下記) | 貫通数 = Lv(上限5、旧仕様は無制限だった) |
| 猛毒 | 着弾した敵を`POISON_DURATION=5秒`間の毒状態にする。毒状態中は毎秒、敵自身の最大HPの`POISON_DMG_PCT=1%`(1スタックあたり、レベルによらず固定、v1.36.90で4%から緩和)を毒ダメージとして受ける | 重ね掛け上限 = Lv(Lv.1=最大1スタック → Lv.5=最大5スタック) |
| 猛毒解放(v1.36.95で追加) | 毒状態が最大スタックに達している敵に、毒を伴う攻撃が当たると(`猛毒`Lv.5到達後にのみ出現、下記)、毒スタックが瞬時に解消され、残り持続時間ぶんの毒ダメージ(`敵の最大HP×POISON_DMG_PCT×スタック数×残り秒数`)がその場で即座に発生する | 単一ランクのみ |
| 狂乱(v1.36.87で仕様変更、v1.36.97で同時上限を追加・v1.36.98で撤回) | 着弾した敵を`frenzyDurationForLevel(Lv)`秒間の狂乱状態にする。狂乱状態の敵は自機だけでなく接触した他の敵にも素の接触ダメージ(倍率なし)を与える(敵同士のフレンドリーファイア)。重ね掛けなし(再着弾は持続時間を最大まで更新するのみ)。移動速度は変化しない(v1.36.59で移動速度増加を廃止)。狂乱状態になれる敵の数に上限はない(v1.36.97で一度`FRENZY_MAX_CONCURRENT=5`という同時上限を導入したが、v1.36.98で撤回し、代わりに暴走側に上限を移した。下記) | 持続時間: `5 + 2*(Lv-1)` 秒(Lv.1=5秒 → Lv.5=13秒) |
| 暴走(v1.36.95で追加) | 狂乱状態の敵の移動AIを完全に上書きし、自機を狙わずランダムに徘徊させる(`狂乱`Lv.5到達後にのみ出現、下記)。射撃系(ガンナー)の敵は通常通りの間隔で攻撃を続けるが、狙いが自機ではなくランダムな方向になる | 単一ランクのみ |
| 爆弾化(v1.36.91でダメージ計算式を変更、v1.36.94で発動確率追加・ダメージに上限) | 着弾時、`BOMBIFY_TRIGGER_CHANCE=25%`の確率で敵を`BOMBIFY_DURATION=3秒`間爆弾化する(`爆発`Lv.5到達後にのみ出現、下記)。生存中は無効果。爆弾化状態のまま倒された敵は、`min(bombifyDamage(p), 爆発した敵の最大HP×BOMBIFY_DMG_CAP_PCT(20%))`(自機の攻撃力・攻撃間隔から算出した1秒あたりのダメージ量相当、ただし爆発した敵自身の最大HPの20%が上限)を、半径`BOMBIFY_RADIUS=180px`以内の他の敵に爆発ダメージとして与える。重ね掛けなし(再着弾は持続時間を最大まで更新するのみ) | 単一ランクのみ(v1.36.91で`maxLevel`を5→1に変更、下記) |
| 衰弱(v1.36.88で出現条件を撤廃) | 着弾した敵を`WEAKEN_DURATION=5秒`間衰弱状態にする。衰弱状態の敵は攻撃力(接触ダメージ)が低下する。重ね掛けなし(再着弾は持続時間を最大まで更新するのみ) | 攻撃力低下率: `5% + 5%*(Lv-1)`(Lv.1=-5% → Lv.5=-25%、v1.36.89で20%→48%に緩和、v1.36.97でさらに5%→25%に緩和) |
| 脆弱(v1.36.87で追加、v1.36.88で仕様変更) | 着弾した敵を`VULNERABLE_DURATION=5秒`間脆弱状態にする。脆弱状態の敵は、あらゆるダメージ源(通常弾・爆発・連鎖・毒・キルゾーン・爆弾化の誘爆・狂乱状態の敵からのフレンドリーファイアなど)に対する被ダメージが増加する(`vulnerableDmgMultForLevel`)。重ね掛けはされず、再度攻撃が当たると持続時間が最大まで更新される | 被ダメージ増加率: `5% + 5%*(Lv-1)`(Lv.1=+5% → Lv.5=+25%、v1.36.89で20%→48%に緩和、v1.36.97でさらに5%→25%に緩和)、衰弱の攻撃力低下率と対になる曲線 |

**貫通はパルスウェーブ・チャージビーム・ランドマイン・マルチミサイル・ソード＆シールド使用中は候補から除外(v1.36.16で追加、v1.36.18でチャージビームも対象に、v1.36.99で新武器3種も対象に)**: パルスウェーブは範囲内の敵を1回の衝撃波で同時に全てヒットさせる方式(§7-4-1)、チャージビームは無条件の無限貫通を最初から持つ方式(§7-4-2)のため、いずれも「1発が当たった敵を貫通して次の敵に届く」という貫通の効果がそもそも意味を持たない。ランドマインはそもそも飛翔しない設置型(§7-4-4)、マルチミサイルは仕様上常に貫通0に固定(§7-4-5)、ソード＆シールドは弾を撃つ概念自体がない常時展開の近接武器(§7-4-6)のため、同じ理由で除外対象に加わった。他の`available`ゲート(攻撃速度アップの下限到達後、爆弾化・衰弱の前提条件)と同じ仕組みで、`available: p => !p.weapon || !['wide', 'charge', 'landmine', 'missile', 'swordshield'].includes(p.weapon.id)`により候補プールそのものから除外している。

**猛毒(v1.36.1で追加)**: プレイヤーの攻撃力ではなく**被弾した敵自身の最大HP**に対する割合ダメージなので、雑魚にも巨大HPのボスにも一律に効く、爆発・連鎖と同様のクラウド/対ボス系の選択肢として機能する。

- **重ね掛け(スタック)**: 毒状態の敵に再度攻撃が当たるとスタックが1増え(上限は`poisonMaxStacksForLevel(Lv) = Lv`)、毎秒の毒ダメージがスタック数倍になる。ただし`POISON_DURATION`のタイマーはスタックが増えても延長されない - 最初に毒状態になった瞬間のタイマーがそのままカウントダウンを続け、0になった瞬間にスタック数ごとリセットされる。攻撃速度が高いビルドほどスタックを稼ぎやすく、連鎖と同様に「攻撃速度と相性が良い」設計
- **ランクアップの効果はスタック上限の引き上げ(v1.36.3で変更)**: 当初は1スタックあたりの毒ダメージ割合自体がレベルで上昇する仕様(3%→7%/秒)だったが、「重ね掛け系の状態異常は、1スタックあたりの効果量は固定のまま、ランクアップで重ね掛け上限が増えることで結果的に火力が伸びる」という設計に統一するため変更した。低レベルのうちは同じ敵を連続して攻撃してもすぐ上限に達して無駄撃ちになりやすく、レベルを上げるほど「攻撃速度を活かしてスタックを積む」戦略の伸びしろが広がる
- **スタック上限を設けた理由**: 上限なしだと、高攻撃速度ビルドで毎秒の毒ダメージが際限なく積み上がってしまう(DPSの青天井化)のに加え、スタック数=表示ドット数(下記)なので、視認性の観点でも上限が必要だった
- **`POISON_DMG_PCT`を4%→1%に緩和(v1.36.90)**: 旧数値だと最大スタック(5)×満期の`POISON_DURATION`(5秒)で累計ダメージが被弾した敵の最大HPの100%に達し、自機の攻撃性能に関わらず「5スタック当てるだけであらゆる敵を倒せる」状態になっていた。1%に緩和したことで同条件の累計は最大HPの25%までとなり、猛毒はあくまで削り・DPS補助として機能し、単体でとどめまで完結する手段ではなくなった

**狂乱(v1.36.4で追加、v1.36.39でスタック効果を再設計、v1.36.59で移動速度増加を廃止、v1.36.87で攻撃力アップ・被ダメージ増加を撤廃しシンプルな同士討ちのみに再設計)**: 狂乱状態の敵は、**自機だけでなく接触した他の敵にも**素の接触ダメージ(`Enemy.dmg`、倍率なし)を与えるようになる。密集した敵集団の中で発生すると敵同士のフレンドリーファイアで自滅を誘発できるが、うまく機能すれば労せず敵を減らせる、外れれば単に敵が自機に迫ってくるだけになる、という賭けの要素を持つ選択肢。

- **重ね掛けなし・ランクアップで持続時間が伸びる(v1.36.87で変更)**: 以前は猛毒と同じ重ね掛け(スタック)方式だったが、後述の再設計でスタックが担っていた役割(攻撃力アップ・被ダメージ増加の強さ調整)自体がなくなったため、爆弾化・衰弱と同じ「重ね掛けなし・再着弾でタイマーが更新される」方式に統一した。ランクアップの効果も「重ね掛け上限」から「持続時間」に変更: `frenzyDurationForLevel(Lv) = FRENZY_DURATION_BASE(5) + FRENZY_DURATION_PER_LEVEL(2) * (Lv-1)`(Lv.1=5秒 → Lv.5=13秒)。Lv.1の値は旧仕様の固定`FRENZY_DURATION`(5秒)と同じなので、新規に取得した瞬間の体感は変わらない
- **移動速度増加は廃止(v1.36.59)**: 導入当初(v1.36.4)から狂乱状態の敵は`FRENZY_SPEED_MULT=1.5倍`の速度になる副作用を持っていたが、微調整の一環として撤廃した。敵の移動速度は狂乱状態の有無に関わらず常に素の`speed`(スロー適用時は`SLOW_MULT`のみ)のままになる
- **敵同士の接触判定**: 狂乱状態の敵は、自機との接触判定(`e.contactCd`)と同じクールダウンを共有する形で、他の敵との接触も毎フレームチェックする。狂乱状態の敵1体につき、そのフレームで最初に見つかった接触相手(自機か他の敵)1体にのみダメージを与え、同じ0.5秒クールダウンが両方の接触経路にまたがって適用される。処理コストを抑えるため、この総当たりチェックは狂乱状態の敵を外側ループにして回している(通常は敵全体のごく一部のはず)
- **カテゴリを「オフェンス」から「クラウド」へ変更(v1.36.87)**: 攻撃力アップという単体強化の側面が消え、「敵同士を戦わせる」という多対応の効果だけが残ったため、クラウドカテゴリ(§4-5-2)に分類し直した。ただし他のクラウド系アップグレード(連鎖・貫通・キルゾーン・磁気嵐・射程アップ)と異なり`crowdPowerMult`(§6-2)への寄与はない(下記)
- **難易度フィードバックなし(v1.36.87で確定)**: 元々`survivalPowerMult`/`crowdPowerMult`/`offensePowerMult`のいずれにも寄与していなかった(v1.36.72時点で既にそうだった)。今回の再設計後もこの扱いは変えていない - 同士討ちが実際にどれだけ発生するかはプレイヤーの立ち回り・ビルド・運に大きく左右され、確実な出力として難易度に織り込める性質のものではないため
- **同時上限を追加(v1.36.97)、その後撤回(v1.36.98)**: 暴走(§4-3-1)と組み合わせると画面全体を同時に狂乱状態にできてしまい強すぎるバランスになっていたため、v1.36.97で`FRENZY_MAX_CONCURRENT=5`という狂乱自体への同時上限を追加した。しかしフィードバックを受け、問題の本質は狂乱そのものではなく暴走(移動AIの無力化)が画面全体に及ぶことだったと整理し直し、v1.36.98で狂乱側の上限は完全に撤回、代わりに暴走側に専用の上限(`RAMPAGE_MAX_CONCURRENT`、下記)を新設して移した。現在、狂乱状態になれる敵の数そのものに上限はない

**v1.36.87での再設計の経緯**: 導入時からの狂乱には、同士討ちそのものに加えて(1)狂乱状態の敵自身の攻撃力を上げる副作用、(2)狂乱状態の敵の被ダメージを増加させる副作用、の2つが常にセットで付いてきていた。この2つの副作用がどれだけ「同士討ちを機能させる」ことに寄与しているかを検証しないまま実装されていたため、実際にシミュレーションで比較したところ、被ダメージ増加の副作用の方が同士討ち自体より体感上の影響が大きく、しかもその副作用は`Game.damageEnemy()`という全ダメージ源の共通経路に乗っていたため、同士討ちに限らずプレイヤー自身の攻撃が狂乱状態の敵を仕留める速さにも(意図せず)寄与してしまっていたことが判明した。この結果を受けて2つの副作用を完全に撤廃し、狂乱は「同士討ちを起こすだけ」のシンプルな効果に戻した。その代わり、被ダメージ増加の効果自体は「脆弱」という独立した弾丸効果(下記)として切り出し、狂乱単体では地味だが、連鎖・磁気嵐で敵を密集させたり脆弱を併せて取ることで積極的に同士討ちを活かすビルドを組める、という選択式のシナジーに置き換えた。

**爆弾化(v1.36.5で追加、v1.36.91でダメージ計算式を変更、v1.36.94で発動確率・ダメージ上限を追加)**: 猛毒とは異なる毛色の変わり種で、生存中は完全に無効果(移動速度・攻撃力・被ダメージ、いずれも変化しない)。価値はすべて「倒された瞬間」の一点に集約されている。

- **重ね掛けなし**: 猛毒と異なりスタック概念自体がなく(`Enemy.bombifyStacks`のようなフィールドは存在しない)、既に爆弾化済みの敵に再度攻撃が当たった場合は単に`bombifyTimer`が`BOMBIFY_DURATION`まで更新されるだけ(スタックによる強化がない代わりに、持続時間はリセットされる - 猛毒とは逆の扱い)。この「重ね掛けなし・再着弾で持続時間更新」方式は衰弱にも共通で、狂乱(v1.36.87)・脆弱(v1.36.88)も後からこちらに合流した - 現状「重ね掛け(スタック)方式」なのは猛毒だけになっている
- **単一ランクに変更(v1.36.91、旧`maxLevel`は5)**: 後述のダメージ計算式変更に伴い、ランクアップで直接強化する数値(旧`bombifyDmgPctForLevel`)自体がなくなったため、ランクという伸びしろの軸そのものを廃止し、1回取得すれば効果が有効になる単発アップグレードにした。取得済み(`bombifyLevel===1===maxLevel`)になった時点で以降のレベルアップ画面の候補から外れる(他の弾丸効果と同じ`notMaxedEffects`のフィルタ条件による)
- **ダメージ計算式を敵の最大HP依存から自機のDPS依存に変更(v1.36.91)**: 旧式(`bombifyDmgPctForLevel(Lv) = 15% + 5%*(Lv-1)`を検出された敵自身の最大HPに乗算)は猛毒と同じ「対象依存」の設計だったが、爆弾化の前提条件(`爆発`Lv.5到達)が実質的にダメージ・攻撃速度型ビルドを要求すること、また敵最大HP依存という個性は猛毒に任せるべきという判断から、`bombifyDamage(p) = p.damage / p.atkCooldown`(自機の攻撃力を攻撃間隔で割った、自機自身の単発DPS相当)に置き換えた。マルチショット数・貫通・他の弾丸効果は計算に含まれない、あくまで素の攻撃力・攻撃間隔のみに基づくシンプルな近似値。爆発誘爆時点の自機の現在ステータスをその場で参照する(`weakenDmgMultForLevel(p.weakenLevel)`などと同じ「都度ライブ参照」方式)ため、ラン中に攻撃力・攻撃速度を強化するほど、既に爆弾化済みで起爆を待っている敵の爆発威力もリアルタイムに強化される
- **連鎖爆発**: 爆発の範囲内に他の爆弾化済みの敵がいて、かつそのダメージで倒れた場合、その敵の爆発も連鎖して誘発される。この連鎖は、1フレーム内で新たな誘爆が起こらなくなるまで繰り返し解決される仕組み(`update()`内の`while`ループ)になっており、配列内の並び順に関係なく確実に連鎖を解決する。密集した爆弾化済みの敵の集団に当たれば、1体の死をきっかけに次々と誘爆が連鎖する多段爆発が起こり得る
- **発動確率を追加(v1.36.94、`BOMBIFY_TRIGGER_CHANCE=25%`)**: v1.36.91のDPS依存ダメージ化と、範囲内全員に減衰なしで着弾する仕様(下記)の組み合わせにより、爆弾化状態にした敵の数がそのまま連鎖の威力に直結し、意図より火力が出やすくなっていた。爆発・連鎖と同じ`TRIGGER_CHANCE`方式(`applyOnHitStatuses`内で`Math.random() < BOMBIFY_TRIGGER_CHANCE`を判定)を導入し、着弾しても必ず爆弾化するわけではないようにした。これにより、密集した敵集団の中で実際に爆弾化状態になる個体数そのものが間引かれ、連鎖のスケールが自然に抑えられる
- **ダメージ上限を追加(v1.36.94、`BOMBIFY_DMG_CAP_PCT=20%`)**: `min(bombifyDamage(p), 爆発した敵自身の最大HP × 20%)`という形で、1回の誘爆が周囲に与えるダメージを、爆発した(倒された)敵自身の最大HPの20%までに制限する。プレイヤーのDPSがどれだけ高くても、1回の誘爆だけで密集した集団を一掃することはできなくなり、広範囲の一掃には複数体が連鎖してこそ届くようにする狙い(「5体以上が連鎖爆発すると広範囲を一掃」という設計目標)。DPSが低く上限に達しない場合は、これまで通り`bombifyDamage(p)`の値がそのまま使われる(`Math.min`のため上限は常に安全側の天井としてのみ働く)
- 爆発ダメージは自機には当たらない(既存の爆発弾丸効果と同じ設計)。周囲の敵にのみ有効

**出現条件(v1.36.7で追加): 爆発Lv.5到達後にのみ出現**: 単体で見た初期性能が強すぎたため、`available: p => p.explosionLevel >= 5`という前提条件を追加した。`BULLET_EFFECTS`側にこの種の出現条件を設けたのは初めてで、`UPGRADE_POOL`/`TRADEOFF_POOL`のatkspeed/trade-atkspeed(§4-1・§4-2、v1.36.2)と同じ`available`という仕組みを流用している(ただしあちらは「上限に達したら出現しなくなる」負のゲート、こちらは「前提条件を満たすまで出現しない」正のゲートという逆方向の使い方)。`onLevelUp()`の`notMaxedEffects`フィルタにこの条件を追加で組み込んでいる。爆発を最大ランクまで取得済み(=爆発・爆風系のビルドに既に踏み込んでいる)というプレイヤーにのみ解禁されることで、ラン序盤からの解禁を防ぎ、既にAoE方向に投資したビルドの上澄みとして機能するようにした。
- **持続時間の短縮(5秒→3秒、v1.36.7)**: 再着弾でタイマーが最大まで更新される仕様(重ね掛けなしの節、上記)はそのまま据え置きつつ、持続時間自体を短くすることで「同じ敵を撃ち続けて倒し切らないと爆発が起こりにくい」設計に変更した。敵をまばらにタグ付けして群れの自然な処理ペースに任せるだけでは発動しにくくなり、狙った的を集中攻撃する立ち回りが要求される。

**衰弱(v1.36.8で追加、v1.36.88で出現条件を撤廃)**: 低速が元々持っていた「接触ダメージを半減させる」効果(旧`SLOWED_DMG_MULT`)を独立した状態異常として切り出したもの。以前は低速を1回でも取得すると自動的に敵の攻撃力低下も付いてきたが、この2つの効果を別々に選び取れるようにする狙いで分離した。

- **出現条件の撤廃(v1.36.88)**: 導入時から「低速をLv.5(最大ランク)まで取得済みでないと出現しない」という`available`ゲート(爆弾化と同じ仕組み)を持っていたが、v1.36.88でこのゲートを完全に撤廃し、他の大半の弾丸効果と同じく最初から常に候補に出現するようにした。脆弱(下記)を衰弱と対になる設計に揃えるにあたり、片方だけがゲート付きで対になる相方はゲートなし、という非対称さを解消する狙い
- **重ね掛けなし・持続時間の扱いは爆弾化と同一設計**: スタック概念がなく、既に衰弱済みの敵に再度攻撃が当たった場合は`weakenTimer`が`WEAKEN_DURATION`まで更新されるだけ。ランクアップはダメージ低下率そのものを直接引き上げる(`weakenDmgMultForLevel(Lv) = 1 - (5% + 5%*(Lv-1))`。導入時は爆弾化の`bombifyDmgPctForLevel`と同じ数値カーブ(`30%→70%`)を流用していたが、`bombifyDmgPctForLevel`は独立した別関数のためv1.36.34の爆弾化ダメージ半減による影響は受けず、その後v1.36.89で単独に`20%→48%`、v1.36.97でさらに`5%→25%`へ緩和した - Lv.1の5%は「一撃で倒せる/倒されるラインが実際に変わる=効果を体感できる最小量」という基準で選んだ値)
- **適用範囲**: 衰弱状態の敵が与える接触ダメージすべてに適用される。自機への接触ダメージだけでなく、その敵が狂乱状態でもある場合の敵同士のフレンドリーファイア(§上記)にも同じ倍率が乗る(v1.36.87で狂乱自身の攻撃力アップは撤廃されたため、現在このフレンドリーファイアのダメージに乗る倍率は衰弱のみ)

**脆弱(v1.36.87で追加、v1.36.88で衰弱と対になる設計に全面変更)**: 狂乱の旧・被ダメージ増加副作用をそのまま独立した弾丸効果として切り出したもの。着弾した敵を脆弱状態にし、**あらゆる攻撃に対する被ダメージ**を増加させる。狂乱そのものとは一切連動しておらず(コード上も狂乱を参照しない)、狂乱を取っていなくても単体で選べる - ただし狂乱の同士討ちや連鎖・磁気嵐など、既に何かしらの形で敵に繰り返しダメージを与える手段と組み合わせて初めて真価を発揮する設計。

- **v1.36.88での仕様変更の経緯**: 導入時(v1.36.87)は猛毒と同じ「重ね掛け(スタック)方式、ランクアップでスタック上限が伸びる」設計だったが、「衰弱(攻撃力低下デバフ)」と「脆弱(被ダメージ増加デバフ)」は本来対になる関係にあるため、両者の仕組みを揃えた方が分かりやすいというフィードバックを受けて再設計した。衰弱の設計(重ね掛けなし・固定持続時間・ランクアップで効果量が直接上昇)にそのまま合わせ、猛毒とは別系統の「単発デバフ」グループに移した
- **重ね掛けなし・持続時間の扱いは衰弱と全く同じ設計**: スタック概念がなく、既に脆弱済みの敵に再度攻撃が当たった場合は`vulnerableTimer`が`VULNERABLE_DURATION(5秒、固定)`まで更新されるだけ
- **被ダメージ倍率(`vulnerableDmgMultForLevel`)はランクアップで直接上昇**: `1 + (5% + 5%*(Lv-1))`(Lv.1=+5% → Lv.5=+25%、v1.36.89で20%→48%に緩和、v1.36.97でさらに5%→25%に緩和)。衰弱の`weakenDmgMultForLevel(Lv) = 1 - (5% + 5%*(Lv-1))`と全く同じカーブを、減少ではなく増加の方向に使っているだけの対になる数式 - 衰弱同様、この曲線はさかのぼれば爆弾化の`bombifyDmgPctForLevel`から流用したもの
- **`Game.damageEnemy(enemy, amount)`への一本化は変更なし**: この被ダメージ倍率をあらゆるダメージ源(通常弾・爆発・連鎖・毒・キルゾーン・爆弾化の誘爆・狂乱状態の敵からのフレンドリーファイア・旧タンクの反撃パッシブ)に漏れなく適用する仕組みは、v1.36.87の導入時から変わらずそのまま引き継いでいる。ただしv1.36.88でスタックが廃止されたことに伴い、`damageEnemy()`は敵側に記録された値ではなく`this.player.vulnerableLevel`をその場で直接参照するようになった(衰弱の`weakenDmgMultForLevel(p.weakenLevel)`と全く同じ読み方) - 付与された瞬間のランクではなく、その一撃が実際に発生した瞬間のプレイヤーの現在ランクが使われるため、脆弱状態の敵に対してランクアップした場合、既に付与済みの脆弱にも即座に新しい倍率が反映される
- **カテゴリはオフェンス、難易度フィードバックなし**: 割合ベースで被ダメージが増加する効果のため、敵HP側の難易度カーブ(§6-2)にフィードバックさせる意味が薄いと判断し、`survivalPowerMult`/`crowdPowerMult`/`offensePowerMult`のいずれにも寄与しない扱いにしている

### 4-3-1. マスタリー系弾丸効果(v1.36.95で追加)

爆弾化(§4-3-1、`爆発`Lv.5到達後にのみ出現)と同じ「前提の弾丸効果をLv.5(最大ランク)まで取得しないと出現しない」`available`ゲートを踏襲した3種。いずれも単一ランク(`maxLevel: 1`)で、取得すればそれ以降のレベルアップ画面の候補から外れる一回限りの解禁。前提となる弾丸効果への投資をさらに深く掘り下げてこそ解禁される、既存ビルドの「仕上げ」的な位置づけの追加アップグレード群。

**雷撃(連鎖`Lv.5`到達後に出現、カテゴリ:クラウド、難易度フィードバック:敵出現頻度)**: 連鎖の伝播方式そのものを、単一経路のホップ(1体ずつ、直前のホップ地点から最も近い未連鎖の敵へ)から分岐型のホップに変更する。

- **実装**: `Game.resolveProjectileHit()`内、`p.lightningLevel > 0`の場合は従来の直線的な`for`ループ(`fromX`/`fromY`を毎ホップ更新する単一経路)の代わりに、フロンティア(直前のホップ地点の集合)ベースの`for`ループを使う。各ホップの「段」ごとに、その時点のフロンティアに含まれる**すべてのノード**それぞれから、`LIGHTNING_BRANCH_FACTOR=2`体までの最も近い未連鎖の敵を同時に対象にする(`.sort()`で距離順に並べ`.slice(0, 2)`)。次の段のフロンティアはこの段で新たに連鎖した敵たち全員になる、というBFS(幅優先探索)的な広がり方をする
- **到達数の伸び方**: 理論上はホップの段が進むごとに到達数が`LIGHTNING_BRANCH_FACTOR`倍ずつ増える(1段目最大2体、2段目最大4体、…)ため、`連鎖`のランク(=ホップ段数、最大5)と実際の敵の密集度次第では、単一経路の連鎖より遥かに多くの敵を1回のプロックで巻き込める。ただし各段の対象数は`CHAIN_RADIUS=150px`以内に実際に存在する未連鎖の敵の数で頭打ちになるため、疎らな状況では単一経路とほぼ同じ結果になる(=敵が密集しているときほど威力を発揮する設計)
- **他の連鎖の仕様は変更なし**: 発動確率(`CHAIN_TRIGGER_CHANCE=50%`)・1回あたりのダメージ割合(`CHAIN_DAMAGE_PCT=50%`)・状態異常の伝播(`applyOnHitStatuses`を各対象に適用)・爆発シナジー(各対象で独立に`EXPLOSION_TRIGGER_CHANCE`を抽選)は、分岐の有無に関わらず全く同じ処理を各対象に対して行う
- **難易度フィードバック**: 3種の中で唯一、既存の`crowdPowerMult`(§6-2、敵の出現頻度に直結)に寄与する(`p.lightningLevel * 0.15`の加算項、pierce/chain/killzone/magnetstorm/rangeと同じ扱い)。他の2つ(暴走・猛毒解放)は難易度フィードバック対象外

**暴走(狂乱`Lv.5`到達後に出現、カテゴリ:ディフェンス、難易度フィードバックなし)**: 狂乱状態の敵のうち、最大`RAMPAGE_MAX_CONCURRENT=5`体まで(v1.36.98で導入。以前は「狂乱状態の敵全員」が対象だった、下記)が、移動AIを完全に上書きされ自機を狙わず徘徊する「暴走」状態になる。

- **`Enemy.rampaging`フラグへの変更(v1.36.98)**: 導入時(v1.36.95)は「狂乱状態(`frenzyTimer > 0`)かつプレイヤーが暴走を取得済み」という条件だけで、狂乱状態の敵全員が無条件に暴走の対象になっていた。これが狂乱の同時上限撤回(上記)とセットで問題視され、「狂乱状態になれる敵の数は無制限のまま、そのうち実際に暴走(移動AI上書き)する敵の数だけを絞る」設計に変更した。`Enemy`に専用の`rampaging`フラグ(真偽値)を追加し、狂乱を(再)付与するたびに`Game.applyOnHitStatuses()`内で「まだ`rampaging`でない対象に限り、現在`rampaging`な敵の数が`RAMPAGE_MAX_CONCURRENT`未満ならこの敵も`rampaging`にする」という判定を行う。埋まっている間に狂乱(化)した敵は、暴走はしないが同士討ち(フレンドリーファイア)には引き続き参加する - 狂乱自体の効果とは独立
- **スロットの解放**: `rampaging`な敵の`frenzyTimer`が0以下になった瞬間(`update()`内の通常のタイマー減算処理)に`rampaging`も自動的にfalseへ戻る。これにより空いたスロットは、以降に狂乱(化)を試みるいずれかの敵(新規対象・既存の狂乱状態への再着弾のどちらでも)が次に獲得できる
- **実装(移動AI上書き)**: `update()`内の敵移動ディスパッチで、`e.rampaging`が最優先の分岐になる(ガンナー用の`updateGunnerMovement`・ブリッツ用の`updateBlitzMovement`・通常敵の直線/フローフィールド移動、いずれよりも先に判定される)。この間、対象敵の種別ごとの移動AI(ガンナーの接近→静止して射撃、ブリッツの接近→静止→突進の3状態など)は完全に停止し、暴走が終わった時点でそれぞれ元の状態から何ごともなかったかのように再開する(暴走中は各状態機械のタイマーが一切進まないため)
- **徘徊の動き方(`Game.updateRampageMovement()`)**: 「ランダムな方向に一定距離進む→約90度(`RAMPAGE_TURN_BASE`)±`RAMPAGE_TURN_VARIANCE`(30度)のランダム方向転換(左右もランダム)→再び一定距離進む」を繰り返す、いわゆる千鳥足(drunken walk)型の移動。連続的にカーブしながら進む方式ではなく、直線区間と方向転換を明確に区切ることで、その場でゆっくり円を描くだけの動きにならないようにしている。1区間の距離は`RAMPAGE_LEG_DISTANCE=120px`固定。最初の区間の方向のみ完全ランダム(0〜360度)、以降の区間は直前の方向を基準に約90度転換する
- **射撃系(ガンナー)敵の扱い**: 移動は上記の徘徊に従うが、`GUNNER_ATK_INTERVAL`ごとの射撃自体は止まらない。ただし狙いは自機の方向ではなく、発動のたびに0〜360度のランダムな角度になる。`rangedCooldown`は通常のガンナーAI(`updateGunnerMovement`)と共有しているため、暴走が終わって通常AIに戻った瞬間も違和感なくクールダウンが引き継がれる
- **補償バフ(v1.36.98、`RAMPAGE_DMG_MULT=2`・`RAMPAGE_SPEED_MULT=1.5`・`RAMPAGE_COLOR='#ff2626'`)**: 同時に暴走できる敵が最大5体までに絞られた分、1体あたりの存在感を補うため、暴走中の敵には個別に強化を加えた。攻撃力2倍(自機への接触ダメージ・狂乱のフレンドリーファイアダメージ・ガンナーの射撃ダメージのいずれにも適用)、移動速度1.5倍(`updateRampageMovement`内の徘徊移動のみに適用)、そして体の色を赤(`RAMPAGE_COLOR`)にティントする(`hitFlash`の白点滅のほうが優先度は高い)。通常の状態異常は敵の頭上のドット表示に留める方針(§4-3の状態異常インジケータ)だが、暴走は同時に存在する数が最大5体と少なく、かつ「見逃せない脅威」であるべきという理由から、例外的に敵本体の色そのものを変える方式を採用している
- **カテゴリと難易度フィードバック**: 狂乱状態の敵の脅威(プレイヤーへの正確な攻撃・的確な追跡)を無力化する効果のため、防御(ディフェンス)寄りの位置づけとしてカテゴリはディフェンス。効果自体は「敵の行動を弱める」性質で、プレイヤー自身の火力・受けるダメージ量・敵の出現頻度のいずれにも直接的な数値変化を及ぼさないため、難易度フィードバックはなし(v1.36.98の補償バフ追加後もこの扱いは変えていない)

**猛毒解放(猛毒`Lv.5`到達後に出現、カテゴリ:オフェンス、難易度フィードバックなし)**: 毒状態が最大スタック(`猛毒`のランクがそのまま上限になるため、この効果自体が出現する頃には常に5スタック)に達している敵に、毒を伴う攻撃が改めて命中すると、毒スタックが瞬時に解消され、残りの毒ダメージが即座にまとめて発生する。

- **背景**: `Game.applyOnHitStatuses()`の毒の分岐は、既に最大スタックに達している敵に毒付きの攻撃が命中しても、`Math.min(poisonMaxStacksForLevel(p.poisonLevel), stacks + 1)`により何も変化しない(スタック上限に張り付いたまま)。この効果を取得していない間、この種の命中は実質的に「無駄撃ち」になっていた
- **実装**: `p.poisonburstLevel > 0 && target.poisonStacks >= poisonMaxStacksForLevel(p.poisonLevel)`の条件が真の場合、通常のスタック加算処理の代わりに`burstDmg = 敵の最大HP × POISON_DMG_PCT × 現在のスタック数 × 残りのpoisonTimer(秒)`を計算し、`poisonTimer`/`poisonStacks`を即座に0にリセットしたうえで、`Game.damageEnemy()`経由(他のダメージ源と同じ、脆弱の被ダメージ倍率なども自動的に反映される)でこの`burstDmg`を即座に与える。この計算式は「本来、毎秒`敵の最大HP × POISON_DMG_PCT × スタック数`ずつ、残り`poisonTimer`秒間かけて与えられるはずだった毒ダメージの合計」そのもの(積分すると同じ値になる)なので、体感としては「持続ダメージを前借りして一気に受け取る」効果になる
- **連鎖との相互作用**: `applyOnHitStatuses`は連鎖のホップ処理からも同じメソッドとして呼ばれるため、連鎖で最大スタックの毒持ち敵に伝播した場合もこの即時解放が同様に発動する
- **カテゴリと難易度フィードバック**: 毒を伴う攻撃をきっかけに追加ダメージを生む純粋な火力効果のため、カテゴリはオフェンス。効果量が「敵の最大HP × 固定割合」という猛毒本来の計算式をそのまま前借りしているだけで、プレイヤー自身のステータスには依存しない・発動条件も狙って作るものというより「最大スタックの敵に改めて攻撃が当たった」という偶発的なタイミング依存のため、難易度フィードバックの対象外とした

**状態異常インジケータ(v1.36.0で導入、v1.36.1でスタック表示に対応、v1.36.4で3種目に対応、v1.36.5で4種目に対応、v1.36.8で5種目に対応、v1.36.87で脆弱を追加・狂乱を単発ドットに変更、v1.36.88で脆弱も単発ドットに変更)**: 状態異常は敵本体の色を変えるのではなく、敵の頭上に小さなドットを表示して示す(`STATUS_DOT_COLORS`)。低速は水色(`#7ec8ff`)、爆弾化は赤色(`#ff3b3b`)、衰弱は灰色(`#aaaaaa`)、狂乱はオレンジ色(`#ff8c1a`)、脆弱はマゼンタ色(`#c026d3`)のドットがそれぞれ1つ(いずれもスタックしないため - 狂乱はv1.36.87、脆弱はv1.36.88でそれぞれ重ね掛けなしに変わったのに伴いこちらに移動)、猛毒は緑色(`#39d353`)のドットがスタック数と同じ数だけ横に並んで表示される。複数種類の状態異常が同時に成立している敵は、それぞれのドットが横一列に並ぶ。敵本体の色は常に`ENEMY_TYPES`本来の色のままなので(ヒット時の白フラッシュのみ例外)、敵種の見分けやすさを状態異常の表示と両立できる。以前は低速中の敵本体を水色にティントする方式だったが、将来複数種類の状態異常を追加した際に単一の色しか表現できない問題があったため変更した。爆発が着弾すると、巻き込まれた敵の位置に大きめの鮮やかなオレンジ色(`#ff4500`)のパーティクルが散る(こちらは状態異常ではなく着弾演出なので従来通り)。

**連撃は廃止(v1.25.0)**: 連撃は「マルチショットと相性が悪く、単発攻撃・攻撃速度と相性が良い」という位置づけの弾丸効果だったが、マルチショットが弾丸効果プールから外れ武器固有効果(§7-4)として全プレイヤーに自動付与されるようになったため(v1.24.0)、連撃が前提としていた「マルチショットを取らない選択もできる」という構図自体が成立しなくなった。プレイヤーレベルが上がれば否応なく同時発射数が増えていくため、これに逆行する形の連撃を維持する意味が薄れたと判断し廃止した。

**連鎖と爆発の組み合わせ**: 連鎖の各ホップ先でも(爆発を取得していれば)独立して爆発が誘発される。その際の爆発ダメージは連鎖の減衰後ダメージ(50%)ではなく、プレイヤーの本来の攻撃力(`proj.damage`、発射時点の本体ダメージ)を基準に計算される。これにより「連鎖で状態異常と爆発を広範囲にばら撒く」という連携が、二重の減衰を受けずに機能する。連鎖の各ホップ先でも、主対象の着弾時と同様に独立して`EXPLOSION_TRIGGER_CHANCE`の抽選が行われる(下記)。

**爆発の発動を確率化(v1.36.43)**: 導入以来、爆発は`explosionLevel > 0`でありさえすれば毎回のヒットで確定発動する仕様だったため、単純に強すぎるという判断でナーフした。連鎖(`CHAIN_TRIGGER_CHANCE=50%`)と同じ形の発動確率`EXPLOSION_TRIGGER_CHANCE=50%`を導入し、着弾時・連鎖の各ホップ時のどちらでも独立して抽選される。連鎖のナーフ時(v1.22.0)とは異なり、発動確率を下げる代わりに1回あたりのダメージ割合を引き上げるような補填は行っていない - 単純な発動頻度の削減。

**連鎖はすべての状態異常を伝播する(v1.36.9で修正、v1.36.87で脆弱を追加)**: 低速・猛毒・狂乱・爆弾化・衰弱・脆弱を1発の弾に乗せていた場合、それらすべてが連鎖のホップ先にも同じように適用される。以前は低速(と、別枠処理の爆発)しか連鎖先に伝わっておらず、猛毒・狂乱・爆弾化・衰弱の4つ(いずれもv1.36.1以降に追加)は連鎖のホップ処理に組み込まれないまま取り残されていた。`Game.applyOnHitStatuses(proj, target)`という共通メソッドに一本化し、着弾時の主対象・連鎖の各ホップ先の両方から同じメソッドを呼ぶことで、今後新しい状態異常を追加した際にも連鎖側への反映漏れが起きない構造にした(v1.36.87で追加した脆弱もこの構造にそのまま乗せるだけで済んでいる)。

**連鎖の発動確率(v1.22.0で追加)**: 連鎖はかつて命中時に必ず発動する確定効果だったが、発動確率50%の「プロック」型に変更し、その分1回あたりのダメージ割合を30%→50%に引き上げた。全体としては弱体化(平均的な連鎖回数・連鎖ダメージ量は低下)だが、「弾を撃つ回数が多いほど連鎖が発動する機会も増える」という性質になったことで、攻撃速度アップグレードとの相性が(単発火力アップグレードよりも)良くなるビルド調整になっている。

**低速は移動速度のみに影響(v1.36.8で攻撃力低下効果を分離)**: 以前は低速状態(`slowTimer > 0`、発生源は連鎖・低速アップグレード・迎撃(v1.36.63以降はドローン、v1.36.82以降はジャミングドローン、下記)のいずれでも共通)の敵は接触ダメージも`SLOWED_DMG_MULT=0.5`倍(半減)になっていたが、この攻撃力低下効果は「衰弱」という独立した状態異常に切り出した(下記)。低速自体は移動速度の低下のみを担う、単純な効果に戻っている。低速と衰弱を同じ敵に両方取得している場合でも、両者は別々のタイマー(`slowTimer`/`weakenTimer`)で管理される独立した状態異常であり、低速アップグレードだけを取っても攻撃力低下は起きない。

**マルチショットは弾丸効果プールから除外(v1.24.0)**: マルチショットはほぼすべてのランでほぼ必ず取得される「実質必須」級のアップグレードになっていたため、弾丸効果プールから外し、代わりに標準武器の「武器固有効果」(§7-4)として自機レベルアップに応じて自動でランクアップする仕組みに変更した。選択の余地がない実質必須アップグレードは、もはや「選択肢」ではなく武器そのものの一部として扱うのが自然という判断。これに伴い、後述(§6-2)のクラウド系スケーリングからもマルチショットの寄与を除外している。

**ジャミングドローンはドローンカテゴリへ移行(v1.36.63)**: 以前は弾丸効果の1種(迎撃)だったが、新設の「ドローン」という独立したアップグレードカテゴリ(§4-3-3)に移した。挙動自体(常時発動の低速オーラ)は変更していない。

### 4-3-2. 着弾効果アップグレード(v1.36.35で追加、v1.36.96で廃止)

「着弾効果」は、上記の弾丸効果(状態異常を敵本体に直接付与するもの)とは別の新しいカテゴリとして、**着弾地点(その一撃が敵に当たった瞬間の座標)を中心に、一定時間その場に留まり続ける円形のエリア効果**を作り出す仕組みだった。`BULLET_EFFECTS`と同じ仕組み(独自の`Player`側ランクフィールド、Lv.1で初回取得・以降のピックでランクアップ、上限Lv.5に達すると候補から消える)に相乗りする形で実装されており、当初は磁気嵐・キルゾーン・ポイズンクラウド・(廃止済みの)狂乱の泉の4種があった。

**v1.36.96で3種ともプレイヤーが選べるアップグレードとしては廃止**: 磁気嵐・キルゾーン・ポイズンクラウドのいずれも、実際のゲーム体験にあまり寄与していないという判断から、`BULLET_EFFECTS`の配列から削除し、レベルアップ画面の候補には二度と出現しなくなった。ただし3種の扱いは同一ではない:

- **キルゾーン・磁気嵐はコードを維持**: `Player.killzoneLevel`/`Player.magnetstormLevel`のフィールド自体、`Game.resolveProjectileHit()`内の`spawnImpactEffect`呼び出し、`update()`内のティック処理・引き寄せ処理、`draw()`側の描画は一切削除せずそのまま残している。現状はどちらのランクも常に0(それを引き上げる手段が存在しない)ため実質的に休眠状態だが、「キルゾーンを新しい武器の武器固有効果として」「磁気嵐を新しいキャラクターのダブルタップ特殊能力として」再利用する可能性を見込んでのこと。将来そうした機能が`p.killzoneLevel`/`p.magnetstormLevel`に直接値を設定するようになれば、以下に記す挙動がそのまま(コード変更なしに)有効になる。**キルゾーン側の見込みはv1.36.99で実現済み**: 新武器「ランドマイン」(§7-4-4)が、`p.killzoneLevel`を経由せず`mine.rank`(武器固有効果「爆発範囲アップ」でランクアップする専用フィールド)を直接`killZoneRadiusForLevel`/`killZoneDmgPctForLevel`に渡す形で、キルゾーンと全く同じ`ImpactEffect('killzone', ...)`生成ロジックを流用している(`Player.killzoneLevel`自体は経由しないため、依然として常に0のまま)。ただしこの利用方法は下記(255行目付近)の「種類ごとに1インスタンストラップ方式」とは別の専用ルートを新設して実現しており、詳細は§7-4-4を参照
- **ポイズンクラウドはコードごと削除**: 将来の再利用が見込まれていなかったため、`POISONCLOUD_DURATION`/`poisonCloudRadiusForLevel`/`Player.poisoncloudLevel`/対応する`spawnImpactEffect`呼び出し/ティックループの分岐/一時停止画面の表示行など、関連コードを完全に削除した。v1.36.92で先に廃止した狂乱の泉と同じ扱い

以下は(現在は休眠中の)キルゾーン・磁気嵐の挙動の記録:

| 効果 | 内容 | ランクごとの変化 |
|---|---|---|
| 磁気嵐 | 着弾地点に`MAGNETSTORM_DURATION=3秒`間残る渦を発生させ、範囲内の敵を中心に向けて`MAGNETSTORM_PULL_SPEED=220px/秒`で引き寄せ、留め置く | 効果範囲(半径)のみ増加: `75 + 18.75*(Lv-1)` px(Lv.1=75 → Lv.5=150) |
| キルゾーン | 着弾地点に`KILLZONE_DURATION=5秒`間残る領域を発生させ、範囲内に留まる敵に`IMPACT_EFFECT_TICK_INTERVAL=0.3秒`ごとにダメージ(その一撃の`proj.damage`を基準にした割合)を与え続ける | 効果範囲・ダメージの両方が増加(v1.36.38で範囲も追加): 範囲`67.5 + 15*(Lv-1)` px(Lv.1=67.5 → Lv.5=127.5)、ダメージ`killZoneDmgPctForLevel(Lv) = 50% + 25%*(Lv-1)`(Lv.1=本体ダメージの50%/tick → Lv.5=150%/tick) |

**着弾効果は連鎖の対象にならない**: 連鎖のホップ先には`Game.applyOnHitStatuses(proj, target)`(§4-3の連鎖の節)が適用されるのに対し、着弾効果の生成は`Game.resolveProjectileHit(proj, e)`側にだけ実装されており、`applyOnHitStatuses`には一切含まれていない。両者は完全に別のメソッドなので、連鎖のホップ先で着弾効果が生成されることは構造上あり得ない。これは連鎖の実装漏れではなく意図的な設計で、密集した敵の集団に連鎖が広がるたびに複数のゾーンが連鎖的に量産される事態を避けている。

**エフェクトの見た目**: 効果範囲(半径)と完全に一致する円を、種類ごとの色(磁気嵐=`#6a5cff`、キルゾーン=`#ff2d55`)で薄い塗りつぶし+縁取りとして描画する。消滅直前の`IMPACT_EFFECT_FADE_OUT=0.5秒`だけフェードアウトする専用の透明度計算になっている。

**同時存在数の上限は種類ごとに1つまで、設置型トラップ方式(v1.36.36で導入、v1.36.37で方式を変更)**: 同じ種類の着弾効果は常に最大1インスタンスまでしか存在できない。既に同種のゾーンが存在する状態で同種の着弾効果がもう一度発生した場合、**既存のゾーンが優先され、新規の同種ゾーンは単純に発生しない**(`Game.spawnImpactEffect`は同種の既存ゾーンがあれば何もせずreturnする)。範囲攻撃の1バリエーションというより「一度設置したら効果が切れるまでその場に留まり続ける罠」という位置づけ。既存のゾーンが寿命を迎えて消滅した後は、次に同種の着弾効果が発生した時点で新しいゾーンが問題なく生成される。**ただしランドマイン(§7-4-4、v1.36.99)はこの上限を経由しない**: 複数の地雷が同時に起爆した際にそれぞれ独立したゾーンとして存在できる必要があるため、`spawnImpactEffect`を呼ばず`Game.impactEffects`へ直接`push`する専用ルートを使っている。この1インスタンス上限はあくまで`spawnImpactEffect`という関数自体が持つ制約であり、`Game.impactEffects`配列や各`ImpactEffect`インスタンスの側には複数同時存在を妨げる仕組みは元々ない。

**磁気嵐の引き寄せの実装**: 通常の敵移動(自機へ向かう移動、`update()`内の既存ロジック)が先に処理された後、磁気嵐の範囲内にいる敵に対して追加でゾーン中心方向への移動を加算する形で実装されている(2つの移動が同一フレーム内で単純に加算される)。`MAGNETSTORM_PULL_SPEED`が一般的な敵の移動速度を上回るため、実際には自機へ向かう移動を打ち消してゾーン中心付近に留まる挙動になる。移動量はゾーン中心までの距離でクランプされており、行き過ぎて中心を通り越すことはない。磁気嵐はキルゾーンと違い、下記の周期ティック方式を使わない唯一の着弾効果(継続的な位置補正という性質上、離散的なティックに区切る意味がない)。壁との相互作用(v1.36.53で修正): 引き寄せ移動の直後に`resolveWallCollision(e)`を呼ぶことで、壁がある場合は壁の面できちんと止まって留め置かれる。

**キルゾーンの周期ティック方式(v1.36.38で導入)**: `IMPACT_EFFECT_TICK_INTERVAL=0.3秒`ごとに効果を繰り返し発生させる。`ImpactEffect.tickTimers`(`Map<Enemy, 次のティックまでの残り秒数>`)を持ち、範囲内にいる敵それぞれが自分自身の独立したカウントダウンを持つ。敵がエリアから離れると、その敵の`tickTimers`エントリごと削除される(再度エリアに入り直すとカウントダウンが最初からやり直しになる)。1ティックごとに`fx.dmgPerSec * IMPACT_EFFECT_TICK_INTERVAL`のダメージを与える(生成時に計算された`proj.damage`基準の値を分割して周期的に与える形になり、合計DPS自体は変わらない)。**複数のキルゾーンが同じ敵に重なった場合は加算されない(v1.36.100で追加)**: ランドマイン(§7-4-4)が複数の独立したキルゾーンを同時に展開できるようになったことを受け、`Game.update()`は毎フレーム、各敵について現在覆っている全キルゾーンのうち`dmgPerSec`が最大の1つだけを`dominantKillzone`(`Map<Enemy, ImpactEffect>`)として選び、実際にティックする(ダメージを与える)のはそのゾーンだけに限定している。担当から外れたゾーンの`tickTimers`エントリは削除せず凍結するだけなので、後で再び最強に戻ればカウントダウンの続きから再開する(ゼロからやり直しにはならない)。

**キルゾーンのダメージ基準**: 生成された瞬間の`proj.damage`(その一撃が実際に与えた本体ダメージ - 通常武器ならプレイヤーの`damage`、パルスウェーブ・チャージビームならそれぞれの倍率が乗った後の値)を基準に、1ティックあたりのダメージ量をゾーン生成時に一度だけ計算してゾーン側に保存する(その後プレイヤーのダメージが変化しても再計算されない)。これは爆発・連鎖が`proj.damage`を基準にダメージを計算する既存の設計と同じ考え方で、「効果の威力は発生させた一撃の強さに応じて決まる」という一貫性を保っている。

### 4-3-3. ドローンアップグレード(`DRONES`、v1.36.63で「補助武器」として追加・迎撃を弾丸効果から移行、v1.36.79で3種目「打撃」を追加、v1.36.82で「補助武器」から「ドローン」の概念へ再編)

弾丸効果・着弾効果とはまた別の第3のアップグレードカテゴリ。名称: ジャミングドローン(旧・迎撃)、インパクトドローン(旧・衝撃)、アタックドローン(旧・打撃)。

**v1.36.82での再編**: 旧「補助武器」は3種のうち常に1種類までしか同時保有できず、新しい補助武器を取得すると保有中のものと入れ替わる(ランクは引き継ぐ)、という仕組みだった。これを「1種類ごとに独立して保有できるドローン」という概念に置き換えた - **アップグレードの取得は「そのドローンを1台獲得すること」を意味し、複数種類のドローンアップグレードを取得すれば、それぞれが混在した状態ですべて同時に稼働する**。「取得数=そのドローンの所有台数」であり、旧来の「共有ランク・入れ替え・引き継ぎ」という考え方は完全に廃止された。各ドローンの個々の行動・エフェクト自体(低速化/ノックバック/直接ダメージ)は旧「補助武器」時代のものと一切変更していない。

- **状態管理**: 弾丸効果と全く同じ構造(`p.jammingDroneCount`/`p.impactDroneCount`/`p.attackDroneCount`という3つの独立した所有台数フィールドと、それぞれに対応する独立したクールダウンフィールド`jammingDroneCooldown`/`impactDroneCooldown`/`attackDroneCooldown`)で表現する。旧来の「どちらか1つしか保有できない」ための単一`auxWeaponId`/共有`auxWeaponLevel`という制約付きフィールド構造は廃止し、`p.explosionLevel`/`p.chainLevel`などの弾丸効果フィールドと同じ素直な「種類ごとの専用フィールド」方式に統一した
- **アップグレードカード(`droneUpgrade()`)**: `bulletEffectUpgrade()`と同じ構造 - 所有台数0の状態で選ぶと「(New)」の新規取得カード(1台目を獲得)、1台以上所有中に選ぶと「N→N+1台」の増設カードになる。旧来の「保有中と異なる種類を選ぶと入れ替わる」という分岐は完全になくなり、3種類とも常に「自分自身の所有台数を1増やすだけ」の単純な処理になった
- **レベルアップ画面での候補生成**: 各ドローン種は上限台数(Lv.5)未満、かつ**全種合計の所有数が保有スロット上限未満**(下記「保有数上限」参照、v1.36.84で追加)の間、候補に入り続ける - 弾丸効果の`notMaxedEffects`と似た判定だが、ドローンは個別の上限に加えてこの共有プールの上限も同時に満たす必要がある点が異なる
- **抽選プールでの扱い**: `onLevelUp()`のプールに`UPGRADE_POOL`/`TRADEOFF_POOL`と同列で加わる。弾丸効果専用の1枠目優先枠(§4-4)やピティの弾丸効果ディスカウント(`pickWeightedIndex`が`bullet-`接頭辞のidだけ重みを下げる仕組み、§4-4-2)の対象には含まれない - ドローンのid接頭辞は`drone-`のため、通常のアップグレードと同じ重み(ピティ加算のみ乗る)で抽選される
- **共通の挙動(自機周囲のオーラ)**: 3種類のドローンいずれも、自機を中心とした半径`droneRangeForRank(Player.droneRangeRank)`px以内にいる敵のうち、近い順に最大`droneTargetCount(所有台数) = 所有台数`体(1台=1体 → 5台=5体)に対して効果を適用する、弾の命中に一切依存しないオーラとして動作する。所有台数がそのまま同時対象数になる、という数値の意味づけは旧来の「ランク」から変わっていない(数え方の名目が「ランク」から「台数」に変わっただけ)
- **索敵範囲の統一(v1.36.84)**: 3種類とも常に同一の`droneRangeForRank(Player.droneRangeRank)`を参照するため、検知半径そのものは元々完全に共通だった。しかしインパクトドローンだけノックバックで対象を最大`IMPACT_DRONE_PUSH_DISTANCE`ぶん自機から遠ざける効果があり、これにより発動後の対象が他の2種より明らかに遠い位置まで押し出され、体感的な「射程」がインパクトドローンだけ広く感じられる状態になっていた。この体感差を解消するため、`IMPACT_DRONE_PUSH_DISTANCE`を`50px→20px`に縮小し、3種の実際の影響範囲を近づけた(ノックバック自体は残しているため、押し出す効果そのものはなくなっていない)
- **索敵範囲(`droneRangeForRank(rank) = DRONE_RADIUS_BASE(90px) + DRONE_RANGE_PER_RANK × rank`、v1.36.67で拡大・v1.36.84でランク制に変更)**: `DRONE_RADIUS_BASE`は元々`DRONE_RANGE_BASE`(旧`INTERCEPT_RADIUS`、60px)をそのまま流用していたが、`DRONE_RANGE_BASE`はパルスウェーブの射程(`WIDE_ATTACK_RANGE`、§7-4-1)の計算にも使われているため、素の`DRONE_RANGE_BASE`自体を拡大するとパルスウェーブの射程まで意図せず伸びてしまう。そのためドローン専用の`DRONE_RADIUS_BASE`という独立した定数に切り出した上で1.5倍に拡大した。v1.36.84で新設した「ドローン射程増加」アップグレード(下記)がこの基礎値をランクに応じて底上げする形になっており、`WIDE_ATTACK_RANGE`は引き続き静的な`DRONE_RADIUS_BASE`のみを参照する(このアップグレードの効果がパルスウェーブへ意図せず波及しないようにするため)
- **攻撃間隔(`DRONE_ATK_INTERVAL`、v1.36.64で追加・v1.36.67で短縮・v1.36.84で4倍に延長)**: 当初は毎フレーム(60回/秒)効果を適用しており強すぎたため、通常の武器と同じ「クールダウン」方式に変更。各ドローン種は自分の所有台数が1以上の間だけ自分のクールダウンが進行し、0以下になった時だけ効果を発動、発動のたびに`droneAtkIntervalForRank(Player.droneAtkSpeedRank)`(下記)を加算する(3種とも独立したクールダウンのため、複数種を同時所有していても互いに干渉しない)。各クールダウンの初期値は0のため、そのドローンを新規取得した直後の1回は即座に発動する。0.5秒(v1.36.67)のまま3種類同時稼働できる仕様(v1.36.82)と組み合わせると全体の発動頻度が高すぎるとの判断で、v1.36.84で基礎値を4倍の2.0秒に調整した(「ドローン攻撃速度増加」アップグレード、下記、で部分的に取り戻せる)
- **クールダウン消費は実際に対象がいた時だけ(v1.36.66で修正)**: 攻撃間隔が経過してもオーラの範囲内に敵が1体もいなければ、そのドローンのクールダウンは消費されない(発動処理自体をスキップし、次のフレーム以降も0以下のまま「発動可能」を維持する)。通常武器の`fireWeapon()`が射程内に対象がいない間`p.atkTimer`を消費しない(=クールタイムを無駄にしない)のと同じ考え方。この修正前は、クールダウンが0になった瞬間にオーラの範囲内へ敵がたまたま1体もいなかった場合(例: メイン武器がその一撃で対象を倒した直後など)でも攻撃間隔をまるまる消費してしまい、次に本当に発動できるまでさらに1周期待たされることで、体感の発動間隔が意図した間隔よりも長く・不規則になっていた
- **各ドローンの効果(いずれも発動間隔に1回だけ発動する「一撃」として扱う)**:
  - **ジャミングドローン(v1.36.91で多機能化)**: 対象に、自機が取得済みのデバフ系状態異常(低速・脆弱・衰弱のうち保有しているもの全て)を発動のたびに付与する。低速なら`slowDurationForLevel(Player.slowLevel)`秒、脆弱なら`VULNERABLE_DURATION`固定秒、衰弱なら`WEAKEN_DURATION`固定秒(いずれも通常の弾丸効果による付与と全く同じ持続時間・効果量)。3つとも保有していれば1回の発動で3つ全てを同時付与する。出現条件(`available`)として低速・脆弱・衰弱のいずれか1つ以上をランク1以上保有していることを要求するため、この効果が「何も付与しない空発動」になることはない。発動するたびに水色(`JAMMING_DRONE_ZAP_COLOR = '#7ec8ff'`)の線エフェクト(自機→対象)が表示される
    - **旧仕様からの変更理由**: 旧仕様(低速専用、未取得時は`JAMMING_BASE_DURATION = 0.4秒`の弱い基礎低速のみ発動)は防御性能として実質ほとんど機能しておらず、取得する動機が薄い問題があった。低速・脆弱・衰弱という「自機が既に選んだデバフ系弾丸効果」をまとめて敵に押し付けるオーラに変えることで、単体のドローンとしての価値と、デバフ系ビルドとの噛み合わせの両方を持たせた
  - **インパクトドローン(v1.36.63で新規追加)**: 発動時、対象を自機から遠ざかる方向へ`IMPACT_DRONE_PUSH_DISTANCE`px(v1.36.63時点は50px、v1.36.84で20pxに縮小、上記)分だけ瞬間的にノックバックさせる(v1.36.64で継続的な押し出し力から、1回の発動につき固定距離を押し出す方式に変更。所有台数に関わらず固定値 - 台数が伸ばすのはジャミングドローンと同じく同時対象数のみ)。ノックバックの直後に`resolveWallCollision`を呼ぶため、壁に向かって押し出された敵は壁の面で止まり、めり込んだり貫通したりしない(磁気嵐の引き寄せに施した同種の修正、v1.36.53と同じ考え方)。発動するたびにオレンジ(`IMPACT_DRONE_ZAP_COLOR = '#ffa53d'`)の`ChainZap`線エフェクトが表示される
  - **アタックドローン(v1.36.79で新規追加)**: ジャミング・インパクトが状態異常/ノックバックなのに対し、こちらは唯一の直接ダメージ型。発動時、対象に`round(Player.damage × REFLECT_DMG_PCT_OF_ATTACK(0.3) + Player.maxHp × REFLECT_DMG_PCT_OF_MAXHP(0.08))`のダメージを`Game.damageEnemy()`経由(通常のダメージ源と同じ、脆弱状態の敵の被ダメージ増加なども自動的に反映される)で与える。この計算式は旧タンクの反撃パッシブ(§7-3、`REFLECT_PASSIVE`として実装を残置済み)のものをそのまま流用している - 被弾時にのみ反応する「反撃」ではなく、ドローンの周期的な発動という形で"能動的に"同じ計算式の一撃を繰り出す点が異なる。発動するたびに赤色(`ATTACK_DRONE_ZAP_COLOR = '#ff5a5a'`、アップグレードカードのオフェンスカテゴリ色と同じ)の`ChainZap`線エフェクトが表示される。弾丸効果(連鎖・低速など)は一切適用されない、純粋な数値ダメージのみの一撃 - ただし「爆発」だけは例外で、下記の専用シナジーが発動する
  - **アタックドローン×爆発シナジー(v1.36.86で追加)**: `Player.explosionLevel > 0`の場合のみ、アタックドローンの各命中が通常の弾丸効果としての爆発と全く同じ判定(`Math.random() < EXPLOSION_TRIGGER_CHANCE(0.5)`)・同じ範囲(`explosionRadiusForLevel(explosionLevel)`)・同じ割合(命中対象へのダメージ×`EXPLOSION_DAMAGE_PCT(0.8)`)・同じ見た目(オレンジのパーティクル)で炸裂を追加で試行する。実装は`Game.resolveProjectileHit()`内の爆発ブロックを流用せず(アタックドローンの命中はそもそも`resolveProjectileHit()`を経由しない)、アタックドローンの発動ブロック内に同じロジックを直接組み込む形にした - 爆発以外の弾丸効果(連鎖・低速など)へは意図的に広げていない。狙いは「アタックドローンを単純な火力補助として使うだけなら現状のまま十分だが、より積極的な攻撃手段として使いたいなら爆発を取得することでアタックドローン自体の火力も底上げできる」という、既存の「爆発」への投資に新しい価値を持たせる形のビルド分岐 - 高難易度でアタックドローン単体の(装甲値に対して相対的に低下しがちな)固定ダメージが火力不足になりやすいという課題への対応でもある
- **`survivalPowerMult`への寄与なし(v1.36.72で除外)**: 弾丸効果時代からv1.36.63の補助武器移行までは共有ランク値が`survivalPowerMult`に加算されていたが、効果が至近距離限定でやや地味なため、v1.36.72で難易度スケーリングへの寄与自体を取りやめた(§6-2)。アタックドローン(v1.36.79)も同様にどの`〜PowerMult`にも加算していない。v1.36.82で各ドローンが独立フィールドになった後も、この「スケーリング対象外」という扱いはジャミング・インパクト・アタックの3種いずれも変えていない - 台数が伸ばすのは同時対象数のみという性質は変わらないため
- **UI: 自機上のドローンスロットドット(v1.36.82で「所有ドローン1台につき1ドット」として追加、v1.36.84で「保有スロット全体」を表示する形に変更)**: 自機の頭上に、**現在の保有スロット上限(`droneSlotCap(Player.droneSlotRank)`)ぶんのドットを常に表示する**(所有数が0でも、ラン開始直後から初期スロット数ぶんのドットが並ぶ)。埋まっているスロット(所有中のドローン)はそのドローンの色(敵の状態異常ドット、§4-3の`STATUS_DOT_COLORS`と同じ「1個体につき1ドット」の描画方式・色はそのドローンの`ChainZap`色と共通)、まだ埋まっていない空きスロットは中立の灰色(`DRONE_SLOT_EMPTY_COLOR = '#5a5a66'`)で表示する。例えば保有スロット上限3のうち「ジャミングドローン2台」だけ所有していれば、水色2つ+灰色1つの計3ドットが自機の上に並ぶ。一時停止画面を開かなくても、現在どのドローンを何台所有していて、あと何台分の空きがあるかが常に一目で分かる

**保有数上限の全種合計管理(v1.36.84)**: 以前は各ドローン種が個別の上限台数(Lv.5)しか持たず、種類ごとの上限に達するまで(理論上は3種合計で最大15台まで)いくらでも取得できた。この上限を「全種合計」で管理する共有プール方式に変更した。

- **状態管理**: `Player.droneSlotRank`(0〜`DRONE_SLOT_UPGRADE_MAX_RANK`、下記アップグレードのランク)と、既存の`jammingDroneCount`/`impactDroneCount`/`attackDroneCount`の合計(`totalDroneCount(p)`)の2つから、`droneSlotCap(rank) = DRONE_SLOTS_BASE(3) + rank`で現在の保有上限を導出する。専用の「現在の保有数」フィールドを別途持たず、既存の3つのカウントフィールドの合計をその都度計算する形にしている(二重管理によるズレを避けるため)
- **初期値**: `DRONE_SLOTS_BASE = 3`。「ドローン保有上限増加」アップグレード(下記)を取得していない状態でも、常に合計3台まで(内訳は自由、例えばジャミング3台でも、ジャミング1台+インパクト1台+アタック1台でも)保有できる
- **候補生成への反映**: `totalDroneCount(player) < droneSlotCap(player.droneSlotRank)`を満たさない限り、3種類のドローンアップグレードカードは1枚も候補に出現しない(上記「レベルアップ画面での候補生成」参照) - 個別種の上限(Lv.5)にまだ余裕があっても、共有プールが埋まっていれば出現しない

### 4-3-4. 新規ドローン関連アップグレード3種(v1.36.84で追加、いずれもユーティリティカテゴリ・難易度フィードバックなし)

`UPGRADE_POOL`に追加された、ドローン3種全てに共通で作用する強化。個別のドローン種を対象にした`DRONES`側のアップグレード(§4-3-3)とは別枠で、`available`ゲート付きの固定ランク上限を持つ点で射程アップ・移動速度アップなどの既存のUPGRADE_POOL系アップグレードと同じ形をしている。3種いずれも`survivalPowerMult`/`crowdPowerMult`/`offensePowerMult`のいずれも参照しないため、既存の迎撃・衝撃・打撃と同じく難易度フィードバックには寄与しない(§6-2)。

- **出現条件を追加(v1.36.91)**: 3種とも`available`に`totalDroneCount(p) > 0`(いずれかのドローンを1台以上保有)の条件が加わった。まだ1台もドローンを保有していない状態でこれらの「ドローンを強化する」カードが出現しても効果の実感がなく候補プールを圧迫するだけだったため、最初のドローンを取得するまでは候補から隠すようにした。

| id | タイトル | 効果 | 最大ランク | ランクごとの変化 |
|---|---|---|---|---|
| `droneslots` | ドローン保有上限増加 | `Player.droneSlotRank`が+1(`droneSlotCap`もそのぶん上昇) | 3 | 保有上限 3(初期)→4→5→6 |
| `dronerange` | ドローン射程増加 | `Player.droneRangeRank`が+1(`droneRangeForRank`もそのぶん上昇) | 3 | 索敵範囲 90px(初期)→約107px→約123px→140px |
| `droneatkspeed` | ドローン攻撃速度増加 | `Player.droneAtkSpeedRank`が+1(`droneAtkIntervalForRank`もそのぶん短縮) | 3 | 攻撃間隔 2.0秒(初期)→約1.667秒→約1.333秒→1.0秒 |

- **ドローン保有上限増加**: `droneSlotCap(rank) = DRONE_SLOTS_BASE(3) + rank`。全取得(ランク3)で保有上限は6になる
- **ドローン射程増加**: `droneRangeForRank(rank) = DRONE_RADIUS_BASE(90) + DRONE_RANGE_PER_RANK × rank`、`DRONE_RANGE_PER_RANK = (DRONE_RANGE_MAX(140) - DRONE_RADIUS_BASE(90)) / 3 ≈ 16.67`。最大ランクでの到達値(140px)は、v1.36.84のバランス調整前にインパクトドローンのノックバックが作り出していた体感的な射程(検知半径90px+ノックバック50px=最大140px程度)を目安に設定した - 「統一によって失われた最大到達距離を、専用アップグレードへの投資で取り戻せる」という位置づけ
- **ドローン攻撃速度増加**: `droneAtkIntervalForRank(rank) = DRONE_ATK_INTERVAL(2.0) - DRONE_ATK_INTERVAL_REDUCTION_PER_RANK × rank`、`DRONE_ATK_INTERVAL_REDUCTION_PER_RANK = (DRONE_ATK_INTERVAL - DRONE_ATK_INTERVAL_FLOOR) / 3`、`DRONE_ATK_INTERVAL_FLOOR = DRONE_ATK_INTERVAL / 2 = 1.0`。最大ランクでも旧・攻撃間隔(v1.36.83以前の0.5秒)の2倍(1.0秒)までしか戻らない - v1.36.84の4倍化は全取得しても完全には打ち消せない、意図的な実質ナーフとして設計している
- **最大ランクの根拠**: ユーザーからの仕様提示では保有上限増加・射程増加の2つが明示的に「最大ランク3」、攻撃速度増加のみ最大ランク未指定(「最大？」)だった。他の2つとの一貫性を優先し、攻撃速度増加も同じ最大ランク3で実装した - 実際のプレイ感触次第で調整の余地がある

### 4-4. 弾丸効果・ドローンの優先枠(1枠目)

3択のうち1枠目は、プレイヤーが**既に1レベル以上取得している**(かつ上限未到達の)弾丸効果、または**既に1台以上所有している**(かつ上限未到達の)ドローン種のランクアップ(増設)があれば、その中から優先的に選ばれる。複数所有していればその中からランダム、何も所有していない(またはすべて上限到達済み)場合は通常の全プール抽選にフォールバックする。これにより、一度手を出した弾丸効果・ドローンは以降のレベルアップでも育てやすくなる。

**ドローンも対象に(v1.36.68で「補助武器」として追加)**: 当初この優先枠は弾丸効果専用で、補助武器(§4-3-3)は対象外だった。しかし補助武器も弾丸効果と同じく「取得後にランクを伸ばして育てていく」性質を持つため、同じ扱いにするのが自然と判断し、現在保有している補助武器のランクアップカードもこの優先枠の候補に含めた。v1.36.82のドローン再編後は、旧来の「現在保有している1種類だけが対象」という制約自体が意味をなさなくなった(そもそも複数種を同時に所有できる)ため、単純に**既に1台以上所有していて上限未到達のドローン種すべて**が対象になる - 弾丸効果側の`ownedEffects`(1レベル以上取得済みかつ上限未到達)と全く同じ判定をドローン側にも並行して適用しているだけで、特別扱いは残っていない

### 4-4-2. 2枠目・3枠目の弾丸効果出現率の抑制

1枠目が弾丸効果の専用枠として機能する一方、2・3枠目が均等抽選のままだと弾丸効果が通常強化+デメリット付き強化(計10種)と同程度の頻度で出てしまい、1枠目の存在意義が薄まる。そこで2・3枠目の抽選には重み付けを導入し、弾丸効果の当選ウェイトを`BULLET_EFFECT_SLOT_WEIGHT = 0.4`(通常枠の40%)に設定。

弾丸効果は現在12種(v1.36.87で脆弱を追加、v1.36.92で狂乱の泉を廃止、v1.36.95でマスタリー系3種を追加、v1.36.96でキルゾーン・ポイズンクラウド・磁気嵐を着弾効果ごと廃止)だが、爆弾化・雷撃・暴走・猛毒解放の4種は前提条件付き(`available`ゲート、上記)のため、序盤(何のゲートも満たしていない状態)では残り8種のみが実際に抽選対象になる。以下の数値はこの序盤の状態を基準にしている。

- 1枠目は、何か所有している弾丸効果があればそれらの中から均等抽選(優先枠、上記)。何も所有していない場合のフォールバックは2・3枠目と同じ重み付き抽選になる(v1.36.44でピティ導入と合わせて均等抽選から変更。弾丸効果出現率 約52.4%、11/21項目)
- 2・3枠目は重み付き抽選(弾丸効果出現率 約30.6%、`4.4/(10+4.4)`)

Playwrightで2万回のレベルアップを模擬し、1枠目 約47.1%・2/3枠目 約27.1%と設計値通りであることを確認済み(v1.36.35、着弾効果追加後の値に更新。この検証はピティ導入前のもので、全項目の`upgradeMissStreak`が0の状態を前提としている)。上記の項目数・比率はv1.36.87で脆弱を追加した後の値に機械的に更新したのみで、この2万回シミュレーション自体は再実行していない - 実測値の再検証は今後の課題。

### 4-4-3. ピティ(お情け)システム(v1.36.44で追加)

弾丸効果が13種まで増えたことで、「特定の1枚が欲しくても、プール全体の中の数枠に薄まってしまい、なかなか出現しない」という不満が生じていた。単純な確率論として、候補がN種ある中で3枠が提示される場合、狙った1枚が出現する確率はおおむね`3/N`に近似できる(N=30なら約1/10)。ローグライク的な「思い通りにならない」体験自体は許容しつつ、「一生出現しない」という理不尽さだけを緩和するため、見逃され続けた項目の抽選重みを徐々に底上げするピティ機構を追加した。

- **`Player.upgradeMissStreak`**: アップグレードid文字列をキーとした「このアップグレードが対象(=抽選プールに存在する状態)でありながら、連続して提示されなかったレベルアップの回数」を保持するオブジェクト。提示されればそのidのカウントは0にリセットされ、対象でありながら提示されなければ+1される。上限に到達した弾丸効果や、`available`ゲートを満たしていない弾丸効果など、そもそも今のプールに存在しない項目のカウントは更新されない(存在しなくなった時点のカウントがそのまま残るが、再度出現可能になるまで参照されないため実害はない)
- **重みへの反映**: `pickWeightedIndex(pool, missStreak)`が、各候補の通常の重み(通常項目=1、弾丸効果=`BULLET_EFFECT_SLOT_WEIGHT`)に対し、`1 + missStreak[id] * PITY_WEIGHT_PER_MISS`(`PITY_WEIGHT_PER_MISS = 0.15`)を追加で乗算する。見逃された回数が多いほど、その項目が実際に抽選される確率が線形に近い形で底上げされていく
- **適用範囲**: 2・3枠目の重み付き抽選、および1枠目が(何も所有していないため)通常の抽選にフォールバックする場合の両方にこのピティが適用される。1枠目が「所有している弾丸効果の中から選ぶ」優先枠として機能している場合(候補が少数に絞られた状態)は、この仕組みの対象外(ピティの狙いは「そもそも一度も手に入らない」問題の緩和であり、既に所有し伸ばしている効果同士の選択にピティを混ぜる意味は薄いため)
- **上限なし・保証ではない**: 見逃し回数に上限は設けていない - 見逃され続けるほど際限なく重みが伸び続ける設計だが、あくまで他の候補との相対的な重み比べの中での底上げであり、「〇回見逃したら次は確定で出現する」という保証ではない。他の項目も同時に見逃され続けていれば、それらとの重み勝負になる
- Playwrightで検証: 見逃し回数0の状態でのある通常強化の出現率(約21%、3000回試行)に対し、見逃し回数50(重み8.5倍相当)まで積み上げた状態では出現率が約82%まで上昇することを確認済み

### 4-5. スキップ(常設4つ目の選択肢)
- ステータス強化は一切行わない
- **このレベルアップ自体を取り消す(v1.36.25で修正)**: `Player.gainXp()`はXPが閾値を超えた時点で`level`のインクリメント・`xpNext`の再計算・武器固有効果の再適用(`applyWeaponInnateEffect()`)を先に済ませてからレベルアップ画面を開く。スキップはこの3つすべてを選択前の状態に巻き戻す(`level--` → `xpNext`をその(元の)レベルで再計算 → `applyWeaponInnateEffect()`を再実行)。修正前は`level`だけが巻き戻らずそのまま加算されてしまい、「不要な強化を避けてスキップしたはずが、次のレベルアップに必要なXP(`xpNextForLevel(level)`は単調増加)だけは黙って増えている」という、スキップの目的(悪い選択肢を回避する)に反する不利益が生じていた
- 巻き戻した(=元の)`xpNext`を基準に、その一定割合(`p.skipRefundPct`、基準値`SKIP_REFUND_PCT_BASE = 30%`)を獲得した状態にする(XPゼロ戻しではない)。この割合はキャラクターのパッシブ能力(§7-3)で変動する場合がある。カードの説明文はプレイヤーの実際の`skipRefundPct`を反映して動的に表示される
- 選択直後にHUDのXPバー・レベル表示へ即座に反映される

### 4-5-2. カテゴリ指定リロール(v1.36.70で追加、v1.36.71でカテゴリを4種に再編)

アップグレードの種類が大幅に増えたことを受け、狙ったジャンルの強化を選びやすくするための仕組み。`UPGRADE_POOL`/`TRADEOFF_POOL`/`BULLET_EFFECTS`/`DRONES`の全エントリに`category`フィールドを付与し、この分類に基づいて再抽選できるようにした。

- **カテゴリの内訳(v1.36.71時点、4種)**:
  - **オフェンス**(10種、v1.36.79でドローン「アタックドローン(旧・打撃)」を追加、v1.36.87で狂乱をクラウドへ移動・代わりに脆弱を追加、v1.36.95で猛毒解放を追加): ダメージ強化・攻撃速度アップ・捨て身の一撃・速射特化・爆発・猛毒・猛毒解放・爆弾化・脆弱・アタックドローン
  - **クラウド**(4種、v1.36.71で新設、v1.36.87で狂乱を追加、v1.36.92で狂乱の泉を廃止、v1.36.95で雷撃を追加、v1.36.96でキルゾーン・ポイズンクラウドを廃止): 連鎖・雷撃・貫通・狂乱 - いずれも1体だけでなく複数の敵を同時に巻き込む(=多対応の)効果という共通点で切り出した(狂乱は「敵同士を巻き込む」という意味で、v1.36.87の攻撃力アップ・被ダメージ増加撤廃に伴いこちらへ再分類)
  - **ディフェンス**(8種、v1.36.95で暴走を追加): 最大HPアップ・ハート回復量増加・鉄壁の構え・低速・衰弱・暴走・ジャミングドローン・インパクトドローン
  - **ユーティリティ**(6種、v1.36.84でドローン関連アップグレード3種を追加、v1.36.96で磁気嵐を廃止): 射程アップ(v1.36.71でオフェンスから移動)・移動速度アップ・回収範囲アップ・ドローン保有上限増加・ドローン射程増加・ドローン攻撃速度増加
  - v1.36.70時点ではオフェンスが14種と他カテゴリに比べて著しく多く、「オフェンスに偏りすぎる」というフィードバックを受けて再編した。射程アップは攻撃力そのものではなく間合い(≒立ち回りの選択肢)を伸ばす性質からユーティリティへ、連鎖・貫通・キルゾーン・狂乱の泉・ポイズンクラウドは「単体ではなく複数の敵に同時に効果が及ぶ」という共通性からクラウドという新カテゴリへ、それぞれオフェンスから切り出した
- **発生条件**: レベルアップ画面を開くたび(`Game.onLevelUp()`)、`REROLL_OFFER_CHANCE = 0.5`(50%)の確率で、常設4つ目の選択肢(通常は「スキップ」)が「リロール:[カテゴリ名]」に置き換わる。カテゴリは`UPGRADE_CATEGORIES`(4種)から均等ランダムに1つ選ばれる。外れた場合(50%)は通常通り「スキップ」のまま
- **選択時の挙動(`Game.rerollCategory()`)**: リロールカードを選ぶと、その回のレベルアップで表示されていた上3つの選択肢(リロールカード自体を含まない)が、指定カテゴリに属するアップグレードだけに絞って再抽選される。**元々表示されていた3つは再抽選の対象から除外される**(同じものが再度出現することはない)。この再抽選には通常のレベルアップ抽選と同じ重み付け(弾丸効果の当選率抑制・ピティ)が適用されるが、対象プールがカテゴリで絞り込まれている点のみ異なる
- **リロール後の4つ目の選択肢は常にスキップ**: リロールの結果に対して、さらにリロールを選ぶことはできない(`renderUpgradeCards(picks, allowRerollOffer)`の`allowRerollOffer`引数が、初回描画では`true`、リロール後の描画では`false`に固定される)
- **候補不足時のバックフィル**: 指定カテゴリの対象エントリが(既に上限到達・入手済みなどの理由で)3つに満たない場合、不足分は全カテゴリを対象にした通常のプールから補う(除外リストは維持される)。現在ユーティリティが4種類しかない(射程アップ・移動速度アップ・回収範囲アップ・磁気嵐)ため、この状況が現実的に発生しうる最も可能性の高いカテゴリ。この仕組みにより、リロールを選んだのに選択肢が2つ以下になる、という状況は発生しない
- **ピティへの影響**: リロールによる再抽選は、`Player.upgradeMissStreak`(§4-4-3)の更新処理を通らない。ピティは「独立したレベルアップごとに対象でありながら提示されなかった回数」を数える仕組みであり、リロールは同じレベルアップイベントの延長線上の処理と位置付けているため、二重にカウントしない
- **UI**: リロールカードは見た目上スキップと同じ「4つ目の枠」の破線スタイル(`.upgrade-card.skip-card`)を継承しつつ、これからリロールする対象カテゴリの色(下記§4-5-3)で左ボーダー・タイトル文字色を塗る(`.reroll-card`修飾クラス) - スキップの「見送る」という消極的な選択とは異なり、リロールは積極的に選ぶ価値のある能動的な選択肢であることを、カテゴリ色という形で視覚的に示す狙い(v1.36.76より前は固定のティール色で「能動的な選択肢であること」だけを示していたが、カテゴリ色分けの導入によりどのカテゴリへのリロールかも同時に一目で分かるようになった)

### 4-5-3. カード上の視覚的な識別表示(v1.36.76で追加)

アップグレードの種類・カテゴリ・ドローンの判別を、カードを開かなくても一目で分かるようにするための表示。

- **カテゴリ色分け**: レベルアップ画面に表示される通常のアップグレードカード(スキップを除く、リロールカードは対象カテゴリの色を使用)を、そのカードの`category`(オフェンス/クラウド/ディフェンス/ユーティリティ、§4-5-2)に応じて色分けする。左側のボーダーを4pxの色付きアクセントにし、タイトル文字色も同じ色に変える(`.upgrade-card.cat-offense`などの修飾クラス、`Game.renderUpgradeCards()`が`up.category`を見て自動的に付与する)。配色は次の通り: オフェンス=赤(`#ff5a5a`)、クラウド=紫(`#c084fc`)、ディフェンス=緑(`#4ade80`)、ユーティリティ=青(`#60a5fa`)
- **ドローンバッジ**: カードのidが`drone-`で始まる(=ドローンのアップグレードカードである、§4-3-3)場合、タイトルの先頭に「ドローン」という小さな角丸バッジ(`.aux-badge`、クラス名自体は導入時の「補助武器」から据え置き)を表示する。ドローンのカードは見た目が弾丸効果のカードとほぼ同じ(タイトル+説明文のみ)で区別がつきにくかったため、視覚的な目印として追加した
- どちらの表示も`renderUpgradeCards(picks, allowRerollOffer)`内でカード生成時に`up.category`/`up.id`を見て機械的に付与しているだけなので、将来カテゴリ分類やドローンの種類が増えても追加のメンテナンスなしに自動的に反映される

### 4-5-4. リジェクト(v1.36.93で追加)

レベルアップ画面のカードを長押しすることで、そのアップグレードを当該ラン中二度と出現しなくなるよう永久に除外できる機能。カードプールを自分の意思で絞り込めるようにすることで、「欲しいカードの出現確率を上げる」というピティ(§4-4-3)とは逆方向の、プレイヤー主導のビルド最適化手段を提供する。

- **対象**: 上3枚の実際に抽選されたカード全て。`UPGRADE_POOL`(ダメージ強化などの基本アップグレード)・`TRADEOFF_POOL`(デメリット付き強化)・`BULLET_EFFECTS`(弾丸効果)・`DRONES`(ドローン)のいずれの出自であっても例外なくリジェクトできる。常設4つ目の枠(スキップ、またはカテゴリリロール、§4-5/§4-5-2)は対象外 - どちらも実際にプールから抽選された「アップグレード」ではなく、常に無条件で提示される特別枠のため
- **操作**: 短いタップ/クリックは従来通りそのカードを選択する。`REJECT_LONG_PRESS_MS = 600`ミリ秒以上押し続けてから離すとリジェクトになる。押し続けている間、`REJECT_LONG_PRESS_MOVE_CANCEL_PX = 20`px以上ポインタ/指が動くとホールドは取り消され(選択もリジェクトも発生しない)、スクロール操作などとの誤爆を防ぐ。しきい値に達すると即座にカードの縁が赤く光る(`.upgrade-card.reject-armed`)視覚フィードバックが入り、そのまま離せばリジェクトが確定することが分かるようになっている
- **判定のタイミング**: 選択かリジェクトかの確定は、ホールド時間を計測するタイマー自身ではなく、指/マウスを離した瞬間(`mouseup`/`touchend`)に行う。タイマーは「しきい値を超えた」というフラグを立てて視覚フィードバックを出すだけで、実際にカードのDOMを組み替える処理(リジェクト成立時)はこの離した瞬間まで一切実行しない。これは、保持中にDOMを組み替えてしまうと、同じ画面位置に新しく生成された別カードへ離した際のイベントが誤って着弾し、意図せず別のアップグレードを選択してしまう事故が起こり得るため(マウス/タッチのreleaseイベントは、押した時の要素ではなく離した瞬間にその位置にある要素へ配送される)
- **実装(`Player.rejectedUpgradeIds`/`Player.rejectUsesRemaining`)**: リジェクトされたアップグレードの最終的なカードid(例: `pierce`、`bullet-poison`、`drone-jamming` - `buildUpgradePool()`が生成する各エントリの`id`そのもの)を`Set`として保持する。`Game.buildUpgradePool()`は生成した全候補の末尾で、この`Set`に含まれるidを一括で除外する(プールの出自を問わない単一のフィルタ)。`Game.onLevelUp()`内部で1枠目の優先枠(§4-4)向けに個別に計算している`notMaxedEffects`/`notMaxedDrones`にも同じフィルタを適用しており、リジェクト済みの効果が優先枠経由で漏れて再出現することはない
- **使用回数**: `REJECT_USES_BASE = 3`回、ランごとにリセットされる(`Player`の他の状態同様、`new Game()`のたびに新しいインスタンスが持つ)。残り回数は`Player.rejectUsesRemaining`で管理し、リジェクトが成立するたびに1減る。0になった状態で長押ししても、しきい値に達する赤いフィードバックが表示されず(タイマー自体を開始しない)、離せば通常通りそのカードを選択する動作にフォールバックする
- **UI表示**: レベルアップ画面のタイトル直下に「長押しでリジェクト　使用回数:残りn回」という説明文(`#reject-hint`)を常時表示する。残り0回になると`.depleted`クラスが付き、半透明になって「もう使えない」ことが視覚的にも分かるようにしている
- **リジェクト成立時の挙動**: リジェクトされたカードのスロットだけを、更新後のプール(現在画面に表示中の他2枚のidを除外)から改めて重み付き抽選(`pickWeightedIndex`、ピティ込み)で1枚引き直し、その場で差し替える。**他の2枚・常設4つ目の枠はそのまま変化しない** - 1枚を除外した結果、他の2つの選択肢まで失われるのは体験として望ましくないと判断した。差し替えで新たに引かれたカードは、その回のピティ集計(`Player.upgradeMissStreak`)がすでに終わった後に登場するため、個別に見逃し回数を0リセットして「今回提示された」扱いにしている
- **極端なケース(プール枯渇)**: ラン終盤で大半のアップグレードが上限到達・リジェクト済みになり、差し替え候補が1枚も残っていない場合は、そのスロットを単純に削除する(3枚あった選択肢が2枚になる)。頻繁には起こらない想定の安全策で、専用のUIメッセージなどは用意していない
- **DOM構造**: 上3枚のカードは`#upgrade-choices`の中に新設した`#upgrade-picks`という専用のサブコンテナに収める(常設4つ目の枠は引き続き`#upgrade-choices`の直下)。これにより、リジェクトによる1枚差し替え時は`#upgrade-picks`の中身だけを再構築すればよく、4つ目の枠(リロールカードなら選んだカテゴリの色・クリックハンドラなど、状態を保持した状態)を無傷で残せる
- **入力方式**: マウス(`mousedown`/`mousemove`/`mouseup`/`mouseleave`)とタッチ(`touchstart`/`touchmove`/`touchend`/`touchcancel`)の両方に個別にハンドラを持たせる方式で、本作の他の入力処理(自機移動のドラッグ操作など、§2)と同じ流儀を踏襲している(Pointer Events APIは使用していない)。カードはこの独自ハンドラのみで選択・リジェクトの両方を処理するため、ブラウザ標準の`click`イベントリスナーはカードに一切設定していない

### 4-6. レベルアップ後・一時停止解除後の無敵時間(v1.30.0で変更)

強化(またはスキップ)を選んだ直後、および一時停止から再開した直後は、指が画面から離れている(≒無操作)状態からゲームが再開する。

- 以前は「タップして再開」という半透明オーバーレイを表示し、実際に画面をタップ/ドラッグ開始するまでゲームを完全に停止させる仕組みだったが、廃止した
  - 廃止理由: この仕組みだと、レベルアップ画面を開いている間に敵が回避不可能な距離まで接近していた場合、プレイヤーは「タップした瞬間に被弾するとわかっていても、タップしないと操作すら再開できない」という詰み状況になり得た
- 代わりに、選択直後・再開直後に`RESUME_INVULN_DURATION = 1.0`秒の無敵時間を付与する(`Player.invulnTimer`を通常の被弾時無敵と同じ仕組みで直接セット。被弾時と同様に点滅表示される)。これにより、ゲームは即座に(タップを待たずに)動き出しつつ、直後の理不尽な被弾は避けられるようになっている
- `awaitingResume`という専用の待機状態は廃止され、レベルアップ画面を閉じた時点・一時停止を解除した時点で即座に通常の`update()`が再開する

---

## 5. 敵

### 敵タイプ(`ENEMY_TYPES`)

| タイプ | HP(基礎) | 速度 | 半径 | 接触ダメージ(基礎) | XP | バースト数 | 解禁条件 |
|---|---|---|---|---|---|---|---|
| grunt(通常) | 12 | 78 | 13 | 8 | 3 | 1 | 難易度1以上(常時解禁) |
| fast(高速) | 7 | 140 | 10 | 6 | 4 | 2 | 難易度2以上 |
| gunner(ガンナー、v1.36.60で追加) | 9 | 70 | 12 | 6 | 5 | 1 | 難易度3以上 |
| tank(タンク) | 47 | 48 | 20 | 14 | 10 | 1 | 難易度5以上 |
| blitz(ブリッツ、v1.36.60で追加、v1.36.74でHP調整) | 18 | 55 | 13 | 8 | 6 | 1 | 難易度6以上 |
| boss(ボス) | 333 | 35 | 32 | 20 | 50 | 1 | 難易度8以上、`BOSS_SPAWN_INTERVAL=90秒`ごとに1体だけ出現 |

- **バースト数(`ENEMY_TYPES[type].burst`、v1.32.0で追加)**: 1回の出現イベントでその敵タイプが何体まとめて出現するかを表す、敵タイプ固有の個性。fastのみ2体(同じ方向から角度をわずかにジッターさせつつ、まとまった集団として出現)、他は1体。以前は難易度・クラウド系ビルドに応じてスケーリングする「同時出現数」という難易度調整の役割を兼ねていたが、その役割は出現頻度(§6-1)側に一本化し、バースト数は純粋に敵タイプの演出要素として独立させた
- **出現タイプの決定(`spawnEnemy()`、v1.36.62でウェーブ単位の出現パターン方式に変更)**: 以前は敵タイプごとに固定の当選率を持つ1回の乱数抽選(`if (D>=5 && r<0.22) tank; else if (D>=2 && r<0.5) fast; ...`)だったが、`Game.currentSpawnPattern`(そのウェーブで出現を許可する敵タイプの配列)からの均等抽選に置き換えた。`currentSpawnPattern`自体は下記「敵の出現パターン」で決まる。ボスは対象外で、`forceType`指定時(ボスの強制出現)はこの抽選自体を経由しない(従来通り)
- ボスは他タイプのような出現率抽選には参加せず、`BOSS_MIN_DIFFICULTY`以上になって以降、90秒ごとに単体で必ず1体出現する独立イベント。基礎HPが他タイプと比べ桁違いに高く、意図的に「マルチショットだけでは処理しづらい単体巨大HP」の壁として機能させ、ダメージ・攻撃速度・爆発・連鎖など他の軸への投資も促す設計
- 画面内(正確にはゲーム全体)に同時に存在できる敵の数には上限(`MAX_ALIVE_ENEMIES = 120`、v1.32.0で追加)があり、上限に達している間は新たな敵が出現しない(出現イベント自体がスキップされる)。撃破が出現に追いつかない状況が続いた場合の、敵の際限ない積み上がりに対する安全弁
  - **例外**: ボスの強制出現(`spawnEnemy('boss')`、通常の抽選を経ない出現)はこの上限の対象外で、上限に達していても必ず出現する。エマージェンシーボムのGEM_CAP例外(§5後述)と同じ考え方で、稀で確定的なイベントを無関係な上限に握り潰されないようにするための措置
- 敵の頭上へのHPバー表示は廃止(v1.30.0)。画面の見やすさを優先し、敵の見た目は単色の円のみとした(状態異常インジケータ用の小さなドット(§4-3)は例外)
- 敵は基本的にプレイヤーへ向けて直進し(壁に阻まれている場合は§10-3の迂回ロジックが働く)、接触するとダメージ(0.5秒クールダウン)を与える
  - **移動のゆらぎ(蛇行)は`fast`タイプのみ(v1.36.56で全タイプに追加→v1.36.57で全撤回→v1.36.58で`fast`のみ再導入)**: 狙う方向の角度に`ENEMY_WANDER_AMPLITUDE = 25度`を振幅とする`sin(this.time * wanderFreq + wanderPhase)`(`wanderFreq`は敵ごとに`ENEMY_WANDER_FREQ_MIN(0.4)〜ENEMY_WANDER_FREQ_MAX(0.9)`ラジアン/秒でランダム、`wanderPhase`も敵ごとにランダム)を加算してから移動する仕組み自体は全タイプ共通で一度実装したが、実際にプレイすると視覚的にやや酔いやすいというフィードバックでv1.36.57にて一旦全撤回。ただし`fast`タイプ単体では酔わずむしろ動きにバラつきが出て良い、というフィードバックを受け、`ENEMY_TYPES.fast`にだけ`wander: true`フラグを立てて`fast`限定で再度有効化した(`Enemy.wanders`、grunt/tank/bossはfalseのまま直進のみ)。壁に阻まれて迂回方向(フローフィールド)を採用している場合も同じゆらぎがかかる点、振幅が90度未満のため自機への接近を妨げない点は変更なし
- 撃破すると一定確率でXPジェムをドロップし、パーティクルが散る(ドロップしなくても撃破数にはカウントされる)
- XPジェムは出現から60秒間(`GEM_LIFESPAN`)未回収のままだと消滅する。高難易度で大量の敵を倒した際に画面がジェムで埋め尽くされるのを防ぐための措置
- **ジェムは確定ドロップではない**: 敵を倒した際、`BASE_GEM_DROP_CHANCE`(§6-1参照。現在 `0.5 / ENEMY_SPAWN_RATE_MULT ≈ 33%`)の確率でのみジェムが生成される。生成された場合のジェムの価値は敵のXP値の`GEM_VALUE_MULT = 2`倍(固定)。ドロップ確率はさらに`crowdPowerMult`(§6-2、貫通×連鎖Lv)の超過分に応じて割り引かれる: `dropChance = min(1, BASE_GEM_DROP_CHANCE / (1 + crowdExtra))`
  - 何もクラウド系強化を取っていないビルド(`crowdExtra = 0`)では、`BASE_GEM_DROP_CHANCE × GEM_VALUE_MULT`が期待値としての「1キルあたりのXP倍率」になる
  - クラウド系ビルドほど難易度側の敵出現数も増える(§6-2)ため、これを対策しないと「敵を捌けるビルドほど討伐数が増え、ジェム供給量も比例して増え、レベルアップが加速し、さらに強くなる」という再帰的なインフレが起きてしまう
  - 旧仕様(ジェムは確定ドロップだが1個あたりの価値を割り引く方式)から変更した理由: 価値を薄めるだけでは生成されるジェムの「個数」自体は変わらず、画面のジェム過密(クラッター)対策にはならなかったため。ドロップ確率側を絞る方式なら、経済(総XP収入の抑制)とクラッター対策を同時に達成できる
- 画面内(正確にはゲーム全体)に同時に存在できるジェム数には上限(`GEM_CAP = 60`)もあり、上限に達している間は(ドロップ抽選に当たっていても)ジェムが生成されない。サバイブ後半、敵出現数の増加とプレイヤーの成長が重なって討伐数がジェム寿命(60秒)より速いペースで積み上がるケースへの二重の安全弁
  - **例外**: エマージェンシーボム(§7-2)による強制撃破(`forceKilled`フラグが立った敵)は、ドロップ確率・上限規制のいずれの対象外でもなく、常に(2倍価値の)ジェムをドロップする。ボムの狙いは「ここぞでジェムを一気に回収して大量レベルアップする」ことにあるため、抽選や上限でそのバーストが握り潰されないようにするための例外措置

### 敵の出現パターン(`Game.rollSpawnPattern`、v1.36.62で追加)

「あるウェーブでは1種類だけ」「別のウェーブでは3種類混合」のように、ウェーブ(§5後述、`DIFFICULTY_CHECK_INTERVAL = 50秒`ごとの区切り)単位で出現する敵タイプの顔ぶれ自体をランダムに変化させる仕組み。ボスは対象外(常に自身の別枠で出現、上記)。

- **`ENEMY_TYPES[type].minDifficulty`**: grunt/fast/gunner/tank/blitzそれぞれの解禁難易度(1/2/3/5/6)を、旧来の抽選コードに埋め込む形ではなく`ENEMY_TYPES`自体のフィールドとして持たせている。`rollSpawnPattern()`と`spawnEnemy()`の両方がここを直接参照するため、解禁条件を二重管理してずれる心配がない(boss除く`NON_BOSS_ENEMY_TYPES`はこのフィールドを持つ全タイプを`Object.keys(ENEMY_TYPES)`から動的に抽出したもの)
- **パターンの決定(`rollSpawnPattern()`)**: その時点の`this.difficulty`で解禁済みの非ボスタイプ一覧(`unlocked`)を求め、`[1, 2, 3, unlocked.length]`という4つの候補サイズから均等な確率で1つを選ぶ(難易度がまだ低く`unlocked.length`が4未満の場合は大きい方のサイズが自動的にクランプされる - 例えば難易度2時点では`unlocked = [grunt, fast]`の2種類しかないため、候補サイズ`[1,2,3,2]`はいずれも最大2に丸められる)。選んだサイズぶんだけ`unlocked`からランダムに(重複なく)タイプを抜き出したものが`Game.currentSpawnPattern`になる
- **再抽選のタイミング**: `Game`のコンストラクタで1回(ゲーム開始直後から有効な状態にするため)、以降はウェーブが切り替わるたび(difficultyの増減が確定した直後)に再抽選する。厳密なローテーション(1→2→3→全種、のような順序)ではなく毎回独立した抽選のため、同じサイズ・同じ組み合わせが偶然連続することもある(仕様として許容 - 「組み合わせやローテーションはランダムで大丈夫」という要望に基づく)
- **`spawnEnemy()`側の変更**: 旧来のタイプごとに固定の当選率を持つ1回の乱数抽選を廃止し、`this.currentSpawnPattern`からの均等ランダム抽選(`currentSpawnPattern[randInt(0, currentSpawnPattern.length - 1)]`)に置き換えた。そのウェーブのパターンに含まれないタイプは(たとえ難易度的に解禁済みでも)一切出現しない。パターンのサイズが小さいウェーブほど、その中の各タイプの実質的な出現頻度は自動的に高くなる(例: 1種類だけのパターンならそのタイプが100%を占める)

### ガンナー・ブリッツの行動パターン(v1.36.60で追加)

grunt/fast/tank/bossが「常に自機へ直進する(壁があれば迂回する)」だけの単純な移動ロジックを共有しているのに対し、gunner・blitzの2種は`Game.updateGunnerMovement`/`updateBlitzMovement`という専用の状態機械で動く。`Enemy.ranged`/`Enemy.charger`フラグ(`ENEMY_TYPES`側の`ranged`/`charger`フラグから生成時に設定)で通常の移動ループから分岐している。いずれも`resolveWallCollision`・状態異常のティック処理・自機との接触ダメージ判定など、既存の汎用ロジックはそのまま共通で通る(接触ダメージ判定は特にブリッツの体当たりダメージにもそのまま使われている、詳細は下記)。

**ガンナー(`gunner`)**: 自機からの距離に応じて「接近」と「静止射撃」を切り替える。

- 自機との距離が`enemyEngagementRadius() * GUNNER_STOP_DIST_FRAC(0.4)`以下になるまでは、他の敵と全く同じ直進(壁があれば迂回)で接近する
- その距離に達すると`Enemy.rangedHolding = true`になり、以後その場から一切動かなくなる。かわりに`GUNNER_ATK_INTERVAL = 1.8秒`間隔で自機に向けて`EnemyProjectile`を発射する(`GUNNER_PROJ_SPEED = 260px/秒`、ダメージは`GUNNER_ATK_DAMAGE_BASE`に接触ダメージと同じ`dmgMult`を掛けた値で、難易度に応じてスケーリングする)。複数体が同時に静止射撃を始めても足並みを揃えて撃たないよう、`rangedCooldown`の初期値は出現時に`0〜GUNNER_ATK_INTERVAL`のランダムな値からスタートする
  - **射撃ダメージの調整(v1.36.61)**: 初期値の`GUNNER_ATK_DAMAGE_BASE = 6`(接触ダメージと同値)は威力が高すぎるとのフィードバックを受け、`fast`の接触ダメージ(`ENEMY_TYPES.fast.dmg = 6`)のおよそ半分となる`3`に引き下げた
  - **色の変更(v1.36.61)**: 本体・射撃弾ともに初期実装時は水色(`#3ad1ff`、ちょうどXPバーの左端の色と同じ)だったが、ジェムの色(`#7fffd4`)と紛らわしいというフィードバックを受け、緑(`#22c55e`)に変更した。射撃弾の描画色は本体の色(`ENEMY_TYPES.gunner.color`)をそのまま参照しているため、今後また変更しても2つが食い違うことはない
- 自機が移動するなどして距離が`enemyEngagementRadius() * GUNNER_RESUME_DIST_FRAC(1.0)`(=ちょうど敵の出現距離、つまり画面外相当)を超えて開くと、`rangedHolding`が解除されて再び接近を再開する。停止距離(0.4倍)より再開距離(1.0倍)を大きく取っているのは、この2つの距離がもし同じだと、その境界上をうろつくだけで静止と接近を毎フレーム切り替えてしまう(ハンチング)ため
- **弾は壁に当たると消える(ご要望通り)**: `EnemyProjectile`の移動処理は自機の弾(`Projectile`)と全く同じ`segmentHitsWall`判定を使い、壁と交差した時点で消滅する。プレイヤー側から見ると、壁の陰に隠れることでガンナーの射撃を防げる

**ブリッツ(`blitz`)**: 「接近」「静止(予備動作)」「突進」の3状態を`Enemy.blitzState`で管理する。

- **接近**: gunnerと同じ直進(壁があれば迂回)で自機に近づく。`enemyEngagementRadius() * BLITZ_STOP_DIST_FRAC(0.3)`(ガンナーの停止距離より自機に近い)まで来ると「静止」へ移行する
- **静止**: `BLITZ_PAUSE_DURATION = 1秒`間その場で完全に停止する(予備動作)。この間、自機側から見て次に突進が来ることを察知する猶予になる
- **突進**: 静止が終わった瞬間の自機の方向をそのまま固定し(以降は自機を追尾しない、直線軌道)、`BLITZ_CHARGE_SPEED_MULT`倍の速度で直進する。接触ダメージも`BLITZ_CHARGE_DAMAGE_MULT = 1.8倍`に一時的に引き上げられる(`Enemy.baseDmg`に難易度スケーリング済みの通常接触ダメージを保持しておき、突進中だけ`Enemy.dmg`をその1.8倍に差し替え、突進が終わると元に戻す仕組み。既存の接触ダメージ判定コード自体は一切変更していない)。自機に当たったかどうかに関わらず、`enemyEngagementRadius() * 2`(画面の直径相当)ぶんの距離を移動し終えると自動的に「接近」へ戻り、同じサイクルを繰り返す
  - **突進速度(`BLITZ_CHARGE_SPEED_MULT`、v1.36.74で調整)**: 導入当初は固定倍率`4`(通常速度55との積で220px/秒)だったが、あらゆる難易度・武器・自機・ビルドの組み合わせで「突進が来る前に大抵倒せてしまい、存在感が薄い」というフィードバックを受けて見直した。単に倍率を上げるのではなく、`BLITZ_CHARGE_SPEED_MULT = GUNNER_PROJ_SPEED / ENEMY_TYPES.blitz.speed`という比率として算出するよう変更し、無被弾(低速効果なし)時の突進速度がガンナーの弾速(`GUNNER_PROJ_SPEED = 260px/秒`)とちょうど一致するようにした(fastの通常速度140よりも明確に速い)。倍率を`effSpeed`(低速・ラッシュなど既存の速度修正が反映された実効速度)に掛ける計算式自体は変えていないため、低速状態の敵の突進が遅くなる・ラッシュ中の敵の突進が速くなるといった既存の副次効果はそのまま維持される
  - **HP(`ENEMY_TYPES.blitz.hp`、v1.36.74で調整)**: 導入時は10(gunnerの9とほぼ同等)だったが、上記と同じフィードバックを受けて18に引き上げた。解禁難易度が1段階遅いtank(47)には遠く及ばない水準に留めている - ブリッツの本質的な脅威は継戦能力ではなく突進そのものであるため、HP自体を過度に伸ばす方向のバランス調整はしていない
  - 突進中は壁の迂回ロジックを一切参照しない(狙いを外れて自機を追尾したりしない、文字通りの直線軌道)。ただし`resolveWallCollision`自体は他の敵と同様に毎フレームかかるため、壁に突っ込めばそこで物理的に止まる。その場合でも突進の残り距離は(壁に阻まれて実際には進めていなくても)時間経過で消費され続けるため、いずれ規定の距離に達して「接近」に戻る - 壁に張り付いたまま永久に突進状態で固まることはない

### 難易度(`difficulty`)とウェーブ(`wave`)
- 初期値は1。`DIFFICULTY_CHECK_INTERVAL = 50秒`ごとに「撃破率に応じた昇降判定」が行われる、階段状の単一の難易度指標
- この50秒区切りは「ウェーブ」と呼ぶ(`Game.wave`、v1.36.13で命名。以前は「50秒ウィンドウ」と呼んでいた)。ゲーム開始時はウェーブ1で、難易度チェックのたびに1つずつ増える
- HUDのタイマー横に「ウェーブN 残りM秒 難易度K」として常時表示(Nはウェーブ番号、Mはそのウェーブが終わるまでの残り秒数、Kは難易度)
- 敵HP・敵ダメージのベース倍率、出現間隔、同時出現数、敵種解禁のすべてが難易度指標で駆動される

#### 撃破率による昇降判定(ウェーブごと、v1.35.6でティアを追加、v1.36.26で計測ロジックを分母側の修正に変更)
直前のウェーブ(50秒間)で「プレイヤーが対処すべきだった敵の総数」(`poolThisWindow`)と、そのうち倒した数(`killsThisWindow`)から撃破率を算出し、次のウェーブの難易度を決める:

```
poolThisWindow = aliveAtWaveStart + spawnedThisWindow
撃破率 = killsThisWindow / poolThisWindow
```

- `spawnedThisWindow`: そのウェーブで新規に出現した敵の数
- `aliveAtWaveStart`: そのウェーブが始まった瞬間に、前のウェーブから持ち越されてまだ生きていた敵の数(ウェーブ境界のたびに`Game.enemies.length`を記録)
- `killsThisWindow`(`Game.killsThisWave`): そのウェーブ中に倒した敵の数(出現ウェーブは問わない。ウェーブ境界のたびに0にリセット)

**この撃破率の目的は「難易度がプレイヤーにとって厳しすぎる/ぬるすぎるかを判定する」ことにある(§6-1で解説)。この目的に照らすと、撃破数はそのウェーブに出現した敵に限定すべきではない**: 前のウェーブから持ち越された敵を倒すのも、プレイヤーから見れば紛れもなく「そのウェーブでの対処」の一部であり、無視すると実際の対応状況を過小評価してしまう。例えば前ウェーブ出現の敵10体・今ウェーブ出現の敵10体(計20体)がいる状況で、今ウェーブに持ち越し分10体だけを倒した場合、プレイヤーの体感は「敵の半数を処理した」だが、撃破数を出現ウェーブで絞り込む実装だと「0体」と計測されてしまい、本来の目的(体感に見合った難易度調整)に反する。

このため、v1.36.25では出現ウェーブが一致する敵のみを撃破数としてカウントする形で対応したが(100%超えは防げるが、上記の過小評価が新たに発生する)、v1.36.26で分母側に持ち越し分(`aliveAtWaveStart`)を足す形に修正した。分子(`killsThisWindow`)は単純に「そのウェーブで倒した敵の総数」に戻し、出現ウェーブによる絞り込みは行わない。持ち越し敵を倒しても・出現数分の敵を倒しても、いずれも同じ「プールを減らす」行為として等しく評価され、かつプール自体が持ち越し分を含む以上、撃破率が100%を超えることも起こらない(倒した敵の総数は、そのウェーブに存在し得た敵の総数を超えられないため)。

| 撃破率 | 次のウェーブの難易度 |
|---|---|
| 90%以上、かつこのウェーブでラッシュ(§6-6)が終了 | `+RUSH_DIFFICULTY_BONUS`(+3) |
| 90%以上(ラッシュ以外の通常ウェーブ) | `+DIFFICULTY_STEP_HIGH_KILL_RATE`(+2) |
| 70%以上90%未満 | +1(通常通り上昇) |
| 50%超70%未満 | 変化なし(据え置き) |
| 30%超50%以下 | -1 |
| 30%以下 | `-DIFFICULTY_STEP_LOW_KILL_RATE`(-2、下限は1) |

元々は「70%超で+1・50%以下で-1・その間は据え置き」という3段階の緩衝(ラバーバンド)のみだったが、両端にさらに極端なティアを追加(v1.35.6)し、好調・不調のどちらでも振れ幅が反映されるようにした。90%以上のティアは通常ウェーブでも+1より大きい+2になるが、そのウェーブがちょうどラッシュの終了タイミング(§6-6)と重なっていれば、ラッシュを撃破率90%以上で走り切った証としてさらに+3まで引き上がる。プレイヤーが敵を捌ききれていない場合に難易度上昇を止める・引き下げるという「撃破率に応じた難易度緩和」の目的自体は変わらず、直近ウェーブのみで判定するため過去の好不調に引きずられず、その時点の戦況に素早く反応する。

HUDには進行中のウェーブにおけるこの撃破率をリアルタイムで表示(`撃破率 XX%`)。まだそのウェーブで敵が1体も出現していない場合は `撃破率 --` と表示する。

---

## 6. 難易度スケーリングの仕組み

難易度は「難易度による緩やかな階段状の底上げ」と「プレイヤーのビルド(強化の取り方)に応じた追加スケーリング」の2層構成。**強化を取っていない軸には追加スケーリングがかからない**設計により、「攻撃力を伸ばしていないのに敵だけ硬くなる」といった噛み合わない難易度上昇を避けている。

### 6-1. 難易度による階段状のベース値(`D = difficulty`)

| 項目 | 計算式 |
|---|---|
| 敵HP倍率 | `1 + (D-1) * 0.08925` |
| 敵ダメージ倍率 | `1 + (D-1) * 0.070125` |
| 出現間隔(難易度基準、`tierInterval`) | `max(0.22, 1.1 - (D-1) * 0.11)` 秒 |
| 実際の出現ペース | `tierInterval`から算出した出現イベント頻度(秒あたり回数)を`SPAWN_RATE_MIN`〜`SPAWN_RATE_MAX`にクランプしたもの(§6-3) |
| fast出現開始 | `D >= 2` |
| tank出現開始 | `D >= 5` |

「同時出現数」は難易度からは撤廃した(v1.32.0)。1回の出現イベントで何体出現するかは、難易度に関係なく敵タイプ固有の`burst`値(§5)で決まる。

敵HP/ダメージ倍率の係数は元々0.18/0.14→0.14/0.11と経て、v1.35.7で0.07/0.055(いずれも半減)にさらに緩めた。理由は防御性能とのバランス検証(下記)で、撃破率95%を維持できる強いプレイでは15分時点で難易度が43まで到達し、HP投資なしだとbossの接触ダメージが最大HPの112%(即死)、最大HPアップを6回集中投資しても71%に達することが判明したため。`tierDmgMult`はDに対して線形かつ無上限(ソフトキャップなし)であることに加え、`survivalPowerMult`経由でHP投資自体が被ダメージを押し上げる設計(§6-2)もあり、「回避が容易な序盤は火力優先→終盤に一撃大ダメージを食らってからではHPを積み増す余地がない」という構造的な問題があった。半減後は同条件でbossの被ダメージが66%(HP投資なし)・56%(2回投資)・48%(4回投資)まで下がり、「火力に余裕が出てきたらHPに振る」という選択が現実的な安全マージンとして機能するようになった。

その後v1.36.27で0.105/0.0825(0.07/0.055の1.5倍)まで再び引き上げた。半減した時点からの間に脅威ビジョン(§7)、ジャミングドローン・衰弱アップグレード、移動速度・回収範囲アップグレードなど、プレイヤー側の被弾回避・耐久手段が複数追加されており、また同バージョンでウェーブごとのHP回復(旧`WINDOW_HEAL_FRAC`/`RUSH_HEAL_FRAC`)を廃止しハート型HP回復アイテム(§6-7)に置き換えたことで難易度カーブを再度締め直す余地ができたため。オリジナルの0.14/0.11には遠く及ばない水準に留めている。

その後v1.36.40で0.08925/0.070125(0.105/0.0825の15%減)まで少し緩めた。着弾効果(§4-3-2)の追加や狂乱のスタック再設計(§4-1、v1.36.39)など、直近の複数の変更が積み重なって全体の難易度が上がりすぎていたための調整。両係数の比率(dmg:hp = 0.11:0.14 ≈ 0.0825:0.105 ≈ 0.070125:0.08925)は一貫して維持し、伸びる速さだけを調整している。

難易度の昇降判定間隔(`DIFFICULTY_CHECK_INTERVAL`)は60秒→**50秒**に短縮。レベルアップ必要XPが上限(v1.9.0)で頭打ちになるようになったため、その分プレイヤーが長時間サバイブした際の自然な難易度上昇も少し速める調整。

**序盤(D1〜5)限定の敵HP割引(`earlyGameHpMult(D)`、v1.36.73で追加)**: 難易度1(何のティア補正もかからない状態)の時点で、grunt(基礎HP12)が初期状態のスタンダード武器(ダメージ10)で1発では倒せず、ラピッドファイア(1発あたり約5.5ダメージ)だと3発必要になるなど、レベルアップで火力系の強化を1つも取っていない新規プレイヤーが最初の敵をなかなか倒しきれない、というバランス上の問題があった。敵の出現頻度・敵ダメージ自体は特に問題視されていなかったため、`hpMult`(敵HP倍率)にのみ、D1で25%引き・D5でちょうど0%(効果なし)になるよう線形に薄れていく追加倍率を掛けている:

```js
EARLY_GAME_HP_DISCOUNT = 0.25
EARLY_GAME_HP_DISCOUNT_END_DIFFICULTY = 5
earlyGameHpMult(D) = D >= 5 ? 1 : 1 - 0.25 * (5 - D) / (5 - 1)
// D=1: 0.75 (25%引き) / D=2: 0.8125 / D=3: 0.875 / D=4: 0.9375 / D=5以降: 1(効果なし)
```

`tierHpMult`・`offenseExtra`・`spawnRateOverflow`・ラッシュ倍率と並ぶ形で`hpMult`の計算式に追加で乗算されるだけなので、既存のどの項の計算式も変更していない。D5以降(`tank`の解禁ラインと一致)では常に1倍(完全に無効)になるため、以降の難易度カーブや`tank`自体のHPには一切影響しない。この結果、D1のgrunt HPは12→9(切り捨てなし、四捨五入)に下がり、スタンダード武器の1発(ダメージ10)で確実に倒せるようになった。ダメージ・出現ペースは一切変更していない(この調整は`hpMult`のみに作用し、`dmgMult`や出現間隔の計算式には一切関与しない)。

### 6-2. プレイヤーのビルドに応じた追加スケーリング

プレイヤーの現在ステータス・弾丸効果レベルを初期値(`BASELINE_STATS`)と比較し、強化度合いに応じた倍率を算出:

```js
offensePowerMult  = (damage / 10) * (0.7 / atkCooldown) * (1 + explosionLevel * 0.15)
                    // ダメージ×攻撃速度、爆発Lvも同じ枠に加算(爆発=実質ボーナスダメージのため)
crowdPowerMult    = (1 + pierce * 0.15) * (1 + chainLevel * 0.15) * (1 + killzoneLevel * 0.15)
                    * (1 + magnetstormLevel * 0.15) * (1 + lightningLevel * 0.15)
                    * (1 + max(0, rangeMult - 1) * 0.5)
                    // 貫通・連鎖・キルゾーン・磁気嵐・雷撃Lvも同じ枠にフル加算(いずれも1回の
                    // 行動で複数の敵を同時に処理・拘束する役割が同じため。雷撃はv1.36.95で
                    // 追加、連鎖と全く同じ理由で加算)。killzoneLevel/magnetstormLevelは
                    // v1.36.96でどちらも常時0になった(§4-3-2、着弾効果アップグレード自体を
                    // 廃止したがコードは維持)ため、この式では実質的に無効な項になっている
                    // (計算式自体は将来の再利用に備えてそのまま残してある)。射程アップは
                    // 超過分の50%のみ加算(理由は下記、最大HPと同じ減衰パターン)。
                    // マルチショットは武器固有効果(選択ではない自動ランクアップ)
                    // になったため、v1.24.0でこの式から除外した。
survivalPowerMult = (1 + max(0, maxHp / 100 - 1) * 0.5 + max(0, heartHealFrac / 0.2 - 1) * 0.5) * (1 + slowLevel * 0.15)
                    // 最大HP・ハート回復量ともに超過分を0.5倍に減衰させてから加算(理由は下記)。
                    // 低速Lvは引き続きフル加算(被弾回数そのものを減らす効果のため)。
```

これらが初期値(=1)を超えた分(`〜Extra = max(0, 〜Mult - 1)`)だけ、以下に上乗せされる:

| プレイヤーが伸ばした軸 | 追加スケーリングの対象 | 係数 |
|---|---|---|
| ダメージ × 攻撃速度 × 爆発Lv | 敵HP | `1 + offenseExtra * 0.25` |
| 貫通 × 連鎖Lv × キルゾーンLv × 磁気嵐Lv × 雷撃Lv × 射程アップ | 出現ペース(イベント頻度)の上昇 | イベント頻度に`(1 + crowdExtra * 0.5)`を乗算(§6-3) |
| 最大HP × ハート回復量 × 低速Lv | 敵の接触ダメージ | `1 + survivalExtra * 0.5` |

貫通×連鎖の影響先は「同時出現数」ではなく「出現ペース」に統一した(v1.32.0)。以前は同時出現数側にも`+ round(crowdExtra * 2)`という別枠の加算があったが、バースト数を敵タイプ固有の演出要素として切り離した(§5)のに伴い撤廃し、クラウド系ビルドの影響はすべて出現頻度側に集約している。

オフェンス系の係数(0.25)は意図的に低めに設定しており、ダメージ/攻撃速度を強化した際に「強くなった実感」が敵の硬さ上昇でほぼ相殺されないようにしている(例: ダメージのみ2倍にした場合、敵HPは約1.25倍に留まる)。

**各軸の割り当ては§4-5-2のアップグレードカテゴリ(オフェンス/クラウド/ディフェンス/ユーティリティ)と対応させて再編した(v1.36.72)**。以前は各`〜PowerMult`への算入・除外がその都度個別の判断で決められていたが、カテゴリという上位の分類がすでに存在するため、原則としてクラウドカテゴリの効果は`crowdPowerMult`、ディフェンスカテゴリの効果のうち「実際にHP/被ダメージを直接動かすもの」は`survivalPowerMult`、という対応に揃えた:

- **`crowdPowerMult`に貫通・連鎖に加えてキルゾーン・磁気嵐・射程アップを追加(v1.36.72)**: いずれも「1回の行動(ヒット・発動)で複数の敵を同時に処理・拘束できる」という共通点を持つ(キルゾーン・磁気嵐は範囲効果、射程アップは同時に交戦範囲に入る敵の数そのものを増やす)。**爆弾化Lvはこの枠から除外**(v1.36.5で追加時は未加算→v1.36.10でクラウド枠に追加→v1.36.72で再度除外)。効果自体はクラウド対応手段として強力だが、爆発Lvへの依存度が高くビルドとして独立した「クラウド投資」と言い切れない面があるため、いったんどの軸にも加算しない扱いに戻した
- **`crowdPowerMult`に雷撃Lvを追加(v1.36.95)**: 連鎖の伝播方式を分岐型に変えるマスタリー系弾丸効果(§4-3-1)。「1回の発動で複数の敵を同時に処理する」という連鎖と全く同じ役割を、より強い形で担うため、連鎖と同じ0.15の加算幅でこの枠に含めた。3種あるマスタリー系弾丸効果(雷撃・暴走・猛毒解放)の中で難易度フィードバックを持つのは雷撃のみ(下記)
- **`survivalPowerMult`に最大HP・低速に加えてハート回復量増加を追加(v1.36.72)**: ハート回復量は最大HPと同じ「実質的なHPプール」を伸ばす効果であるため、最大HPと全く同じ減衰パターン(超過分の50%のみ加算)で扱う。**ドローン(ジャミング/インパクト)・衰弱Lvはこの枠から除外**(ドローンは v1.36.63でこの枠に追加されたが、効果が至近距離限定でやや地味なため、衰弱はv1.36.10でこの枠に追加されたが、対象を難易度スケーリングに含めるほどの防御効果とは言えないと判断し、共にv1.36.72でどの軸にも加算しない扱いに変更)
- **移動速度アップは引き続きどの軸にも未加算**: 敵を回避しやすくなる効果ではあるものの、直接HP/ダメージ/出現ペースのいずれかを動かす効果ではないため対象外のまま
- **猛毒Lv・狂乱Lv・脆弱Lv・回収範囲アップ・ドローン保有上限増加/射程増加/攻撃速度増加は引き続きどの軸にも未加算**: 猛毒はプレイヤーのダメージ量に依存しない(被弾した敵自身の最大HP基準の割合ダメージ)ため`offensePowerMult`には馴染まず、単体に持続ダメージを蓄積する効果(複数体を1発で処理する効果ではない)なので`crowdPowerMult`にも該当せず、防御的な効果でもないため`survivalPowerMult`にも該当しない。狂乱は(v1.36.87で攻撃力アップ・被ダメージ増加の副作用を撤廃して以降)実際にどれだけ効果を発揮するかがプレイヤーの立ち回りや運に大きく左右され、確実な出力として難易度に織り込める性質のものではないため対象外。脆弱は割合ベースで被ダメージが増加する効果のため、敵HP側の難易度カーブにフィードバックさせる意味が薄いという判断(v1.36.87、§4-3-3のドローン節と同じ考え方)で対象外。回収範囲アップは戦闘そのものに影響しないユーティリティ効果のため対象外。ドローン関連の3アップグレード(v1.36.84)は§4-3-3・§4-3-4で述べた通り、既存の迎撃・衝撃・打撃と同じ判断基準でどの軸にも加算していない
- **暴走Lv・猛毒解放Lvも未加算(v1.36.95)**: 暴走は狂乱状態の敵の行動を弱める効果で、プレイヤー自身の火力・受けるダメージ・敵の出現ペースのいずれも直接動かさないため対象外。猛毒解放は猛毒本来のダメージ計算式をそのまま前借りしているだけの一回性の追加ダメージで、発動条件も「最大スタックの敵に改めて攻撃が当たる」という偶発的なタイミングに依存するため、確実な出力として難易度に織り込める性質のものではないと判断し対象外とした(狂乱と同じ考え方)

最大HP・ハート回復量自体がもたらす`survivalPowerMult`への寄与は、超過分の**50%**のみが乗る形に緩和している(以前は超過分がそのまま100%乗っていた)。フルウェイトのままだと、最大HP/ハート回復量を盛るほど敵の接触ダメージも比例して上がってしまい、HP強化による耐久力アップが被弾ダメージの増加でかなり相殺されてしまっていたため。低速Lvは「被弾回数や被ダメージそのものを減らす」効果であり、耐久力の水増しにはならないため引き続きフルウェイトのまま。射程アップの`crowdPowerMult`への寄与も同じ理由(直接的な戦闘力の底上げではなく、間合いという間接的な要素)で超過分の50%のみを加算している。

被ダメージへの跳ね返り係数も0.7→**0.5**に緩和(v1.35.8)。オフェンス側の係数(0.25)と比べると、最大HPは`survivalPowerMult`への寄与時点で既に0.5倍に減衰されているとはいえ、旧係数0.7では実質`0.5 * 0.7 = 0.35`もの跳ね返りが乗っており、オフェンス側の0.25より依然重い罰則になっていた。0.5への変更で実質係数が`0.5 * 0.5 = 0.25`となり、最大HP軸の跳ね返り率がオフェンス側と揃う。低速Lvは`survivalPowerMult`にフルウェイトで乗るため、この変更でそちら側の跳ね返りも0.7→0.5に下がる(意図した副次効果)。

### 6-3. 敵の出現ペース(`ENEMY_SPAWN_RATE_MULT`・`SPAWN_RATE_MIN`/`MAX`、v1.28.0・v1.32.0)

プレイ中の体感で敵の出現密度がやや薄く感じられたため、出現ペースを`ENEMY_SPAWN_RATE_MULT = 1.5`倍に引き上げた(v1.28.0)。単純に敵を増やすと難易度が跳ね上がってしまうため、以下の2点でバランスを取り直している:

- **敵HPの引き下げ**: `ENEMY_TYPES`の基礎HPを、旧基準値をそのまま`ENEMY_SPAWN_RATE_MULT`で割った値に変更(grunt: 18→12、fast: 10→7、tank: 70→47、boss: 500→333)。敵が1.5倍出現する分、1体あたりのHPを約1/1.5にすることで、単位時間あたりに処理すべき合計HP量(=必要DPS)はほぼ変えず、代わりに同時に相手取る敵の「数」が増える方向にバランスをシフトしている
- **ジェムドロップ率の引き下げ**: `BASE_GEM_DROP_CHANCE`を`0.5`から`0.5 / ENEMY_SPAWN_RATE_MULT`(≈33%)に変更。敵の出現数が1.5倍になった分そのまま討伐数も1.5倍近くになるため、ドロップ率を据え置くと総XP収入が単純に1.5倍化してしまう。ドロップ率を同じ比率で割ることで、討伐数が増えても総XP収入(=レベリング速度)は据え置きになる

#### 出現イベント頻度への一本化と上下限クランプ(v1.32.0)

出現ペースは「1回の出現イベントで何体出るか(バースト数)」と「出現イベントが何回発生するか(頻度)」の2つの掛け算だったが、バースト数を敵タイプ固有の演出要素(§5)として切り離したことに伴い、難易度・クラウドビルドによるスケーリングは頻度側だけが担うようにした。

```js
rawEventsPerSec = (1 + crowdExtra * 0.5) * ENEMY_SPAWN_RATE_MULT / tierInterval
eventsPerSec    = clamp(rawEventsPerSec, SPAWN_RATE_MIN, SPAWN_RATE_MAX)
curInterval     = 1 / eventsPerSec
```

`SPAWN_RATE_MIN = 1`、`SPAWN_RATE_MAX = 8`(v1.36.27で5→8に引き上げ。秒あたりの出現イベント回数の下限・上限)。

- 導入理由: クラウド系強化なしのビルドで難易度が上がり続けるプレイを想定した場合、旧式の出現間隔の下限(`0.15秒`)と同時出現数の際限ない増加(5難易度ごとに+1体、上限なし)が組み合わさって、難易度3時点で約1.7体/秒だった出現ペースが難易度11時点では約20体/秒まで跳ね上がる(約11.7倍)という急激な増加が起きていた。上限`SPAWN_RATE_MAX`により、この暴走を頭打ちにしている
- 5→8への引き上げ(v1.36.27): 難易度HP/ダメージ係数の再引き上げと合わせ、貫通・連鎖といった出現ペース投資型のビルドがより長く「出現ペース自体が上がり続ける」恩恵を受けられるようにする調整。上限に張り付いた後の敵HPへの転嫁(`SPAWN_OVERFLOW_HP_COEFF`、§6-5)自体はそのまま残っており、上限を撤廃したわけではない
- 下限`SPAWN_RATE_MIN=1`回/秒は現状の数式では通常プレイではまず到達しない(難易度1でも約1.36回/秒)が、将来の数式調整に対する安全弁として設定してある

### 6-4. 敵の同時存在数上限(`MAX_ALIVE_ENEMIES`、v1.32.0)

画面内(正確にはゲーム全体)に同時に存在できる敵の総数に上限`MAX_ALIVE_ENEMIES = 120`を設けた。§6-3の出現ペース上限は「新しく出現する速さ」を抑えるものだが、それだけでは撃破が出現に追いつかない状況が続いた場合に敵が際限なく積み上がってしまうため、総数そのものにも別枠の安全弁を用意している。上限に達している間は新たな出現イベントがスキップされる(ボスの強制出現を除く。§5参照)。

#### 敵の「射撃系」「通常系」分類(`RANGED_ENEMY_TYPES`/`NORMAL_ENEMY_TYPES`、v1.36.81で追加)

将来的に射撃系の敵をさらに追加する可能性を見据え、非ボスの全タイプを内部的に2分類した。分類は各`ENEMY_TYPES[type].ranged`フラグ(既存 - ガンナーのAI分岐にも使われている、§5)から動的に導出しており、専用の別リストを二重管理しているわけではない:

- **射撃系(`RANGED_ENEMY_TYPES`)**: `ranged: true`を持つタイプ。現状はガンナーのみ。停止して遠距離から攻撃する
- **通常系(`NORMAL_ENEMY_TYPES`)**: それ以外の非ボスタイプ全て(grunt・fast・tank・blitz)。接近して接触ダメージを与える

将来射撃系のタイプを追加する場合、`ENEMY_TYPES`の新エントリに`ranged: true`を付けるだけで、下記の同時存在数上限・出現パターンの制約の両方に自動的に組み込まれる(このセクション・rollSpawnPattern側のどちらのコードも変更不要)。

#### 射撃系の同時存在数上限(`RANGED_MAX_ALIVE = 10`、v1.36.80で追加、v1.36.81でガンナー専用から射撃系カテゴリ全体に一般化)

`MAX_ALIVE_ENEMIES`とは独立に、射撃系カテゴリ全体(現状ガンナーのみ)に専用の上限を設けた。出現パターン(§5の「ウェーブ単位の敵出現パターン」)が射撃系偏重になった場合、遠距離から一方的に撃ってくる敵が同時に何十体も積み上がってしまい、同じ頭数の近接型の敵に比べて著しく難易度が跳ね上がる(全方位からの同時射撃を一箇所の位置取りで避けきることができないため)問題があった。

- **判定箇所**: `Game.spawnEnemy(forceType)`内、出現タイプが`this.currentSpawnPattern`から決まった直後に`ENEMY_TYPES[type].ranged`を確認する。`forceType`が指定されていない通常の出現でのみ働く(`MAX_ALIVE_ENEMIES`のボス強制出現に対する免除と同じ考え方 - 現状射撃系が`forceType`経由で出現するケースは無いが、念のため揃えてある)
- **挙動**: 現在生存している射撃系の総数(`this.enemies.filter(e => e.ranged).length` - `Enemy.ranged`はタイプ問わず`ranged`フラグ持ちなら`true`になる既存フィールド)が`RANGED_MAX_ALIVE`以上であれば、その出現試行は何も生み出さずに終わる(`return`)。出現タイマー自体は`spawnEnemy()`の呼び出し元が管理しているため、この判定で出現ペースそのものが遅くなることはなく、あくまで「射撃系という結果だけ」が抑制される - 出現パターンに通常系タイプも含まれていれば、次の出現試行で別のタイプが選ばれれば通常通り出現する
- **複数の射撃系タイプがいる場合の扱い**: 上限は射撃系カテゴリ全体で共有される合算値であり、タイプごとに個別の枠を持たない。将来2種目の射撃系タイプが追加された場合、「ガンナー7体+新タイプ3体」のような組み合わせでも合計10体で頭打ちになる(新タイプ単体で別途10体まで許されるわけではない)
- **`MAX_ALIVE_ENEMIES`との関係**: 完全に独立した別枠のチェックで、`RANGED_MAX_ALIVE(10)`は`MAX_ALIVE_ENEMIES(120)`よりずっと小さい値のため、実質的に射撃系はこちらの上限で頭打ちになる(全体の敵総数の上限に達するよりずっと早い)。通常系タイプ(grunt・fast・tank・blitz)・ボスにはこのような個別上限はなく、`MAX_ALIVE_ENEMIES`のみが効く

#### 出現パターンにおける「射撃系のみ」の禁止(v1.36.81で追加)

`Game.rollSpawnPattern()`(§5)が生成する出現パターンは、**射撃系タイプのみで構成されることがない**ように制約されている。射撃系のみのパターン(サイズ1でのガンナー単独パターンも含む)を許すと、そのウェーブ中の出現がほぼ全て射撃系の同時存在数上限(`RANGED_MAX_ALIVE`、上記)に張り付き続ける形になり、上限を設けた意味が薄れてしまう。

- **実装**: その難易度で解禁済みの通常系タイプ(`unlocked`のうち`NORMAL_ENEMY_TYPES`に含まれるもの)が1つでもあれば、まずその中から1つを無条件でパターンに加える。残りの枠は、既に選ばれたタイプを除いた解禁済み全タイプ(射撃系・通常系問わず)からランダムに埋める。grunt(`minDifficulty = 1`)は常に最初から解禁されているため、通常系タイプが1つも解禁されていない状況は実質発生しない
- パターンのサイズ自体(1・2・3・全解禁数のいずれか)を決めるロジックは変更していない - あくまで「その枠の中身」に対する制約であり、パターンが平均的に大きくなったり小さくなったりすることはない
- Playwrightで難易度10(全タイプ解禁)にて3000回パターンを生成し、射撃系のみのパターンが一度も出現しないことを確認済み。ガンナー自体は依然として約43%のパターンに登場しており、「出現しにくくなる」のではなく「単独では出現しなくなる」だけであることも確認している

### 6-5. 出現ペース上限到達後のオーバーフロー分の敵HPへの転嫁(`SPAWN_OVERFLOW_HP_COEFF`、v1.33.0)

出現ペース(§6-3)が`SPAWN_RATE_MAX`に張り付いた後は、それ以上クラウド系ビルドを強化しても出現ペースはもう上がらない。この「行き場を失ったスケーリング分」を捨てるのではなく、敵HPへ転嫁する仕組みを追加した。

```js
rawEventsPerSec    = (1 + crowdExtra * 0.5) * ENEMY_SPAWN_RATE_MULT / tierInterval   // §6-3
spawnRateOverflow  = max(0, rawEventsPerSec / SPAWN_RATE_MAX - 1)
hpMult             = tierHpMult * (1 + offenseExtra * 0.25) * (1 + spawnRateOverflow * SPAWN_OVERFLOW_HP_COEFF)
```

`SPAWN_OVERFLOW_HP_COEFF = 0.3`。`spawnRateOverflow`は「本来欲しかった出現ペースが上限の何倍だったか」を表す値で、ちょうど上限と同じかそれ以下なら0(無効果)、上限の2倍欲しかった場合は1.0(敵HP+30%)、というように連続的に増加する。

- **設計意図**: 「貫通・連鎖といったクラウド系の強化は出現ペースをスケーリングさせる」という既存の設計(§6-2)を土台にしているため、将来的に新しいクラウド系強化(貫通・連鎖以外)が追加された場合、`crowdPowerMult`経由で自動的にこの計算に組み込まれる。個別の強化を追加するたびにこの仕組み自体を手直しする必要はない
- **上限が自然に効く理由**: `tierInterval`自体に下限(`0.22秒`)があるため、`crowdExtra`が一定であれば`rawEventsPerSec`、ひいては`spawnRateOverflow`もある値で頭打ちになる(暴走しない)。ただし`crowdExtra`自体がより強力なクラウド系強化の追加で今後大きくなれば、`spawnRateOverflow`もそれに応じて大きくなる - つまり「上限到達を早める強化ほど、この経由での敵HP上昇という追加コストも大きくなる」形で自己均衡する
- 難易度D・クラウド投資の両方から計算されるため、ボス(§5)を含むすべての敵タイプに同様に適用される

### 6-6. ラッシュ(v1.34.0で追加、v1.35.2でトリガーを簡略化、v1.35.3で終了報酬を変更、v1.35.4で発生頻度を制限、v1.35.5で時間延長・敵HP強化・難易度ボーナスを追加、v1.35.6で難易度ボーナスを+2→+3に引き上げ、v1.36.75で最速発生ウェーブに下限を追加)

持続的に高い撃破率を維持できているプレイヤーへの、一時的な大量出現イベント+難易度ボーナスという報酬イベント(HP回復・ジェム全回収による報酬はv1.36.27で廃止。詳細は§6-7参照)。

#### 発生条件と流れ

1. **判定**: 専用のトラッカーは持たず、難易度の撃破率判定(§5)と全く同じ`DIFFICULTY_CHECK_INTERVAL = 50秒`のウェーブ・同じ`killRate`変数に相乗りする形で判定する。50秒が経過して難易度の上昇/維持/低下が決まるのと同時に、そのウェーブの`killRate`が`RUSH_KILL_RATE_THRESHOLD = 90%`を超えていれば(かつ現在`idle`状態であれば)ラッシュの予告状態に入る。複数ウェーブにまたがる連続条件はなく、直近1ウェーブの数値だけで判定する
2. **発生頻度の上限**: 好調が続くと毎ウェーブ発生してしまい特別感が薄れるため、`RUSH_WINDOW_COOLDOWN = 3`ウェーブ(150秒)に一度しか発生しない。`Game.rushWindowCooldown`は毎ウェーブ1ずつ減り、0以下でなければ条件を満たしていてもトリガーされない。ラッシュがトリガーされるたびに`RUSH_WINDOW_COOLDOWN`にリセットされる
3. **最速発生ウェーブの下限(`RUSH_MIN_WAVE = 6`、v1.36.75で追加)**: 上記の条件をすべて満たしても、`Game.wave >= RUSH_MIN_WAVE`でなければトリガーされない。以前は`rushWindowCooldown`の初期値が0のため理論上ウェーブ3(1回目の難易度チェック窓の終了時点)からラッシュが発生し得たが、この時点ではレベルアップがまだ1〜2回程度しか済んでおらず、`RUSH_HP_MULT`・`RUSH_BURST_MULT`で強化された敵を捌ききるだけのビルドが育っていないという、序盤バランス緩和(§6-1の`earlyGameHpMult`と同じ狙い)の一環として追加した。ウェーブ6以降でのみ条件判定が有効になるため、実際にラッシュの予告・発生状態に入れるのはウェーブ6以降(以後は引き続き`RUSH_WINDOW_COOLDOWN`の3ウェーブおきの間隔で発生し得る)
4. **予告**: 条件(撃破率+クールダウン+ウェーブ下限)を満たした瞬間、`RUSH_WARNING_DURATION = 10秒`の予告状態に入る。画面上部に赤橙色でパルスする「ラッシュまであとN秒」アラートを表示する(`#rush-alert`)
5. **発生**: 予告の10秒が経過すると、`RUSH_DURATION = 40秒`間の発生状態に入る。アラートは赤色・より速いパルスの「ラッシュ発生中！ 残りN秒」表示に切り替わる
6. **終了・報酬**: 発生状態が終わると、難易度ボーナスが適用される(詳細は後述。HP回復・ジェム全回収による報酬はv1.36.27で廃止)

この一連の状態(`idle`→`warning`→`active`→`idle`)は`Game.rushState`で管理され、一時停止中・レベルアップ画面表示中は他のタイマー同様すべて進行が止まる(`update()`が丸ごとスキップされるため)。

**タイミング設計**: `RUSH_WARNING_DURATION(10秒) + RUSH_DURATION(40秒) = 50秒`は`DIFFICULTY_CHECK_INTERVAL`と意図的に一致させてある。ラッシュは必ずウェーブの境界でトリガーされるため、予告+発生の合計もちょうど1ウェーブ分となり、**ラッシュの発生状態は必ず次の難易度チェックのタイミングで終了する**。これを利用し、発生状態の終了処理(HP回復・ジェム回収)と難易度ボーナス判定(後述)は、`Game.rushTimer`という独立タイマーではなく、難易度チェック側の`this.rushState === 'active'`という状態確認だけで検出している。2つの独立したタイマーの一致に賭ける実装ではないため、フレームレート由来のズレでこの一致が崩れる心配がない。`rushTimer`自体は発生状態中も毎フレーム減算され続けるが、あくまでHUDの「残りN秒」表示用の演出目的であり、状態遷移の判定には使われない

#### 発生中の効果

| 効果 | 内容 |
|---|---|
| バースト数2倍 | `spawnEnemy()`が生成する敵の数が`ENEMY_TYPES[type].burst`(§5)の2倍(`RUSH_BURST_MULT = 2`)になる |
| 敵の移動速度1.2倍 | 存在中の敵・新規出現する敵の両方に`RUSH_SPEED_MULT = 1.2`が掛かる(低速状態の減速とも重ね掛けされる) |
| 敵HP1.5倍 | ラッシュ中に出現する敵の`hpMult`に`RUSH_HP_MULT = 1.5`が追加で掛かる(§6-2のtierHpMult等の通常計算はそのまま維持した上での上乗せ) |

**出現ペース上限をあえて超える設計**: バースト数(1回の出現イベントで何体出るか)と出現ペース(イベントが秒間何回発生するか、§6-3)は既に分離されている(v1.32.0)ため、バースト数だけを一時的に引き上げることで、`SPAWN_RATE_MAX`(§6-3)による出現ペース自体の上限は変更せずに、実質的な敵の総流入量だけを一時的に押し上げられる。**`MAX_ALIVE_ENEMIES`(§6-4、同時存在数の上限)はラッシュ中も変更なく有効**であり、際限なく敵が積み上がることはない。

#### 終了時の難易度ボーナス

- **難易度ボーナス**: ラッシュが終了するそのウェーブの撃破率も`RUSH_KILL_RATE_THRESHOLD(90%)`以上だった場合、難易度スケーリング表(§5)のうち最も高いティア`RUSH_DIFFICULTY_BONUS = 3`(+3)が適用される(90%以上でもラッシュ以外の通常ウェーブなら+2、70%台なら+1)。バースト2倍・敵HP1.5倍・敵速度1.2倍という強化された敵をラッシュ終了まで90%以上のペースで処理し続けたプレイヤーへの、その分大きい難易度上昇という追加リターン
- 「行き場を失ったスケーリング分」を敵HPへ転嫁する§6-5と同様、"クラウド系ビルドで出現ペース上限に張り付いても報われる"設計思想の延長線上にある。ラッシュは撃破率を高く維持できる(=十分に強い)プレイヤーへの明確な「逆転ではなくボーナス」の一手として機能する
- **報酬の変遷**: 当初は確定レベルアップ2回→効果が強すぎたため(v1.35.3)HP回復+ジェム回収に変更→v1.36.27でそのHP回復・ジェム回収も廃止し、難易度ボーナスのみになった(廃止理由は§6-7参照)

### 6-7. ウェーブごとのHP回復・ジェム全回収の廃止とハート型HP回復アイテム(v1.36.27)

`WINDOW_HEAL_FRAC`(通常ウェーブ終了時の20%回復)・`RUSH_HEAL_FRAC`(ラッシュ終了時の追加25%回復)・ラッシュ終了時のジェム全回収は、いずれもv1.36.27で廃止した。

- **廃止理由**: これらは「50秒ごとに自動でまとまった回復が発生する」仕組みだったため、プレイヤーの行動や状況に関係なく一律に安全マージンが与えられてしまい、「徐々に追い詰められていく緊張感」を薄める方向に働いていた。ジェム全回収も同様に、通常の接触回収という行動を伴わずに確定でXPが得られる仕組みだった
- 代わりに導入したのが下記のハート型HP回復アイテム。「自動で回復する」から「探しに行けば回復できる」への転換で、回復を得るのに多少なりともプレイヤー側の行動(接触しに行く/経路をそちらに寄せる)を要求する設計に変えている

#### ハート(`Heart`、`HEART_*`定数)

低確率で自然発生するHP回復アイテム。

| 項目 | 値 |
|---|---|
| 出現判定間隔 (`HEART_SPAWN_CHECK_INTERVAL`) | 1秒に1回抽選 |
| 出現確率 (`HEART_SPAWN_CHANCE`) | 抽選1回あたり4%(v1.36.28で2%から倍増、期待値で約25秒に1回) |
| 同時存在数上限 (`HEART_MAX_COUNT`) | 5個(v1.36.29で1個から引き上げ。画面上に既に5個あれば抽選自体を行わない) |
| 出現位置 | プレイヤーから距離200〜900px(`HEART_SPAWN_MIN_DIST`〜`HEART_SPAWN_MAX_DIST`、v1.36.28で上限を400から1200に拡大→v1.36.69で900に縮小)のランダムな方向 |
| 回収方法 | プレイヤーの当たり判定が直接触れた場合のみ(ジェムのような磁力による引き寄せ(pickupRadius)は働かない) |
| 回復量 (`Player.heartHealFrac`初期値、`HEART_HEAL_FRAC`) | `Player.maxHp`の20%(v1.36.29で20%に引き上げ→v1.36.32で10%に戻し→v1.36.69で再び20%に倍増、`maxHp`でクランプ。アップグレードで最大50%まで伸ばせる、詳細は下記) |
| 寿命 | なし(回収されるまでその場に留まり続ける) |

- ジェムと違い磁力も寿命もないため、「たまたま近くを通ったら自動で入る」ものではなく、見つけたら意図的にそちらへ寄る必要がある小さな寄り道として機能する
- **出現範囲の拡大(v1.36.28)**: 旧上限(400px)は典型的な画面サイズの範囲内に収まってしまい、出現した瞬間ほぼ必ず画面内に見えていた。上限を1200pxまで拡大したことで、画面外に出現するケースも多くなり、「今どこかにハートが出ているはず」と敵をかいくぐりながら探しに行くという行動選択が実質的に生まれた。移動速度アップグレードが被弾回避以外にも活きる場面を増やす狙いも兼ねている
- 出現確率も2%→4%に倍増(v1.36.28)。探しに行く価値のある機会自体を増やす調整
- **同時存在数上限の引き上げ(v1.36.29)**: 出現範囲が画面外まで広がった(v1.36.28)結果、同時存在数が1個のままだと遠くに出た1個が次の抽選をブロックし続け、より近くに出たかもしれない別の1個の可能性を潰してしまっていた。上限を5個に引き上げることで、遠い1個を無理に追いかけなくても、近くに出た別の1個で妥協するという選択肢も生まれる
- **回復量の変遷(v1.36.29→v1.36.32→v1.36.69)**: v1.36.29では見つけにくさを補う目的で基礎回復量を10%→20%に引き上げたが、ハートコンパス(v1.36.31)の追加で見つけやすさそのものが直接改善されたため、v1.36.32で基礎回復量は10%に戻した。その後、出現範囲を狭める調整(下記)と合わせたバランス調整として、v1.36.69で基礎回復量を再び20%に倍増した(この時の理由はv1.36.29と異なり見つけにくさの補填ではなく、単純にハート1個の価値を上げるための調整)
- **出現範囲の縮小(v1.36.69)**: `HEART_SPAWN_MAX_DIST`を1200pxから900pxに縮小。v1.36.28の拡大がもたらした「画面外まで探しに行く」感覚自体は維持しつつ、出現範囲が広すぎた分をやや狭めるバランス調整。回復量の倍増(上記)とセットで行われた

#### ハート回復量増加(アップグレード、`id: 'heartheal'`、v1.36.32で追加)

ハート1個あたりの回復量(`Player.heartHealFrac`)を伸ばすレベルアップ強化。射程アップ・移動速度アップ・回収範囲アップと同じ「×1.1、上限までクランプ」の形で、ピックごとに+10%(相対)。上限は`STAT_LIMITS.maxHeartHealFrac = 0.5`(v1.36.69で0.25から倍増)で、基礎値20%から最大50%まで伸ばせる。基礎回復量の倍増(v1.36.69)に合わせて上限も同じ2.5倍の比率を保つよう倍増させたもので、上限だけ据え置くと基礎値がほぼそのまま上限に近くなり、このアップグレード自体の伸びしろがほとんど残らなくなってしまうための調整。上限に達すると選択肢から除外される(他の上限付きアップグレードと同じ`available`ゲート)。

#### ハートコンパス(`#heart-compass`、v1.36.30で追加、v1.36.31でUIを刷新、v1.36.33で矢印形状を調整)

出現範囲が画面外まで拡大した(v1.36.28)ことで、実際には「そもそもどちらの方向を探せばいいか分からない」という難点が生じた。これを軽減するため、最も近いハートへの方向を示すUIを追加した。

- 毎フレーム、存在するハートの中からプレイヤーに最も近い1個を選び、そのハートが現在の画面内(カメラのズーム`viewScale()`を考慮したスクリーン座標)に収まっているかを判定する
- **画面内に収まっている場合はコンパス自体を非表示にする**。既に見えているハートに対して方向表示は不要かつ視覚的なノイズになるため
- ハートが1個も存在しない場合も非表示
- ジェムや敵と違い、この判定に使う「画面内かどうか」はカメラのズーム倍率(射程アップグレードによる`rangeMult`経由)を考慮した実際の表示範囲で行うため、ズームアウトしているほど「画面内」と判定される範囲も広がる(ズームアウトで実際に見えている範囲が広がることと整合)
- **UI形状(v1.36.31で変更)**: 初期実装は画面右下に上下左右の矢印4個を並べたUIだったが、操作可能なボタンに見えてしまう懸念があったため、HP/XPバー直下に置いた円形のコンパス(内側で回転する矢印)に変更した。プレイヤーからハートへの方向ベクトル(ワールド座標のdx, dy)から角度を`atan2(dx, -dy)`で算出し、矢印を`rotate()`で回転させて指し示す(0度=真上、時計回りに90度で右、180度で下、270度で左)。離散4方向ではなく連続的な角度で示すため、斜め方向でも自然な向きを表現できる。回転自体にCSSトランジションは付けていない(付けると-179度→+179度のような角度またぎで矢印が丸ごと1周回転してしまうため)
- **矢印形状の調整(v1.36.33)**: 当初は`▲`のテキストグリフを矢印として使っていたが、小サイズだとほぼ正三角形に見えてしまい、どの方向を指しているか判別しづらいという問題があった。SVGで描いた細長い二等辺三角形(底辺8px・高さ24px)に置き換え、どちらが「先端」かを一目で判別できるようにした

| 画面/要素 | 内容 |
|---|---|
| スタート画面 | タイトル、操作説明、「ゲーム開始」ボタン、右下にバージョン表記 |
| スタート設定画面 | 「キャラクター」「武器」を個別に1つずつ選んでハイライトし、両方選ぶと「決定」ボタンが有効化されゲーム開始(詳細は§7-1) |
| HUD | HPバー、XPバー、タイマー、プレイヤーLv.、ウェーブ/残り秒数/難易度、撃破率、撃破数 |
| レベルアップ画面 | 強化3択 + スキップの計4択カード。閉じると即座にゲームが再開し、短時間の無敵が付与される(詳細は§4-6) |
| ゲームオーバー画面 | 生存時間・撃破数・到達レベルを表示、「もう一度」ボタン |
| 一時停止ボタン | 画面上部中央、`II`。押すと一時停止画面(§7-5)を開く(一時停止画面が表示されている間、このボタン自体は隠れる) |
| 一時停止画面 | 現在のプレイヤーステータス表示、「再開」ボタン、「タイトルに戻る」ボタン(詳細は§7-5) |
| 特殊能力インジケーター | 画面下部中央。準備完了(水色パルス)/バフ発動中(黄色パルス)/クールタイム中(通常表示)の3状態(詳細は§7-2) |
| ラッシュアラート | 画面上部中央。予告中(橙色パルス、「ラッシュまであとN秒」)/発生中(赤色・高速パルス、「ラッシュ発生中！ 残りN秒」)の2状態、それ以外は非表示(詳細は§6-6) |
| 脅威ビネット | 画面端が赤くパルスする警告演出。プレイヤーを一撃で倒しうる敵が近くにいる間だけ表示(詳細は§7-6) |
| ハートコンパス | HP/XPバー直下の円形ゲージ。最も近いハートが画面外にある間だけ表示され、内側の矢印が回転してその方向を指し示す(詳細は§6-7) |

### 7-1. スタート設定画面(`CHARACTERS` / `WEAPONS`)

将来的な武器種の多様化に備えて、ゲーム開始前に「キャラクター」と「武器」を個別に選ぶ画面を用意している。

- フロー: スタート画面の「ゲーム開始」→ スタート設定画面 → キャラクターカード・武器カードをそれぞれタップして選択(ハイライト表示)→ 両方選択済みで「決定」ボタンが有効化 →「決定」でゲーム開始
- ゲームオーバー画面の「もう一度」も直接リスタートするのではなく、スタート設定画面に戻る(選び直せる。選択状態は毎回リセットされる)
- 役割分担:
  - **キャラクター**(`CHARACTERS`): HP・移動速度・リジェネ・XP回収範囲などの生存/ユーティリティ系ステータスで差別化。ダブルタップで発動する特殊能力(§7-2)・常時発動のパッシブ能力(§7-3)もここに紐づく
  - **武器**(`WEAPONS`): ダメージ・発射速度・貫通などの攻撃系ステータスで差別化。武器自体が持つ「武器固有効果」(§7-4)もここに紐づく
- 現時点では `CHARACTERS` に「スタンダード」「ヒーラー」「スピード」の3エントリ、`WEAPONS` に「スタンダード」「パルスウェーブ」「チャージビーム」「ラピッドファイア」「ランドマイン」「マルチミサイル」「ソード＆シールド」の7エントリが存在。武器固有効果は別途§7-4、各武器の攻撃方式自体は§7-4-1(パルスウェーブ)・§7-4-2(チャージビーム)・§7-4-3(ラピッドファイア)・§7-4-4(ランドマイン)・§7-4-5(マルチミサイル)・§7-4-6(ソード＆シールド)を参照。いずれも`apply`はステータス変更なし(`apply: (p) => {}`)だが、マルチミサイルのみ例外で、取得した瞬間に「爆発」ランク1を内蔵付与する(`apply: (p) => { p.explosionLevel = Math.max(p.explosionLevel, 1); }`、詳細は§7-4-5)。各キャラクターの詳細は§7-2(特殊能力)・§7-3(パッシブ能力)を参照
- 各エントリは `{ id, name, desc, apply(player) }` の形で定義され、`apply` が `Player` インスタンスに対して開始時のステータス変更を行う。`Player` のコンストラクタは `(character, weapon)` の順で受け取り、`character.apply()` → `weapon.apply()` → 武器固有効果の初回適用 → 特殊能力の初期化 → `passive.apply()` の順で適用する。新しい選択肢はそれぞれの配列にエントリを追加するだけで出現する
- キャラクターカードには `desc` に加えて、`passive` が設定されている場合は「パッシブ「[名前]」: [説明]」の行が追加で表示される(特殊能力は本文中では表示されず、ゲーム中のインジケーター(§7-2)でのみ確認できる)。武器カードには同様に、`innateEffect` が設定されていれば「武器固有効果「[名前]」: [説明]」の行が表示される

### 7-2. ダブルタップ特殊能力

「ピンチで倒せない敵に囲まれ、ジェムも拾えずレベルアップできず、そのままゲームオーバー」という詰み展開への逆転手段として、キャラクターごとにダブルタップで発動する特殊能力を持たせられる仕組み。

- **発動方法**: 画面(canvas)を素早く2回タップ(`DOUBLE_TAP_MAX_INTERVAL_MS=350ms` 以内、`DOUBLE_TAP_MAX_DIST=40px` 以内の位置)。2回目のタップはそのまま通常の移動ドラッグとしても機能する
- キャラクターの `special` フィールドに `{ name, desc, cooldown, activate(player, game) }` の形で定義。`Player` インスタンスは自身の `special`(なければ`null`)・`specialCooldownRemaining`・`specialBuffTimer` を保持する
- **ゲーム開始時はクールタイムのカウントダウンから始まる**(いきなり使える状態ではない)
- **クールタイムはバフ時間が終わってから始まる**(バフ時間とクールタイムは同時進行ではなく直列。1サイクル = バフ時間 + クールタイム)
- クールタイム中・バフ発動中、およびレベルアップ画面/一時停止中/ゲームオーバー後などプレイ中でないタイミングでの発動は無視される
- 画面下部中央に常時インジケーターを表示:
  - 準備完了時: 水色でパルス表示、「[能力名] 準備完了(ダブルタップ)」
  - バフ発動中: 黄色でパルス表示、「[能力名]バフ有効中 n秒」
  - クールタイム中: 通常表示、「[能力名] n秒」
- **バフ中の効果はキャラクターごとに宣言的に指定する**: `special` オブジェクトに以下のオプションフィールドを持たせると、`specialBuffTimer > 0` の間、対応するシステム側の汎用コードが自動的にそれを参照する(どのキャラクターがどの効果を持つかは各`special`定義側の責務で、汎用コード側はハードコードしない)
  - `buffSpeedMult`: `Player.speed`ゲッターに乗算される移動速度倍率
  - `buffDamageMult`: `fireWeapon()`で弾を生成する瞬間の`p.damage`に乗算されるダメージ倍率(発射時に弾へ焼き込まれ、命中時に再評価されるわけではない)
  - `buffPickupRadiusMult`: ジェム吸引判定で使う実効回収範囲(`p.pickupRadius`に乗算。`Infinity`を指定すれば事実上无制限になる)
  - `buffXpMult`: バフ中に回収したジェムのXP量に乗算される倍率
  - `buffDamageTakenMult`: 敵の接触ダメージ判定(`effDmg`)に乗算される被ダメージ倍率(v1.36.34で追加。`0.3`なら被ダメージ70%カット。脅威ビネットの一撃死判定も同じ`effDmg`を参照するため、この倍率も自動的に反映される)

#### 現在の実装: 各キャラクターの特殊能力

| キャラクター | 能力名 | 効果 | クールタイム |
|---|---|---|---|
| スタンダード | エマージェンシーボム | 発動時に存在する敵をすべて強制撃破(通常の撃破と同様にXPジェムをドロップし、撃破数・撃破率にもカウントされる)。加えて10秒間、`buffPickupRadiusMult=Infinity`(ジェム吸引範囲が画面全域)・`buffXpMult=1.5`(獲得XP1.5倍) | 120秒(バフ終了後にカウント開始。実質1サイクル130秒) |
| ヒーラー | リカバリーダッシュ | 最大HPの50%を回復。加えて20秒間、`buffSpeedMult=1.5`(移動速度1.5倍)・`buffDamageTakenMult=0.3`(被ダメージ70%カット、v1.36.34で追加) | 60秒(実質1サイクル80秒) |
| スピード | オーバードライブ | 10秒間、`buffDamageMult=1.5`(攻撃力1.5倍)・`buffPickupRadiusMult=3`(ジェム回収範囲3倍) | 10秒(実質1サイクル20秒) |

**スタンダード**: 強制撃破だけでは直後にまた「倒せない敵」に囲まれて終わってしまうため、敵を一掃した直後にジェムを根こそぎ回収してレベルアップまで一気に進められるようにし、明確な巻き返しの一手として機能させている。獲得XP1.5倍のボーナスにより、「能力を頻発するより、ギリギリまでジェムを貯めて一気に回収する方が得」というインセンティブが働く。

**ヒーラー**: HPが減ってきたタイミングで使う典型的なサステイン(継続戦闘力維持)ツール。移動速度バフにより、回復直後に素早く移動してジェム回収がしやすくなる。スタンダードのボムに比べてクールタイムが短く、1回あたりの効果も控えめな「頻用できる保険」の位置づけ。被ダメージ70%カット(v1.36.34で追加)により、移動速度バフだけでは「回復してもすぐ被弾で相殺される」問題があったのを解消し、効果時間中は回復した分を維持しやすい真の耐久バフになった。

**スピード**: バフ時間とクールタイムが共に10秒(1サイクル20秒)と非常に短く設計されており、うまく回せばプレイ時間の半分近くをバフ状態で過ごせる。積極的にダブルタップし続けることが高DPS維持の前提になっており、操作頻度の高さがそのままキャラクター性能に直結する上級者向け設計。

### 7-3. パッシブ能力

ダブルタップで能動的に発動する特殊能力(§7-2)とは別に、常時効果が働くキャラクター固有のパッシブ能力を持たせられる仕組み。

- キャラクターの `passive` フィールドに `{ name, desc, apply(player) }` の形で定義(特殊能力の `special` と同じ形の定義パターン)。`Player` インスタンスは自身の `passive`(なければ`null`)を保持する
- `Player` コンストラクタで `character.apply()` → `weapon.apply()` → 武器固有効果(§7-4)の初回適用 → 特殊能力の初期化 の後、最後に `passive.apply(player)` が呼ばれ、開始時に一度だけステータスへ反映される(活性化操作は不要、常時有効)
- 特殊能力とは異なりクールタイムや発動条件を持たないため、専用のHUDインジケーターは無く、キャラクター選択画面のカード上の説明文でのみ内容を確認できる(§7-1)
- `apply(player)` によるステータス上書き型のパッシブに加え、`onContactDamage(player, enemy, game)` という任意のフックも定義できる。設定されていれば、敵の接触ダメージ処理(§5)の直後に毎回呼ばれる(プレイヤーが無敵時間中で実際のダメージが0でも、接触自体は発生しているため呼ばれる)。旧タンクの反撃パッシブ(`REFLECT_PASSIVE`、下記)はこのフックで実装されていた

#### 現在の実装: 各キャラクターのパッシブ能力

| キャラクター | パッシブ名 | 効果 |
|---|---|---|
| スタンダード | 倹約家 | レベルアップの「スキップ」(§4-5)選択時に払い戻されるXPが、基準の30%から**60%**に上昇する |
| ヒーラー | 自然回復 | HPが時間経過で徐々に回復する(`p.regen += 1`、1秒あたり1回復) |
| スピード | 高速連射 | 武器の攻撃間隔が半分(攻撃速度2倍)になる代わりに、攻撃力が半分になる(合計DPSは基準武器と変化なし) |

**スタンダード**: `Player.skipRefundPct`(基準値`SKIP_REFUND_PCT_BASE = 0.3`)をこのパッシブの`apply`で`0.6`に上書き。スキップの`desc`・`apply`はどちらも固定値ではなくこの`skipRefundPct`を参照するため、パッシブの有無で表示・挙動の双方が自動的に変わる。「弱いアップグレードを渋々取るより、思い切ってスキップしてXPだけ稼ぐ」という選択が、通常の30%よりずっと現実的な選択肢になることを狙ったパッシブ。

**ヒーラー(v1.36.65、旧タンクから改名)**: 従来のタンクは移動速度に`speedMult *= 0.7`のペナルティを持っていたが、fast・ガンナーのような足の速い/遠距離型の敵に対してこの速度差が回避不能な不利に直結し、「移動速度アップグレードを取らないと一部ウェーブを突破できない」というバランス崩壊を起こしていた。この移動速度ペナルティを完全に撤廃(スタンダードと同じ`speedMult = 1`)した上で、最大HPを180→130に抑え、攻撃力もスタンダード比70%(`round(10 × 0.7) = 7`)に下げてステータス面のトレードオフを再構成し、代わりに常時HPが回復する`Player.regen`(既存のフィールドで、以前は削除済みの「リジェネ」アップグレードが同じ+1/秒の値を付与していた)を使ったパッシブに差し替えた。反撃パッシブ自体は`REFLECT_PASSIVE`という独立した定数として実装をそのまま残してあり(§7-3末尾の実装メモ参照)、現在はどのキャラクターにも割り当てられていないが将来の再利用に備えている。

**スピード**: `atkCooldown`を`× 0.5`(下限`STAT_LIMITS.minAtkCooldown`)、`damage`を`× 0.5`(下限`STAT_LIMITS.minDamage`)した状態が初期値になる。基準武器とベースDPSは変わらないが、単位時間あたりのヒット回数が倍増するため、連鎖(命中ごとに発動確率判定がある)など「命中回数」に依存する弾丸効果と特に相性が良くなる。

**実装メモ(`REFLECT_PASSIVE`)**: 旧タンクの反撃パッシブ(被ダメージ時に攻撃してきた敵へ`round(攻撃力 × 30% + 最大HP × 8%)`の反射ダメージを与える、`REFLECT_DMG_PCT_OF_ATTACK=0.3`/`REFLECT_DMG_PCT_OF_MAXHP=0.08`)は、ヒーラーへの改名に伴いどのキャラクターの`passive`からも参照されなくなったが、実装自体(定数・`onContactDamage`フック本体)は`main.js`内に`REFLECT_PASSIVE`という独立した定数として削除せず残してある。

### 7-4. 武器固有効果(v1.24.0で追加)

弾丸効果(§4-3)のようにレベルアップ画面で選ぶアップグレードとは別に、**武器そのものが最初から持つ固有の効果**を、レベルアップに応じて自動でランクアップさせる仕組み。マルチショットがほぼすべてのランで実質必須級のアップグレードになっていた問題を受けて、「選択の余地がないなら、もはや選択肢ではなく武器の一部として扱う」という方針で新設した。

- 武器(`WEAPONS`)の `innateEffect` フィールドに `{ name, desc, applyRank(player, rank) }` の形で定義。武器を持たない・`innateEffect`が未設定の武器は対象外
- ランクは**プレイヤーレベルから直接計算**される(選択や個別のレベルアップ判定は無い): `rank = min(WEAPON_INNATE_MAX_RANK, floor((level - 1) / WEAPON_INNATE_LEVELS_PER_RANK) + 1)`。現在の定数は `WEAPON_INNATE_MAX_RANK = 5`・`WEAPON_INNATE_LEVELS_PER_RANK = 3`(Lv.1〜3=ランク1、Lv.4〜6=ランク2、…Lv.13以降=ランク5で頭打ち)
- `Player`はコンストラクタで武器への参照(`this.weapon`)を保持し、ゲーム開始時(レベル1相当)と、その後のレベルアップのたびに`applyRank(player, rank)`を呼び直して現在のランクを反映する。`applyRank`は絶対値を設定する実装が前提(前のランクからの差分ではない)なので、ランクが変わっていない呼び出しでも安全に呼べる
- 弾丸効果の優先枠(§4-4)やレベルアップの3択そのものには一切関与しない。純粋にプレイヤーレベルに紐づく自動強化

#### 現在の実装: スタンダード武器の固有効果「マルチショット」

| 項目 | 内容 |
|---|---|
| 効果 | 同時発射数(`projCount`)がそのままランクの値になる(ランク1=1発 → ランク5=5発) |
| ランクアップ条件 | 自機レベルアップ3ごとに+1ランク、最大ランク5(Lv.13以降で頭打ち) |

以前は弾丸効果プールの1エントリとしてプレイヤーが選択していたが、ビルドを問わずほぼ確実に取得される「実質必須」アップグレードになっていたため、この仕組みに移行した。これに伴い、マルチショットのランク(≒`projCount`)は敵出現数のスケーリング(`crowdPowerMult`、§6-2)からも除外している。

#### 現在の実装2: パルスウェーブ武器の固有効果「波動拡大」(v1.36.15で追加、v1.36.17で武器名を「薙刀」から変更)

| 項目 | 内容 |
|---|---|
| 効果 | 衝撃波の半幅(`Player.wideHalfWidth`)がランクに応じて広がる(`16 + 10*(ランク-1)`px。ランク1=16px → ランク5=56px) |
| ランクアップ条件 | マルチショットと同じ`WEAPON_INNATE_LEVELS_PER_RANK`/`WEAPON_INNATE_MAX_RANK`を共有(自機レベルアップ3ごとに+1ランク、最大ランク5、Lv.13以降で頭打ち) |

パルスウェーブは`projCount`を一切持たない(常に1)代わりに、この幅の広がりが実質的な「範囲拡大」としてランクアップの旨みを担う。詳細な攻撃方式は次項(§7-4-1)を参照。

#### 現在の実装3: チャージビーム武器の固有効果「マルチビーム」(v1.36.18で追加、v1.36.22で仕様変更、v1.36.83で再度仕様変更)

| 項目 | 内容 |
|---|---|
| 効果 | 最大同時発射ビーム数(`Player.chargeBeamCount`)がそのままランクの値になる(ランク1=1本 → ランク5=5本)。標準武器のマルチショット(`projCount`)と全く同じ考え方。v1.36.85以降はこの値が「チャージ段階の上限」も兼ねる(§7-4-2) - フルチャージ(この段階数ぶんホールド)して初めてランク分の本数が出揃い、途中で離すとその時点の段階数ぶんの本数しか発射されない |
| ランクアップ条件 | 他の武器と同じ`WEAPON_INNATE_LEVELS_PER_RANK`/`WEAPON_INNATE_MAX_RANK`を共有(自機レベルアップ3ごとに+1ランク、最大ランク5、Lv.13以降で頭打ち) |

v1.36.22時点ではランクが「最大チャージ段階数(`Player.chargeMaxStages`)」を伸ばし、そのぶん長く溜めるほどビームの幅と威力の両方が上がる、という仕組みだった。しかしこれは「手数を出せず、常に一方向のみにしか攻撃できない」武器のままで、本作のような四方八方から迫る敵を捌くゲーム性とは相性が悪いという判断から、v1.36.83で標準武器と同じマルチショット方式に作り直した。ランクは「1本のビームをどれだけ強くするか」ではなく「何本同時に撃てるか」を伸ばす役割に変わり、各ビームの威力・幅は常に固定値になった(詳細は次々項§7-4-2)。v1.36.83では発射本数はランク分が常に一斉発射だったが、高ランクでの見た目が派手すぎるとのフィードバックを受け、v1.36.85で「チャージ段階に応じて本数が1本ずつ解放される」形に調整した。

#### 現在の実装4: ラピッドファイア武器の固有効果「同時射撃数アップ」(v1.36.45で追加、v1.36.46で弾の並び方を変更)

| 項目 | 内容 |
|---|---|
| 効果 | 同時発射数(`Player.rapidfireBarrels`)がそのままランクの値になる(ランク1=1発 → ランク5=5発)。全弾は進行方向に対して垂直な向きに横一列に等間隔で並んで発射される(ランク1="-"、ランク2="="、ランク3="≡"のようなイメージ) |
| ランクアップ条件 | 他の武器と同じ`WEAPON_INNATE_LEVELS_PER_RANK`/`WEAPON_INNATE_MAX_RANK`を共有(自機レベルアップ3ごとに+1ランク、最大ランク5、Lv.13以降で頭打ち) |

マルチショットの`projCount`と役割は似ているが、狙いが異なる。マルチショットは複数の異なる敵(異なる方向)へ個別に飛ばして「同時に狙える対象を増やす」効果である一方、ラピッドファイアの同時発射数は全弾が**全く同じ角度・同じ速度**で進行方向に対して横一列に並んで飛ぶため、進行方向の先にある短い横幅の範囲をまとめて薙ぎ払う効果になる。初期実装(v1.36.45)では進行方向の同じ1本の弾道上に前後にずらして並べる方式だったが、v1.36.46で進行方向と垂直に並べる方式に変更した(下記)。詳細な攻撃方式は次項(§7-4-3)を参照。

### 7-4-1. パルスウェーブ武器の攻撃方式(近接範囲・前後同時攻撃、v1.36.15で追加、v1.36.17でバックショットを追加)

パルスウェーブ(`WEAPONS`の`id: 'wide'`、初期実装時の武器名は「薙刀」)は他の武器と異なり、飛翔する`Projectile`を撃つのではなく、`Game.fireWideSweep()`による**瞬間的な衝撃波**で攻撃する。マルチショット(遠距離から複数体を個別に狙う「広く浅く」)とは対照的な、「近距離だが範囲内は漏れなく・威力も高い」プレイスタイルを提供する武器という位置づけ。

- **射程**: `WIDE_ATTACK_RANGE = DRONE_RADIUS_BASE + 30 = 120px`(v1.36.68で`DRONE_RANGE_BASE`(旧`INTERCEPT_RADIUS`)基準から`DRONE_RADIUS_BASE`(旧`AUX_WEAPON_RADIUS`、v1.36.84で`DRONE_RADIUS`から改名)基準に変更)。v1.36.67でドローンのオーラ半径(`DRONE_RADIUS_BASE`)が90pxに拡大された結果、パルスウェーブの射程(当時90pxのまま据え置き)と偶然同値になっていたが、「パルスウェーブはドローンよりわずかに長い近接専用武器」という本来の設計意図(§4-3-3)からは外れてしまっていた。そのため基準を`DRONE_RADIUS_BASE`に付け替えて120pxに拡大し、以後はドローンのオーラ半径が変わっても常にその30px上を維持する形にした。v1.36.84でドローンの索敵範囲自体がランクで伸びるようになった後も、この定数は静的な`DRONE_RADIUS_BASE`だけを参照し続けており、ドローン射程増加アップグレードの効果はパルスウェーブへ一切波及しない(§4-3-3)
- **判定**: プレイヤーから射程内にいる最も近い敵を狙って衝撃波の向きを決め、その向きを基準にした矩形状の当たり判定(前方`WIDE_ATTACK_RANGE`px・左右`Player.wideHalfWidth`px、いずれも対象自身の半径ぶんの余裕を追加)に入る敵を全て同時にヒットさせる。射程内に敵が1体もいなければ発射自体行わない(通常武器が射程外の敵を無視するのと同じ扱い)
- **バックショット(v1.36.17で追加)**: 同じ判定・威力の衝撃波が、狙った方向とちょうど反対方向にも同時に発生する。同一の当たり判定を前後2方向ぶん判定するだけで、射程・威力・弾丸効果の適用ロジックはすべて共通。射程を伸ばして解決するのではなく前後2方向に広げる形を選んだのは、「近接必須のハイリスク武器」という個性を保ったまま、単一方向ゆえの死角(背後から迫る敵に対して無防備)だけを解消するため
- **威力**: 通常のダメージ計算(`Player.damage` × 特殊バフ倍率)に`WIDE_DAMAGE_MULT = 2.0`倍を掛けた値を、ヒットした敵全員(前方・後方とも)に与える
- **弾丸効果との連携**: 貫通(`pierce`)以外の弾丸効果(爆発・連鎖・低速・猛毒・狂乱・爆弾化・衰弱・脆弱)は通常の弾丸と全く同じロジックで適用される。ヒット処理そのもの(ダメージ・状態異常付与・爆発の飛び火・連鎖の追撃)を`Game.resolveProjectileHit(proj, target)`という共通メソッドに切り出し、通常のプロジェクタイル衝突判定とパルスウェーブの衝撃波の両方から同じロジックを呼ぶことで、今後どちらかの経路だけ弾丸効果の反映が漏れる事態を防いでいる(実際のProjectileインスタンスではなく、必要なフィールドだけを持つ一時オブジェクトを渡す形で共通化)。貫通は「1回のヒットで範囲内の敵を同時に処理する」パルスウェーブの性質上そもそも意味を持たないため、そもそもレベルアップ画面の候補にすら出現しない(§4-3)
- **視覚効果(v1.36.16で変更)**: 攻撃が命中すると、衝撃波の向き・射程・幅から角度を算出した同心円状の弧(クレセント)を3本、プレイヤーを中心に短時間(0.15秒)光らせて消す(`SweepEffect`)。バックショット追加後は前後それぞれの向きに1組ずつ、計2組の弧が表示される。初期実装では当たり判定と全く同じ矩形をそのまま描画していたが、実際の見た目が単調だったため、当たり判定の形そのものではなく方向感・広がりを演出する弧に変更した

### 7-4-2. チャージビーム武器の攻撃方式(溜め撃ち・無限貫通・複数ビーム同時発射、v1.36.18で追加、v1.36.19で最低チャージ時間を追加、v1.36.20でチャージ時間の効果をダメージから幅に変更、v1.36.22で段階制に変更、v1.36.49でダメージのチャージ段階スケーリングを幅と併用する形で再追加、v1.36.77でラピッドファイア方式のターゲティングに変更→実際にプレイした結果あまり改善しなかったためv1.36.78で撤回、代わりにターゲットロック方式を追加、v1.36.83でチャージ段階制を廃止しマルチショット方式に全面変更、v1.36.85でビーム幅を縮小し段階制をビーム本数の解放という形で復活)

チャージビーム(`WEAPONS`の`id: 'charge'`)は他のどの武器とも異なり、`atkTimer`/`atkCooldown`による毎ショットのクールダウンでは一切駆動されない。「ホールドで溜め、離すと発射」という溜め撃ち武器で、他の武器が持つ「連射」の代わりに「一撃のロマン」(高威力・全体貫通の一撃)を提供する。

**v1.36.83での全面刷新**: v1.36.82以前は、長く溜めるほど「段階」が進み、1本のビームの幅と威力の両方がその段階に応じて上がる仕組みだった。しかしこの仕組みだと、この武器は最後までどれだけ育てても「手数が出せず、常に一方向のみにしか攻撃できない」武器のままで、四方八方から迫る敵を捌く本作のゲーム性とは相性が悪いという判断で刷新した。段階制そのものを廃止し、代わりに標準武器と同じマルチショット方式(`Player.chargeBeamCount`、§7-4の「マルチビーム」)を採用: 1本ごとの幅・威力は常に固定値のまま、ランクアップに応じて**同時に発射できるビーム数**が増え、それぞれが自動で別々の敵を狙う。「一方向にしか撃てない」という弱点そのものを、育てるほど解消できる武器に変わった。

**v1.36.85での再調整**: v1.36.83は「フルチャージに達した瞬間、ロックしている全ビームが一斉に発射される」仕様だったため、高ランクでは見た目が派手すぎるとのフィードバックを受けた。2点変更した - (1) ビーム1本ごとの幅を`8px→8/3px`(ちょうど1/3)に縮小、(2) 段階制を「発射されるビームの本数」という新しい形で復活させた。具体的には、`chargeBeamCount`(ランクで決まる最大同時本数)がそのまま「チャージ段階の上限」も兼ねるようになり、ホールド時間に応じて発射本数が1本ずつ段階的に解放される(下記)。威力・当たり判定・ターゲティング・貫通など、本数以外の仕組みはv1.36.83からそのまま。

- **チャージ操作**: 移動操作と全く同じ押下/ドラッグ操作(`isMoveInputHeld()`。タッチ・マウス・キーボードいずれの移動入力も対象)を押している間、`Player.chargeTime`が溜まっていく。移動用の操作をそのまま流用しているため、**チャージ自体は移動を一切妨げない**(チャージ中も普通に動ける)
- **チャージ段階とビーム本数(v1.36.85で復活)**: チャージは`CHARGE_TIME_PER_STAGE = 0.5秒`ごとに1段階ずつ進む離散的な仕組みで、`Player.chargeBeamCount`(ランクで決まる、§7-4のマルチビーム)段階を上限にそれ以上は溜まらない(`chargeTimeMax = chargeBeamCount × CHARGE_TIME_PER_STAGE`)。v1.36.22時点の段階制と異なり、段階が上がっても1本ごとの幅・威力は変化しない - 段階が決めるのは**発射されるビームの本数**(`chargeStageForCharge(chargeTime, chargeBeamCount) = min(chargeBeamCount, floor(chargeTime / CHARGE_TIME_PER_STAGE))`)のみ。例えばランク3(`chargeBeamCount = 3`)の場合、0.5秒保持(段階1)で離すとロック中の最も近い1体だけに1本、1.0秒(段階2)で近い2体に2本、1.5秒(段階3=フルチャージ)で3体全員に3本発射される。段階を上限まで満たした後はそれ以上保持しても増えない
- **発射**: 移動入力を離した瞬間(押下→非押下の立ち下がりを毎フレーム検出)に発射され、`chargeTime`はヒットの有無に関わらず即座に0にリセットされる。発射されるのはロック中の対象のうち近い順に現在の段階数ぶんだけで(`Player.chargeTargets`は元々近い順にソート済み、下記)、ロック中の対象が1体もいない状態(下記のターゲティング方式で誰も捕捉できていない状態)で離してしまうと、それまでのチャージが何も生まず単純に無駄になる - 「良いタイミングで離す」判断そのものがこの武器のリスクになっている
- **ターゲティング方式(ターゲットロック、v1.36.78で単一対象向けに追加、v1.36.83で複数対象に一般化)**: 対象の選定は**「射程内にいる、最も近い`Player.chargeBeamCount`体の敵」**を全方位から探す(標準武器のマルチショットと同じ「近い順にN体」ロジック) - ロックする対象の数自体は現在の段階に関わらず常に`chargeBeamCount`体ぶん確保しておき、実際に発射する段になって初めてその先頭(近い順)から現在の段階数ぶんだけを使う(上記)。この探索自体は**`CHARGE_TARGET_LOCK_INTERVAL = 1秒`に1回しか実行・切り替えを許可しない**ロック機構の対象になる(`Player.chargeTargets`=現在ロック中の敵の配列、`Player.chargeTargetLockTimer`=次に切り替え可能になるまでの残り時間、いずれも`Game.updateChargeBeam()`内で管理)。ホールド中は毎フレーム、(1)ロック中の敵のうち1体でも既に死亡/消滅していないか、(2)ロックの残り時間が0以下かのいずれかを満たした時だけ全方位探索を再実行し、その結果(最大`chargeBeamCount`体の新しいセット)でロック対象を丸ごと更新する(何も見つからなかった場合はロック残り時間を進めず、次のフレームで即座に再探索する - 「何もいない状態」のまま無駄に1秒間ロックされ続け、後から敵が近づいてもすぐには反応しない、という事態を避けるため)。これにより、ホールド中に一瞬だけ近くを敵が横切っても、ロック中の対象がすぐに死ぬか1秒経過しない限り狙いが不用意に切り替わらなくなり、離す瞬間に全く別方向へ暴発する問題を、v1.36.78の単一対象版と同じ考え方のまま複数対象に拡張して解消している。発射(`fireChargeBeam()`)は`Player.chargeTargets`をそのまま使うだけで、発射時に改めて探索し直すことはない(プレビュー表示と発射内容が食い違うことがない)。ホールドを離すたび(発射の有無に関わらず)`chargeTargets`/`chargeTargetLockTimer`はリセットされ、次にホールドを始めた瞬間また新規にロックし直す
- **最低チャージ時間(v1.36.19で追加、v1.36.85で「段階1」を要求する形に統一)**: `chargeTime`が`CHARGE_TIME_PER_STAGE`(=段階1)未満の状態で離した場合は何も発射されず、他のケースと同様`chargeTime`だけが0にリセットされる。この下限がないと、画面を連打する(押した瞬間にすぐ離す)だけで`atkCooldown`に一切縛られない連射ができてしまう抜け穴があったため、それを塞ぐ目的で追加した。チャージ中は自機周囲のリング(下記)がこの閾値未満では暗いグレーで表示され、まだ発射できる状態ではないことが視覚的にわかる
- **チャージ速度と攻撃速度アップグレードの関係**: 攻撃速度アップ/速射特化などの`atkCooldown`を下げるアップグレードは、他の武器のような「攻撃間隔短縮」ではなく**チャージ速度の倍率**として作用する: `chargeRate = ATK_COOLDOWN_BASE(0.7) / Player.atkCooldown`。基準値(素の`atkCooldown = 0.7`)ではchargeRate = 1、`atkCooldown`が下がるほどchargeRateが上がり(比例)各段階・フルチャージまでの体感時間が短縮される。攻撃速度系のアップグレードがこの武器でも腐らないよう、既存のロジックをそのまま転用する形にしている
- **威力(v1.36.83でチャージ段階に関係ない固定値に統一、v1.36.85でも据え置き)**: `Player.damage` × 特殊バフ倍率に、固定倍率`CHARGE_DAMAGE_MULT = 3.0`を掛けた値を、発射される対象それぞれに個別のビームとして与える(値自体は旧・段階1の値をそのまま引き継いでいるため、1本あたりの威力はv1.36.83以降変わっていない)。伸びるのは1本あたりの威力ではなく、発射される本数(=同時に攻撃できる敵の数)
- **幅(v1.36.83でチャージ段階に関係ない固定値に統一、v1.36.85で1/3に縮小)**: 実効半幅は`CHARGE_HALF_WIDTH = 8/3px`固定(v1.36.83時点は8px)。複数の全幅ビームが同時発射されると見た目が派手すぎるというフィードバックを受け縮小した。ビームごとに独立してこの幅を持つため、発射される本数ぶんのビームがそれぞれ別方向へ、それぞれこの幅で同時に発射される
- **射程・判定**: 射程は他の武器と同じ通常の`weaponRange(p)`(画面サイズに応じた長距離)を使用し、パルスウェーブのような近接縛りはない。発射される対象それぞれについて、自機からその対象への方向を軸にした矩形状の当たり判定(前方`weaponRange(p)`px・左右`CHARGE_HALF_WIDTH`px)を個別に計算し、その範囲に入る敵を**貫通数の上限なく全員同時に**ヒットさせる(`pierce`は一切参照しない、文字通りの無限貫通)。1本のビームが複数の敵を貫通してヒットさせる挙動(狙った対象と同じ直線上に別の敵がいれば、そちらも巻き込む)自体はv1.36.83から変わっていない - 変わったのは、その「1本の無限貫通ビーム」が今の段階で何本飛ぶかという点のみ
- **弾丸効果との連携**: パルスウェーブと全く同様、貫通以外の弾丸効果(爆発・連鎖・低速・猛毒・狂乱・爆弾化・衰弱・脆弱)は`Game.resolveProjectileHit()`経由で通常の弾丸と同じロジックで適用され、ビームごとに(=ヒットした敵ごとに)個別に発動する。貫通のみ、この武器の性質上そもそも意味を持たないためレベルアップ画面の候補から除外される(パルスウェーブと同じ`available`ゲート、§4-3)
- **視覚効果**: 発射時、実際に何かに命中したビームごとに、方向・射程・幅に一致する帯状の光が0.2秒だけ光って消える(`BeamEffect`)。何にも当たらなかったビーム(発射対象がいたにもかかわらず射線上に誰もいなかった場合)はエフェクトを出さない(パルスウェーブ・標準武器の「命中しなければ何も起きない」という扱いと同じ)。チャージ中は自機の周囲に、チャージ量に応じて太く・明るくなっていくリングを表示し、「今離すと発射できる状態か」を視覚的に把握できるようにしている(段階1未満はグレー、以降は通常の水色〜白の進行。リング自体は段階の区切りに関係なく滑らかに成長するアンビエントな表示で、実際に発射される本数は下記のプレビューが示す段階的な値が正)
- **狙い方向プレビュー(v1.36.21で追加、v1.36.78でターゲットロック方式に追従、v1.36.83で複数対象ぶんを同時表示、v1.36.85で現在の段階数ぶんだけ表示するよう変更)**: どちらに・何本発射されるかが撃ってみるまでわからない問題を解消するため、チャージ中は毎フレーム、`Game.updateChargeBeam()`が管理している`Player.chargeTargets`(現在ロック中の対象の配列、上記)のうち、**現在の段階数ぶん**(`chargeStageForCharge(chargeTime, chargeBeamCount)`、近い順)を読み取り、実際に離した場合の発射方向・固定幅と一致する矩形の破線アウトラインを対象ごとに自機から伸ばして表示する。プレビュー自体が独自に対象を探索することはなく、発射(`fireChargeBeam()`)と全く同じ参照先・同じ段階計算を見ているため、両者が食い違うことは構造上あり得ない。ロックが実際に切り替わった瞬間、または段階が1つ進んだ瞬間だけプレビューが更新されるため、「今離したらどこへ何本飛ぶか」を落ち着いて確認しながらチャージできる。段階1未満はグレー、以降は水色の破線になる(リングと同じ配色ルール)

### 7-4-3. ラピッドファイア武器の攻撃方式(移動方向への高速連射、v1.36.45で追加、v1.36.46でバランス・弾の並び方を調整、v1.36.47でオートエイム範囲を追加・コーンから長方形に変更)

ラピッドファイア(`WEAPONS`の`id: 'rapidfire'`)は他のどの武器とも異なり、標準武器・チャージビームのような**画面内のどこにいる敵でも自由に自動照準する**仕組みを持たない。基本の狙いはプレイヤー自身が「今向かっている方向」で、そこへ向けて高速射撃の`Projectile`を撃ち続ける。「進みたい方向を自分で選び、その先を切り開く」攻撃的なプレイスタイルを提供する武器という位置づけ。

- **狙い方向 (`Player.moveDirAngle`)**: 自機が実際に移動している間(`update()`の移動処理で`mag > 0.02`が成立する間)、毎フレーム`Math.atan2(my, mx)`(現在のドラッグ/キーボード入力方向)で更新される。停止すると更新が止まり、最後に移動していた方向をそのまま保持し続ける(0にリセットされたりはしない) - 移動を止めた瞬間も、直前まで向かっていた方向へ発射し続けられる
- **前方長方形オートエイム(v1.36.47で追加、当初はコーン(扇形)で実装したが同バージョン内で長方形に変更)**: 進行方向ぴったりに敵を合わせないと当たらない仕様は実際にプレイすると厳しすぎたため、`moveDirAngle`方向へ`weaponRange(p)`まで伸びる、左右`RAPIDFIRE_AUTOAIM_HALF_WIDTH = 60px`幅の長方形の範囲内に敵がいれば、その中で最も近い敵をそのまま狙う。範囲内に該当する敵が1体もいなければ、これまで通り`moveDirAngle`へそのまま発射する(フォールバック)。判定は`moveDirAngle`方向の単位ベクトルとその垂直ベクトルへの内積で敵の相対座標を分解し、前方成分が`0〜weaponRange(p)`かつ横方向成分の絶対値が`RAPIDFIRE_AUTOAIM_HALF_WIDTH`以内かで行う。当初はコーン(進行方向から半角15度、合計30度)で実装したが、コーンは自機に近いほど幅が狭くなり、接敵直前(オートエイムが最も欲しい場面)でむしろ機能しなくなる欠点があったため、距離によらず幅が一定の長方形に変更した。あくまで「進行方向のごく近く」限定の狭いオートエイムであり、標準武器・チャージビームのような射程内全域を対象にした自由な照準ではない - 武器としての方向性の縛りは維持したまま、ピクセル単位の完全な位置合わせだけを緩和する調整
- **威力・攻撃間隔・弾速**: 通常のダメージ計算(`Player.damage` × 特殊バフ倍率)に`RAPIDFIRE_DAMAGE_MULT = 0.55`倍(v1.36.45時点は0.4倍だったが、実際にプレイして弱すぎたためv1.36.46で引き上げ)を掛けた威力。`atkCooldown`には`RAPIDFIRE_COOLDOWN_MULT = 0.35`を掛けた短い間隔で連射する(攻撃速度アップグレードの効果はこの短縮後の間隔にもそのまま乗る、こちらはv1.36.46で変更なし)。弾速も`Player.projSpeed`に`RAPIDFIRE_PROJ_SPEED_MULT = 1.5`を掛けた高速弾(v1.36.46で追加) - 他の武器の弾より明確に速く飛ぶ、この武器だけの特性になっている
- **同時発射数と弾の並び方(v1.36.46で変更)**: `Player.rapidfireBarrels`(武器固有効果「同時射撃数アップ」、§7-4)の数だけ、実際に発射する角度(狭角オートエイムが対象を捉えていればその方向、なければ`moveDirAngle`)と全く同じ角度・同じ速度の`Projectile`を同時に生成する(マルチショットのような角度のばらつきは一切ない)。初期実装(v1.36.45)では進行方向と同じ1本の弾道上に前後にずらして並べていたが、v1.36.46で発射方向に対して**垂直**な向きに等間隔で並べる方式に変更した: `i`番目(0始まり)の発射位置は、発射方向を90度回転させた向きに`(i - (barrels-1)/2) * RAPIDFIRE_BARREL_GAP`px(`RAPIDFIRE_BARREL_GAP = 14`)だけ自機からずらした位置になる。中心を基準に左右対称に配置されるため、発射数がいくつでも横一列の並びが中央からずれない(ランク1="-"、ランク2="="、ランク3="≡"のようなイメージ)。速度・当たり判定・到達距離はどの発射位置でも共通で、影響するのは発射位置(=横方向のカバー範囲)のみ
- **射程・判定**: 標準武器と同じ通常の`Projectile`として生成される。前方長方形オートエイムの対象探索にのみ`weaponRange(p)`を使うが、実際の弾の到達距離は他の通常弾と同じ`Projectile.life`(寿命)と(引き上げられた)実効`projSpeed`で決まり、`weaponRange(p)`そのものが弾の届く距離を制限するわけではない
- **弾丸効果との連携**: 貫通(`pierce`)を含むすべての弾丸効果(爆発・連鎖・低速・猛毒・狂乱・爆弾化・衰弱・脆弱)が、標準武器と全く同じ`Game.resolveProjectileHit()`経由でそのまま適用される。パルスウェーブ・チャージビームと異なり通常の`Projectile`(飛翔・貫通のライフサイクルを持つ)を使うため、貫通がそもそも意味を持たない武器としての除外対象にはならない(`available`ゲートに含まれていない)
- **弱点**: 前方長方形オートエイムはあくまで進行方向の細い帯状の範囲しかカバーしないため、複数方向から同時に迫られる状況では対応できる範囲が狭い。他の武器のような全方位・広範囲のカバーは苦手で、代わりに移動しながら進行方向の敵を継続的に薙ぎ払うプレイスタイルに特化している

### 7-4-4. ランドマイン武器の攻撃方式(設置型・時限/接触起爆、v1.36.99で追加、v1.36.100で弾丸効果連携・多重設置対策を追加)

ランドマイン(`WEAPONS`の`id: 'landmine'`)は他のどの武器とも異なり、`Projectile`を一切生成しない。`Game.fireLandmine()`が呼ばれるたびに、その瞬間の自機の位置にその場で`Landmine`インスタンス(`Game.landmines`配列で管理、飛翔しない)を1個ずつ設置するだけの武器で、マルチショットの概念(`Player.projCount`)を持たない。

- **起爆条件**: 設置から`MINE_FUSE_TIME=5秒`が経過するか、いずれかの敵と接触(`MINE_RADIUS=10px`)すると起爆する(`Game.update()`内、毎フレーム`Game.landmines`をフィルタしながら判定)。どちらが先でも同じ処理に合流する
- **v1.36.99版の課題(v1.36.100で解消)**: 初期実装は起爆と同時にダメージエリア(旧・キルゾーン仕様)を作るだけで、地雷自体が`Projectile`ではないため爆発・連鎖・猛毒・狂乱・爆弾化・衰弱・脆弱といった弾丸効果が一切乗らなかった。また複数の地雷を並べて起爆させるとダメージエリアが単純に増える一方だったため、範囲火力が過剰になりやすいという指摘を受け、以下の2点を修正した
- **起爆は「弾丸効果込みの一撃」+「残存ダメージエリア」の2段構え(v1.36.100)**: 起爆すると、まず爆心(ダメージエリアと同じ半径`killZoneRadiusForLevel(mine.rank)`以内)にいる敵**全員**に対して、ソード＆シールドのソード判定(§7-4-6)と同じ「仮想`Projectile`を`Game.resolveProjectileHit()`に通す」パターンで一撃を与える。この仮想`Projectile`の`damage`(=`mine.damage`)と爆発半径・連鎖回数・低速時間・猛毒/狂乱/爆弾化/衰弱/脆弱の各フラグは、いずれも**設置の瞬間(`Game.fireLandmine()`)にスナップショット**され(`Landmine.effects`、通常の`Projectile`が発射時点でこれらの値を固定するのと同じ考え方)、起爆までの間に自機のビルドが変わっても再計算されない。これにより爆発の誘爆・連鎖の伝播・各種状態異常の付与が、他のどの武器の一撃とも変わらず地雷にも乗るようになった。この一撃の後、これまで通り旧・キルゾーン仕様(§4-3-2)の`ImpactEffect('killzone', ...)`を1個生成し、持続時間`KILLZONE_DURATION=5秒`のダメージエリアとして残す(半径は同じ`killZoneRadiusForLevel(mine.rank)`、1秒あたりのダメージは`mine.damage × killZoneDmgPctForLevel(mine.rank)`)
- **複数地雷の同時爆発に対応**: `Game.spawnImpactEffect()`は同じ`type`のゾーンが1個しか同時に存在できない仕組み(1エントリの単一インスタンス方式)だが、地雷は同時に複数個設置・起爆できる必要があるため、この重複排除をあえて経由せず、`Game.impactEffects`に`ImpactEffect`を直接`push`する専用ルートを使っている。これにより、離れた場所に置かれた複数の地雷がそれぞれ独立したダメージゾーンとして同時に存在できる
- **重なったダメージエリアは加算されない(v1.36.100)**: 複数のキルゾーン系ゾーンが同じ敵に重なった場合、以前は各ゾーンが独立してティックするため単純に合算されていたが、v1.36.100で「その瞬間その敵に効いているゾーンのうち、最も`dmgPerSec`が高い1つだけが実際にティックする」方式に変更した(`Game.update()`内、`dominantKillzone`という`Map<Enemy, ImpactEffect>`を毎フレーム構築し、各キルゾーンのティック処理はこのマップで自分が担当だと分かった敵にしか適用しない)。担当から外れている間、そのゾーン側の`tickTimers`はリセットされず単に凍結される(ゾーンを抜けたのではなく強いゾーンに覆われているだけの間は時間が進まない扱いになり、後で再び自分が最強になった時にゼロからやり直しにはならない)。これにより、地雷を並べて範囲をカバーする戦術の面白さ(=カバー範囲の広さ)は残しつつ、1体を複数ゾーンの重ねがけで袋叩きにする単純な火力の合算は防いでいる。詳細な実装は§4-3-2も参照
- **武器固有効果「爆発範囲アップ」**: レベルアップに応じて`Player.landmineRank`が自動でランクアップし(他の武器固有効果と同じ`WEAPON_INNATE_LEVELS_PER_RANK`/`WEAPON_INNATE_MAX_RANK`を共有)、起爆時のダメージエリア半径(=一撃が届く爆心の範囲でもある)が広がっていく(`killZoneRadiusForLevel`はランクに応じて半径・秒間ダメージ割合の両方を上げる、§4-3-2)

### 7-4-5. マルチミサイル武器の攻撃方式(低速追尾・強制爆発、v1.36.99で追加)

マルチミサイル(`WEAPONS`の`id: 'missile'`)は標準武器のマルチショット(近い順にN体を個別に狙う)と同じ対象選定ロジックを流用しつつ、生成する`Projectile`自体の挙動が大きく異なる。

- **低速・貫通なし**: 弾速は`MISSILE_PROJ_SPEED=150`(標準弾より明確に遅い)固定、貫通は仕様上常に0固定(`pierce`引数に`0`を渡して生成)。生存時間も`MISSILE_LIFE=6秒`と長め(ゆっくり飛ぶ分、標準弾の寿命のままでは目標まで届かないため延長)
- **追尾(ホーミング)**: 生成した`Projectile`に`proj.homing = true`と`proj.target`(発射時点で選ばれた対象)を追加で持たせる。`Game.update()`の弾移動処理の直前で、`proj.homing`な弾ごとに、(1)現在の対象が既に存在しない(撃破/消滅した)場合のみ、その時点で最も近い敵に再ロックオン(対象がいなければ`null`のまま)、(2)対象がいれば毎フレーム、現在位置から対象の現在位置へ向く角度に`proj.vx`/`proj.vy`を同じ速度のまま再計算、という2ステップを行う。「対象がキルされたら再ロックオンする」という設計どおり、対象が生きている間は常に現在位置へ瞬時に向き直る(徐々に旋回するのではなく、フレームごとに正確に照準し直す)一方、対象を勝手に乗り換えることはない
- **強制100%爆発(`Projectile.explosionChance`、v1.36.99で追加した任意コンストラクタ引数)**: 取得した瞬間に武器固有の`apply`で「爆発」ランク1を内蔵付与する(`p.explosionLevel = Math.max(p.explosionLevel, 1)`)。加えて、この武器自身が生成する`Projectile`にのみ`explosionChance=1`を渡すことで、`Game.resolveProjectileHit()`内の爆発発動判定(本来は`EXPLOSION_TRIGGER_CHANCE`に従う確率抽選)を100%に固定している。この上書きは`Projectile`インスタンス単位(`proj.explosionChance`が未指定なら既存の`EXPLOSION_TRIGGER_CHANCE`にフォールバック)のため、他の武器の爆発発動率には一切影響しない
- **武器固有効果「マルチショット」**: `Player.projCount`をそのままランクの値として使う、標準武器と全く同じ仕組み(§7-4の「現在の実装」参照)。近い順にランク分の対象を選び、それぞれへ1発ずつ追尾ミサイルを発射する

### 7-4-6. ソード＆シールド武器の攻撃方式(常時展開の近接武器、v1.36.99で追加)

ソード＆シールド(`WEAPONS`の`id: 'swordshield'`)は他のどの武器とも異なり、`atkTimer`/`atkCooldown`によるクールダウン駆動の「発射」という動作自体を持たない。`Game.fireWeapon()`の冒頭、`atkTimer`のガードより前の位置で武器IDが`swordshield`かどうかを判定し、該当すれば毎フレーム無条件に`Game.updateSwordShieldWeapon(dt)`を呼んで即座に`return`する(チャージビームの溜め撃ち判定と同じ早期分岐の位置)。シールド・ソード双方の判定がこの1メソッド内で毎フレーム解決される

- **「前方」の基準**: ラピッドファイアと同じ`Player.moveDirAngle`(直近の移動入力方向、停止後も最後の向きを保持)を「前方」の基準として使う
- **シールド(前方、ノックバックのみでダメージなし)**: 前方(`moveDirAngle`)を中心に左右`SHIELD_HALF_ANGLE=60度`(合計120度)の扇形範囲、半径`Player.shieldRadius`以内にいる敵を毎フレーム検出し、ダメージは与えず`SHIELD_KNOCKBACK_SPEED=150px/秒`で扇の外側方向へ押し出し続ける(`Game.resolveWallCollision()`で壁の中にめり込まないよう補正)。同じ扇形範囲に入った敵の射撃(`Game.enemyProjectiles`)は、消滅させる(`life = 0`)形で打ち消す(反射ではなく破壊)
- **ソード(右側面、高威力)**: 前方から時計回りに90度回転した方向(`moveDirAngle + Math.PI/2`、自機の右側)へ`Player.swordLength`px伸びる、幅`SWORD_HALF_WIDTH×2`の細長い長方形の当たり判定。命中した敵ごとに`SWORD_HIT_COOLDOWN=0.4秒`の専用クールダウン(`Enemy.swordHitCd`、`contactCd`とは別の独立したフィールド)を設け、連続フレームで同じ敵に何度もヒットしないようにしている。威力は`Player.damage × 特殊バフ倍率 × SWORD_DAMAGE_MULT(2.5倍)`で、命中判定を満たすたびに`Game.resolveProjectileHit()`(パルスウェーブの衝撃波と同じ「仮想`Projectile`経由」の呼び出し方)を通すため、爆発・連鎖・猛毒・狂乱・爆弾化・衰弱・脆弱などあらゆる弾丸効果がソードの一撃にもそのまま乗る。壁越しには判定が通らない(`Game.segmentHitsWall`で自機と対象を結ぶ線分を検査)
- **武器固有効果「拡大」**: レベルアップに応じてランクが自動で上昇し、`Player.swordLength = SWORD_BASE_LENGTH(70) + SWORD_LENGTH_PER_RANK(15)×(ランク-1)`と`Player.shieldRadius = SHIELD_BASE_RADIUS(55) + SHIELD_RADIUS_PER_RANK(10)×(ランク-1)`を1つのランクアップ効果として同時に伸ばす(ソード・シールド別々のランクではなく、常に連動して大きくなる)

### 7-5. 一時停止画面(v1.30.0で追加)

一時停止ボタン(画面上部中央、`II`)を押すと開くオーバーレイ画面。

- **現在ステータス表示**: HP、レベル、ダメージ、攻撃間隔、同時発射数、移動速度、HP自然回復、回収範囲、所有中のドローン(v1.36.63で弾丸効果の行と別立てに、v1.36.82で複数種類を同時に列挙する形に変更、各ドローンの所有台数を表示)、取得済みの弾丸効果(ランク表示、v1.36.89でアップグレードのランク表記を自機レベルと区別するため「Lv.」から「ランク」に統一)、生存時間、撃破数、現在の難易度をまとめて表示する。HUDには常時表示されていない情報(ダメージ・攻撃間隔・貫通・ドローン所有台数など)もここで確認できる
- **「再開」ボタン**: ゲームを再開する。一時停止画面はほかのオーバーレイ画面と同じく`.overlay`(全画面・`z-index: 30`)のため、背後にある一時停止ボタン自体はこの間押せない(隠れる) - そのため再開操作は一時停止ボタンの再クリックではなく、この専用ボタンで行う。再開時、レベルアップ後の再開と同様に`RESUME_INVULN_DURATION`秒の無敵時間が付与される(§4-6)
- **「タイトルに戻る」ボタン**: 現在のランを終了し(`game.over = true`)、スタート画面に戻る。副次的なボタンのため、通常のアクションボタンより控えめな見た目(`.overlay button.secondary`)にしている

### 7-6. 脅威ビネット(`THREAT_RADIUS`、v1.36.14で追加)

難易度に応じて敵HP・敵ダメージが柔軟にスケーリングするぶん(§6)、プレイヤーが「難易度Nならこのくらいの被ダメージ」と数値的に見積もるのは難しく、油断していると接触一撃でHPを大きく失う(最悪即死する)事態が起こりうる。これを避けるため、具体的な数値をHUDに出す代わりに、危険な状況そのものを直感的に警告する演出を追加した。

- **判定**: 毎フレーム、`Game.update()`内の敵移動ループで各敵について「プレイヤーとの距離が`THREAT_RADIUS = 220`px未満」かつ「その敵の実効接触ダメージがプレイヤーの現在HP以上」かを判定する。1体でも条件を満たせば`Game.threatNearby = true`になる
- **実効接触ダメージ**: 衰弱状態(`weakenDmgMultForLevel`による減少、§4-1)を反映した値(`effDmg`)で判定する(v1.36.87で狂乱の攻撃力アップが撤廃されたため、現在このeffDmgに影響するのは衰弱のみ)
- **表示**: 判定結果は`#threat-vignette`要素の`active`クラスに直結し、trueの間だけ画面四隅から中央に向けて赤い放射状グラデーションが0.6秒周期でパルスする(`pointer-events: none`のためプレイ操作を妨げない)。画面中央のゲームプレイ領域は透明のままで、視界を遮らない
- **設計意図**: 中央にテキストアラートを出す案(ラッシュアラート§6-6と同様の方式)も検討したが、この脅威判定は敵の出入りで頻繁にON/OFFが切り替わりうるため、テキスト表示だと点滅が煩わしく視界も遮られる。画面端のビネットなら邪魔にならず「際どい状況にいる」という空気感だけを伝えられる
- ラッシュ中(§6-6)は敵HP・敵速度が強化されるため、通常時よりこの警告が発生しやすくなる

---

## 8. カメラ・描画

- プレイヤーは常に画面中央に固定され、ワールド座標をカメラオフセットで描画(無限マップ的な見た目)
- 背景はシンプルなグリッド線(64px間隔、ワールド座標基準)
- 被弾時に画面シェイク(0.15秒)。一時停止中・レベルアップ画面表示中はシェイクの再抽選を止める(`!paused && !this.levelingUp`)。後者を怠ると、被弾とレベルアップが同時に起きた際、レベルアップ画面を開いている間ずっと画面が揺れ続けてしまう不具合があったため対応(v1.30.0)
- 敵・プレイヤー・弾・XPジェムはすべて単色の円/矩形によるシンプルな図形描画
- 描画順(背面→前面): 背景グリッド → XPジェム → 敵 → パーティクル/連鎖ライン → 弾 → プレイヤー。パーティクル・連鎖ラインは敵より後(上)に描画することで、敵グラフィックに隠れて見えなくなるのを防いでいる

### 8-1. カメラズーム(`viewScale`、v1.36.23で追加)

射程アップ(§4-1)で`Player.rangeMult`が伸びると、標準武器・チャージビームの実際の攻撃範囲(`weaponRange(p)`)は画面の可視範囲より外側まで伸びてしまう。見えない場所の敵を撃っているだけになってしまわないよう、カメラのズームを`rangeMult`と連動させ、視界そのものを射程に合わせて広げている。

- `viewScale(p) = 1 / p.rangeMult`。`rangeMult = 1`(射程アップ未取得)なら`viewScale = 1`で、この機能導入前と完全に同じ見た目になる
- `draw()`冒頭で`ctx.translate(W/2 + shakeX, H/2 + shakeY) → ctx.scale(viewScale, viewScale) → ctx.translate(-camX, -camY)`という1つの変換行列を組み、以降の描画はすべて素のワールド座標で行う(個々の描画コードがオフセットを手動で足す必要がなくなった)。画面シェイクはこのズーム変換の**外側**で screen-pixel 単位のまま適用しているため、ズームレベルに関わらず常に一定の見た目のシェイク量になる(ズーム変換の内側だと、ズームアウトするほどシェイクが縮んで見えてしまう)
- 背景グリッドは現在のズームで実際に見えているワールド範囲(`camX ± (W/2)/viewScale`など)を毎フレーム計算して描画するため、ズームアウトしても画面全体を隙間なく覆う
- オフスクリーンの敵/ジェムを描画スキップする従来のカリング処理は、ワールド座標描画への切り替えに伴い削除した(`MAX_ALIVE_ENEMIES = 120`程度の描画コストであれば、カリング無しでも十分軽量なため)

---

## 9. 既知の設計上の注意点

- `main.js` はIIFEで完結しており、外部モジュールやビルドステップは無し
- ゲームロジックの主要な状態は `Game` クラスのインスタンス(`game`変数)に集約
- HUD更新は通常 `update()` 内でのみ行われるため、`pickUpgrade()` 内でのステータス変更(スキップのXP反映など)は明示的に `updateHud()` を呼ぶ必要がある(`update()`の次回実行を待たず即座にHUDへ反映するため)
- バージョン番号は `main.js` 冒頭の `GAME_VERSION` 定数を書き換えるだけで更新可能

---

## 10. 地形(壁)(`Wall`、v1.36.50で追加)

移動不可・破壊不可の矩形オブジェクト。自機・敵とも通過できず、一部の武器を除いて攻撃も遮る。狙いは「敵はただ自機に向かってくるだけ」「自機はただ敵に近づく/離れるだけ」だった単純な駆け引きに、地形を使った回避・迂回という新しい選択肢を加えること。

### 10-1. 生成方式: マップ全体への一括生成ではなく、探索に応じたチャンク単位の継続生成

このゲームの「マップ」は実質無限で、敵(`spawnEnemy()`)もハート(§6-7)も常に自機の**現在位置**を基準に生成される(固定のマップ境界は存在しない、§8のカメラズームのコメントにある通り「無限マップ的な見た目」)。そのため、ゲーム開始時に自機周辺だけへ壁を一度きり生成する方式だと、ランが自機の初期位置から十分離れた時点で壁の恩恵が完全に失われてしまう。これを避けるため、壁はワールドを`WALL_CHUNK_SIZE = 800`px四方の格子(チャンク)に区切り、自機が未生成のチャンクに近づくたびにそのチャンク単位で壁を生成し、以後永久に残す方式を採用している(ラン中に消えることはない)。

- **生成判定**: `Game.update()`の自機移動処理の直後に毎フレーム`generateNearbyChunks()`を呼ぶ。自機の現在のチャンク座標を中心に`WALL_GEN_RADIUS_CHUNKS = 2`チャンク四方(5×5=25チャンク)の範囲を走査し、`Game.generatedChunks`(`"cx,cy"`形式の文字列を要素とする`Set`)に未登録のチャンクがあればそのチャンクの壁を新規に生成する。既に登録済みのチャンクは`Set`の`has()`チェック1回で即座にスキップされるため、範囲内が生成済みになった後は実質無視できるコストしかかからない。`Game`のコンストラクタでも1回呼ばれるため、ゲーム開始直後(1フレーム目)から既に周囲の壁が存在した状態になる
- **1チャンクあたりの生成内容(v1.36.51でサイズを半分に、個数・頻度を引き上げ。v1.36.54でさらに個数・頻度を引き上げ)**: `WALL_CHANCE_PER_CHUNK = 0.85`の確率で「そのチャンクに壁を作る」判定を行い、当たれば`WALL_COUNT_MIN(1)〜WALL_COUNT_MAX(4)`個の壁を生成する。各壁のサイズは幅・高さともに`WALL_SIZE_MIN(50)〜WALL_SIZE_MAX(130)`pxの範囲でランダムに決まり、必ずそのチャンク自身の範囲内に完全に収まる位置に配置される(チャンク境界をまたぐ壁は存在しない)。生成結果は`Game.wallChunks`(`"cx,cy"` → `Wall[]`の`Map`、壁0個のチャンクも空配列として登録される)にチャンクごとに保存される。初期実装(v1.36.50)はサイズ100〜260px・出現率0.45・個数1〜2個だったが、実際にプレイした結果「壁が大きすぎる、数はもっとあっていい」というフィードバックを受け、v1.36.51で平均サイズを約半分(約180px→約90px)に、平均個数を1チャンクあたり約0.68個→約1.48個に調整。さらに「壁の数をもっと増やしたい」というフィードバックを受け、v1.36.54で出現率・上限個数を再度引き上げ、平均個数を約2.1個(初期実装の約3倍)まで増やした
- **自機の現在位置とのクリアランス**: 生成しようとした壁が自機の**現在位置**(`WALL_PLAYER_CLEARANCE = 250`px以内)と重なる場合、その壁の生成だけを取りやめる(別の位置に移動させるのではなく単純にスキップ)。壁は探索の進行に応じて継続的に生成され続けるため、この判定がなければ移動中に新しいチャンクへ入った瞬間、自機のちょうど真上に壁が出現してしまいかねない - この仕組みがそれを防いでいる
- **既存の敵とのクリアランス、および同一チャンク内の壁同士の重なり回避(いずれもv1.36.52で追加)**: 自機だけでなく、その時点で**既に存在している敵**とも重ならないようにスキップする(そのチャンクの敵`radius`を基準にした距離判定)。チャンクは自機の探索に応じて後から生成されるため、敵が先にそこに立っていた後からチャンクが生成される順序も普通に起こりうる - この判定がないと、既に敵がいる場所に壁が後出しで生成され、「敵が壁の中に生成された」ように見える不具合になる(実際には壁が後から現れているだけだが、プレイヤーからは敵が壁に埋まって見える)。加えて、同じチャンク内で複数の壁を生成する際、既にそのチャンクに置いた壁と重なる位置への生成もスキップする(壁同士が重なると、下記10-2の押し出し判定が1回では抜け出せない形状になりうるため)
- **`spawnEnemy()`自体の後処理(v1.36.52で追加)**: 敵の出現位置(`spawnDist`、自機からの距離のみで決まり壁の有無は見ていない)は上記の生成時クリアランスとは別物のため、出現位置がたまたま既存の壁と重なることは起こりうる。これに対応するため、`spawnEnemy()`は各敵を`this.enemies`に加える直前に`resolveWallCollision(enemy)`を1回呼び、壁に埋まった状態のまま出現することがないようにしている
- **ハート(HP回復アイテム、§6-7)も同様(v1.36.55で追加)**: ハートの出現位置(`p.x/y`からの角度・距離のみで決まる、§6-7参照)も壁の有無を見ていない点は敵の出現と同じだが、ハートは敵と違い出現後に一切動かない(自前の毎フレーム移動処理を持たず、`resolveWallCollision`が敵の移動ループのように継続的にはかからない)。そのため敵のケースと違い「そのうち壁から押し出される」自己修復が起きず、壁に埋まって出現すると**その場に永久に埋まったまま**になり、埋まり方によっては自機がその壁を通り抜けられない以上ハート自体に触れられず回収不能になる恐れがあった。実際に壁が無い状態と比べて生成直後の埋め込み率を測定したところ約4.6%(壁の密度がv1.36.54で引き上げられた後の値)が埋め込まれる計算になったため、ハート生成時にも敵と全く同じ`resolveWallCollision(heart)`を1回呼ぶよう修正した
- **なぜチャンク単位のMapか**: 壁の総数はラン時間とともにどこまでも増え続けるため(消えない)、衝突判定・視線判定のたびに全壁を線形走査すると長時間プレイでどんどん重くなってしまう。`wallChunks`をチャンク座標で引けるMapにしておくことで、任意の一点・一区間の近傍にある壁だけをO(1)に近いコストで取得でき(`Game.wallsNear()`は3×3チャンク分、`segmentHitsWall()`は線分が通過するチャンク分だけを見る)、生成済みチャンク数に関わらず判定コストがほぼ一定に保たれる

### 10-2. 衝突判定: 自機・敵とも通過不可

`Game.resolveWallCollision(entity)`が円(自機・敵とも`radius`を持つ円として扱われる、既存の当たり判定と同じ前提)と矩形の押し出し判定を行う。対象の近傍3×3チャンク分の壁(`wallsNear()`)それぞれについて、矩形上の最近接点と円中心の距離が`radius`未満なら、その最近接点から円中心へ向かうベクトル方向に(`radius`−距離)だけ押し出す。円の中心が矩形の内部に完全に入ってしまっているケース(最近接点=自分自身の位置になり距離0で正規化できない)は、矩形の中心からのオフセットと半幅・半高を比較し、より浅くめり込んでいる軸(上下 or 左右のうち近い方の辺)へ、その軸の半幅/半高+radius分だけ押し出すことで対応している。

- **自機**: `update()`の移動処理(ドラッグ/キーボード入力に基づく`p.x`/`p.y`の更新)の直後に`resolveWallCollision(p)`を呼ぶ。壁に向かって移動を続けても、壁の面に沿って止まるだけで貫通はしない
- **敵**: 敵の移動処理(後述10-3)で新しい位置を計算した直後、各敵ごとに`resolveWallCollision(e)`を呼ぶ。敵同士の重なりには従来通り一切の押し出しがない(壁専用の処理)点は変更していない
- **複数回パス(`WALL_COLLISION_PASSES = 4`、v1.36.52で追加)**: 1回の呼び出しで押し出し判定を`wallsNear()`の全壁に対して`WALL_COLLISION_PASSES`回繰り返す。隣接する2つの壁(異なるチャンク由来で、生成時に互いの重なりを一切チェックしていない組み合わせも含む)が近接して角を作っている場合、片方の壁から押し出した結果がもう片方の壁の内部になってしまうことがあり、1回のパスの中で片方を処理した後にもう片方を再チェックする機会がないと解消できないため、複数回繰り返すことで両方から抜け出せる位置に収束させている
- **完全にめり込んだケースの押し出し量の不具合(v1.36.52で修正)**: 初期実装では、上記「中心が矩形内部にあるケース」の押し出し量に、軸選択に使うペネトレーション量(`penX`/`penY`、中心から最寄り辺までの距離)を誤ってそのまま使っており、本来必要な半幅/半高ぶんの押し出しに届いていなかった。この誤った押し出し量は「まだ矩形の内部にある別の点」に移動するだけの結果になり、次のパスで再度めり込み判定→逆方向に同じだけずれた別の内部点へ、を繰り返す周期2の振動に陥っていた(`WALL_COLLISION_PASSES`が偶数だと元の位置にちょうど戻ってしまい、見た目上何も起きていないように見える)。これが「敵が壁の中に生成/滞留してしまう」不具合の直接の原因だった。修正後は正しく矩形の外(半幅/半高+radius分の位置)まで一発で抜け出す

### 10-3. 敵の迂回ルート探索(フローフィールド、`Game.buildFlowField`/`flowFieldDirectionAt`)

「壁の向こうにいる敵は自機への直線移動ができず立ち往生する」問題を避けるため、自機への直線移動が壁で遮られている敵だけ、迂回方向を教えてくれる経路探索の仕組みを使う。

- **判定**: 毎フレーム、各敵について`Game.segmentHitsWall(e.x, e.y, p.x, p.y)`(自機への直線上に壁の矩形と交差する点があるか、Liang-Barsky線分クリッピングで判定)を呼ぶ。falseなら(大半のケース - 壁があるチャンク自体少ない)これまで通り自機への直線方向にそのまま移動する。trueの場合のみ、下記のフローフィールドを参照する
- **フローフィールドの構築(`buildFlowField()`)**: `FLOW_FIELD_CELL = 64`px四方のマス目からなる格子を自機中心に構築し(一辺は自機からの敵の想定交戦距離+400pxの余裕を確保)、壁と重なるマスを「通行不可」として、自機のいるマスを起点(距離0)に4方向(上下左右、斜め移動は無し)のBFS(幅優先探索)で各マスの「自機までの最短マス数」を計算する。1ステップのコストが常に1(斜め移動を許さないことでコスト混在を避けている)なので、単純なFIFOキューのBFSだけで各マスの最短距離が保証される(ダイクストラ法のような優先度付きキューは不要)。壁がなければ迂回の必要がないため、このフィールドは実際に必要になった時(=直線移動が遮られた敵が最初に現れた時)まで構築されない、遅延構築
- **キャッシュ**: 一度構築したフィールドは`FLOW_FIELD_REFRESH_INTERVAL = 0.4`秒の間再利用される(`Game.flowFieldCache`)。同じフレーム・同じ0.4秒の間に複数の敵が同時にブロックされていても、フィールドの再構築は1回で済む
- **迂回方向の決定(`flowFieldDirectionAt(x, y)`)**: 敵の現在位置が属するマスの上下左右4マスのうち、記録されている距離が最も小さい(=自機により近い)マスへ向かう方向をそのまま移動方向として採用する。対象の敵がフィールドの範囲外にいる場合や、周囲4マスに到達可能(距離が`Infinity`でない)なマスが1つもない場合は`null`を返し、呼び出し側(敵の移動処理)は自機への直線方向へフォールバックする(壁に沿って止まるだけで、行動不能にはならない)
  - **自分自身のマスの距離値には依存しない(v1.36.52で修正)**: 初期実装では、まず自分がいるマス自身の距離値が有限かどうかを最初にチェックし、`Infinity`(到達不能=壁扱い)なら周囲を見る前に即座に`null`を返していた。ところが、壁に押し出された直後の敵(まさに`resolveWallCollision`が置く位置)は、その敵自身の座標を含むマス自体が壁の矩形と部分的に重なっているせいで「そのマスまるごと通行不可」に分類されていることが普通にある(粗い格子の1マスの一部だけ壁が入り込んでいても、そのマス全体が通行不可扱いになるため)。その結果、壁のすぐそばにいる敵ほど`null`が返りやすく、自機への直線方向へフォールバック→また同じ壁にぶつかる→の繰り返しで、まさに壁際でその場にとどまり続けてしまう不具合の原因になっていた。修正後は自分のマス自身の値を見ずに、周囲4マスそれぞれの距離値だけを直接調べて最小のものを採用するようにし、この早期`null`化を無くした
- **移動の見た目**: 斜め移動を許さない4方向BFSのため、迂回中の敵は壁の角を曲がる際にやや直角的な(階段状の)軌道を描く。滑らかな斜め迂回より簡素だが、コーナーの切り抜け(2つの壁マスの間を斜めにすり抜けてしまう)を考える必要が一切ない単純さを優先した

### 10-4. 攻撃との相互作用: 一部の無限貫通武器を除き、攻撃も壁で消える

- **スタンダード・ラピッドファイアの弾(`Projectile`インスタンス)**: 毎フレームの移動処理で、直前フレームの位置から今回の新しい位置までの区間に壁との交差があれば、その場で`life = 0`にして消滅させる(貫通の残り回数に関わらず即座に消える - 壁は「貫通しきれない障害物」であり、敵を貫通した扱いにはならない)
- **パルスウェーブ(近接の前後同時衝撃波)**: 瞬間発生型のため`Projectile`は使わないが、ヒット判定のループ内で対象の敵ごとに自機からその敵への視線(`segmentHitsWall(p.x, p.y, e.x, e.y)`)を個別にチェックし、壁越しの敵はヒット対象から除外する。射程内・幅内に入っていても、壁の向こうにいれば衝撃波は届かない
- **チャージビーム(唯一の例外、無限貫通武器)**: 一切`segmentHitsWall`を呼ばない。この武器の identity そのものが「本物の無限貫通」であるため、他の攻撃と同じ扱いにせず、壁の有無に関わらず狙った方向の敵を全員貫通してヒットさせる(実装上は該当箇所に`segmentHitsWall`を呼ばないコメントを明示し、後から「チェック漏れ」として誤って追加されないようにしている)
- **弾丸効果・着弾効果への波及は対象外**: 爆発の飛び火・連鎖(chain)・着弾効果ゾーン(§4-3-2)など、一次ヒットから波及する二次的な効果には壁判定を追加していない。これらはあくまで既にヒットした後の副次効果であり、一次ヒット自体が壁でブロックされていれば発生しようがないため、追加のスコープとしなかった

### 10-5. 描画

`draw()`内、背景グリッドの直後(ゲーム内のあらゆる動くオブジェクトより手前)に描画する。現在のビューポート(カメラズームを反映した実際に見えているワールド範囲)と重なるチャンクだけを`wallChunks`から取得して描く(生成済みの全チャンクを毎フレーム舐めるコストは避けている)。単色の塗り矩形+縁取りのシンプルな見た目で、他のオブジェクトと同じ「単色の円/矩形」という作品全体の作画スタイルに合わせている。
